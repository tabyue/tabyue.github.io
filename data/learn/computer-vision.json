{
  "id": "computer-vision",
  "name": "计算机视觉基础",
  "stage": "阶段三：感知与视觉",
  "importance": "核心",
  "estimatedHours": 50,
  "description": "图像处理、特征提取、目标检测是机器人感知环境的第一步。从 OpenCV 到 YOLO 到 SAM，构建完整的视觉感知能力。",
  "prerequisites": [
    "Python 编程",
    "深度学习基础"
  ],
  "sections": [
    {
      "title": "图像处理 (OpenCV)",
      "content": "### 计算机视觉基础\n\n计算机视觉是让机器\"看懂\"图像和视频的技术。在具身智能中，视觉是机器人获取环境信息的**最重要**传感器。\n\n### 图像的本质\n\n数字图像就是一个**三维数组** (高, 宽, 通道)：\n- 灰度图：(H, W, 1)，每个像素 0-255\n- 彩色图：(H, W, 3)，RGB 三个通道\n- 深度图：(H, W, 1)，每个像素是距离值（米）\n\n```\n一张 640×480 的 RGB 图像：\n- 尺寸：480 × 640 × 3 = 921,600 个数值\n- 内存：约 0.9 MB（uint8）\n```\n\n### OpenCV 能做什么？\n\nOpenCV 是最广泛使用的计算机视觉库，提供了从底层到高层的各种功能：\n\n| 层级 | 功能 | 示例 |\n|------|------|------|\n| 像素级 | 颜色空间转换、阈值化 | BGR→HSV、二值化 |\n| 滤波 | 模糊、锐化、边缘检测 | 高斯模糊、Canny、Sobel |\n| 几何 | 缩放、旋转、透视变换 | 图像矫正、裁剪 |\n| 特征 | 角点、关键点检测 | Harris、ORB、SIFT |\n| 分割 | 轮廓、连通域 | 物体分割、计数 |\n| 3D | 标定、立体匹配 | 相机标定、深度估计 |\n\n### 在机器人中的典型应用\n\n1. **ArUco 标记检测**：用于机器人定位和物体追踪\n2. **颜色分割**：在 HSV 空间中按颜色过滤目标物体\n3. **相机标定**：获取内参矩阵，将像素坐标转为3D坐标\n4. **手眼标定**：确定相机和机械臂之间的变换关系\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> pip install opencv-python\n> ```\n\n\n**📌 演示：OpenCV 基础图像处理** — 读取图像、颜色空间转换、高斯模糊、Canny 边缘检测、轮廓提取：\n\n```python\nimport cv2\nimport numpy as np\n\n# 读取和显示\nimg = cv2.imread('scene.jpg')\nprint(f'图像形状: {img.shape}')  # (H, W, 3) BGR 格式\n\n# 色彩空间转换\ngray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\nhsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\n\n# 高斯滤波（降噪）\nblurred = cv2.GaussianBlur(gray, (5, 5), 0)\n\n# Canny 边缘检测\nedges = cv2.Canny(blurred, 50, 150)\n\n# 形态学操作\nkernel = np.ones((5, 5), np.uint8)\ndilated = cv2.dilate(edges, kernel, iterations=1)\neroded = cv2.erode(edges, kernel, iterations=1)\n\n# 轮廓检测\ncontours, _ = cv2.findContours(dilated, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)\nprint(f'检测到 {len(contours)} 个轮廓')\n\n# 在原图上画轮廓\nresult = img.copy()\ncv2.drawContours(result, contours, -1, (0, 255, 0), 2)\n```\n\n### 相机标定（张正友法）\n\n```python\n# 准备棋盘格标定板数据\ncriteria = (cv2.TERM_CRITERIA_EPS + cv2.TERM_CRITERIA_MAX_ITER, 30, 0.001)\nobjp = np.zeros((6*9, 3), np.float32)\nobjp[:, :2] = np.mgrid[0:9, 0:6].T.reshape(-1, 2) * 0.025  # 25mm 格子\n\nobj_points, img_points = [], []\n\nfor img_path in calibration_images:\n    img = cv2.imread(img_path)\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    ret, corners = cv2.findChessboardCorners(gray, (9, 6), None)\n    if ret:\n        corners2 = cv2.cornerSubPix(gray, corners, (11, 11), (-1, -1), criteria)\n        obj_points.append(objp)\n        img_points.append(corners2)\n\nret, K, dist, rvecs, tvecs = cv2.calibrateCamera(\n    obj_points, img_points, gray.shape[::-1], None, None)\n\nprint(f'内参矩阵 K:\\n{K}')\nprint(f'重投影误差: {ret:.4f} 像素')\n```\n\n### 练习\n1. 用 OpenCV 检测图像中的红色物体（HSV 阈值分割 + 轮廓检测）\n2. 实现相机标定：打印棋盘格 → 多角度拍照 → 标定 → 去畸变\n3. 实现基于颜色的物体追踪：实时视频中标记并追踪特定颜色物体\n\n### 参考答案\n\n**1. 用 OpenCV 检测图像中的红色物体（HSV 阈值分割 + 轮廓检测）**\n\n```python\nimport cv2\nimport numpy as np\n\ndef detect_red_objects(image_path=None):\n    \"\"\"HSV颜色空间中检测红色物体\n    \n    红色在HSV中跨越0度，需要两个范围:\n    低范围: H=0-10, S=100-255, V=100-255\n    高范围: H=160-180, S=100-255, V=100-255\n    \"\"\"\n    if image_path:\n        img = cv2.imread(image_path)\n    else:\n        # 生成测试图像（带红色、蓝色、绿色物体）\n        img = np.ones((400, 600, 3), dtype=np.uint8) * 200  # 灰色背景\n        cv2.circle(img, (150, 200), 50, (0, 0, 220), -1)     # 红色圆\n        cv2.circle(img, (300, 200), 40, (220, 0, 0), -1)     # 蓝色圆\n        cv2.rectangle(img, (400, 150), (500, 250), (0, 200, 0), -1)  # 绿色方块\n        cv2.circle(img, (450, 350), 35, (0, 0, 180), -1)     # 深红色圆\n    \n    # BGR → HSV\n    hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\n    \n    # 红色的两个HSV范围\n    lower_red1 = np.array([0, 100, 100])\n    upper_red1 = np.array([10, 255, 255])\n    lower_red2 = np.array([160, 100, 100])\n    upper_red2 = np.array([180, 255, 255])\n    \n    mask1 = cv2.inRange(hsv, lower_red1, upper_red1)\n    mask2 = cv2.inRange(hsv, lower_red2, upper_red2)\n    mask = mask1 | mask2\n    \n    # 形态学操作去噪\n    kernel = np.ones((5, 5), np.uint8)\n    mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, kernel)   # 去小噪点\n    mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel)  # 填小洞\n    \n    # 轮廓检测\n    contours, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)\n    \n    result = img.copy()\n    red_objects = []\n    for cnt in contours:\n        area = cv2.contourArea(cnt)\n        if area > 500:  # 过滤小噪点\n            x, y, w, h = cv2.boundingRect(cnt)\n            cx, cy = x + w//2, y + h//2\n            cv2.rectangle(result, (x, y), (x+w, y+h), (0, 255, 0), 2)\n            cv2.putText(result, f'Red ({area:.0f}px)', (x, y-10),\n                       cv2.FONT_HERSHEY_SIMPLEX, 0.5, (0, 255, 0), 2)\n            red_objects.append({'center': (cx, cy), 'area': area, 'bbox': (x,y,w,h)})\n    \n    print(f'检测到 {len(red_objects)} 个红色物体')\n    for i, obj in enumerate(red_objects):\n        print(f'  物体{i+1}: 中心={obj[\"center\"]}, 面积={obj[\"area\"]:.0f}px')\n    \n    cv2.imwrite('red_detection.png', result)\n    print('已保存 red_detection.png')\n    return red_objects\n\ndetect_red_objects()\n```\n\n**2. 实现相机标定**\n\n```python\nimport cv2\nimport numpy as np\n\ndef camera_calibration_demo():\n    \"\"\"完整的相机标定流程\n    \n    步骤: 打印棋盘格 → 多角度拍照 → 检测角点 → 标定 → 去畸变\n    这里用模拟数据演示整个流程。\n    \"\"\"\n    # 棋盘格参数\n    board_size = (9, 6)  # 内角点数\n    square_size = 0.025   # 格子边长 25mm\n    \n    # 3D 世界坐标\n    objp = np.zeros((board_size[0]*board_size[1], 3), np.float32)\n    objp[:, :2] = np.mgrid[0:board_size[0], 0:board_size[1]].T.reshape(-1, 2)\n    objp *= square_size\n    \n    # 模拟内参（已知的真实值用于验证）\n    true_K = np.array([[600, 0, 320],\n                       [0, 600, 240],\n                       [0, 0, 1]], dtype=np.float64)\n    true_dist = np.array([-0.2, 0.1, 0, 0, 0], dtype=np.float64)\n    \n    # 模拟多角度拍摄（生成虚拟角点）\n    obj_points, img_points = [], []\n    for i in range(15):  # 15张图\n        # 随机旋转和平移\n        rvec = np.random.randn(3) * 0.3\n        tvec = np.array([0, 0, 0.3]) + np.random.randn(3) * 0.05\n        \n        # 投影到图像\n        img_pts, _ = cv2.projectPoints(objp, rvec, tvec, true_K, true_dist)\n        img_pts = img_pts.reshape(-1, 1, 2).astype(np.float32)\n        \n        # 检查是否在图像内\n        if img_pts.min() > 10 and img_pts.max() < 630:\n            obj_points.append(objp)\n            img_points.append(img_pts)\n    \n    print(f'使用 {len(obj_points)} 张有效标定图片')\n    \n    # 标定\n    ret, K, dist, rvecs, tvecs = cv2.calibrateCamera(\n        obj_points, img_points, (640, 480), None, None)\n    \n    print(f'\\n重投影误差: {ret:.4f} 像素')\n    print(f'\\n估计内参 K:\\n{np.round(K, 2)}')\n    print(f'\\n真实内参 K:\\n{true_K}')\n    print(f'\\n焦距误差: fx={abs(K[0,0]-true_K[0,0]):.2f}, fy={abs(K[1,1]-true_K[1,1]):.2f}')\n    print(f'主点误差: cx={abs(K[0,2]-true_K[0,2]):.2f}, cy={abs(K[1,2]-true_K[1,2]):.2f}')\n    \n    # 去畸变演示\n    print(f'\\n畸变系数: {np.round(dist.flatten()[:5], 4)}')\n    print(f'真实畸变: {true_dist}')\n    print('\\ncv2.undistort(img, K, dist) 即可去畸变')\n\ncamera_calibration_demo()\n```\n\n**3. 基于颜色的物体追踪**\n\n```python\nimport cv2\nimport numpy as np\n\nclass ColorTracker:\n    \"\"\"基于颜色的实时物体追踪器\n    \n    用HSV阈值 + 轮廓检测 + 卡尔曼滤波平滑。\n    适合追踪单一颜色的物体。\n    \"\"\"\n    def __init__(self, color='red'):\n        self.color = color\n        self.hsv_ranges = {\n            'red':   [([0,100,100], [10,255,255]), ([160,100,100], [180,255,255])],\n            'blue':  [([100,100,100], [130,255,255])],\n            'green': [([35,100,100], [85,255,255])],\n            'yellow':[([20,100,100], [35,255,255])],\n        }\n        self.positions = []  # 追踪历史\n    \n    def detect(self, frame):\n        hsv = cv2.cvtColor(frame, cv2.COLOR_BGR2HSV)\n        mask = np.zeros(frame.shape[:2], dtype=np.uint8)\n        \n        for lower, upper in self.hsv_ranges.get(self.color, []):\n            mask |= cv2.inRange(hsv, np.array(lower), np.array(upper))\n        \n        # 形态学\n        kernel = np.ones((7, 7), np.uint8)\n        mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, kernel)\n        mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel)\n        \n        contours, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)\n        \n        if contours:\n            # 选最大轮廓\n            largest = max(contours, key=cv2.contourArea)\n            if cv2.contourArea(largest) > 300:\n                M = cv2.moments(largest)\n                if M['m00'] > 0:\n                    cx = int(M['m10'] / M['m00'])\n                    cy = int(M['m01'] / M['m00'])\n                    self.positions.append((cx, cy))\n                    return (cx, cy), largest\n        return None, None\n    \n    def draw(self, frame, center, contour):\n        if center and contour is not None:\n            cv2.drawContours(frame, [contour], -1, (0, 255, 0), 2)\n            cv2.circle(frame, center, 8, (0, 0, 255), -1)\n            # 画轨迹\n            for i in range(1, min(len(self.positions), 30)):\n                pt1 = self.positions[-i]\n                pt2 = self.positions[-i-1]\n                cv2.line(frame, pt1, pt2, (255, 0, 0), 2)\n            cv2.putText(frame, f'{self.color} ({center[0]},{center[1]})',\n                       (center[0]+10, center[1]-10),\n                       cv2.FONT_HERSHEY_SIMPLEX, 0.5, (0,255,0), 2)\n        return frame\n\n# 使用方式\ntracker = ColorTracker(color='red')\n\n# 实时追踪（需要摄像头）：\n# cap = cv2.VideoCapture(0)\n# while True:\n#     ret, frame = cap.read()\n#     center, contour = tracker.detect(frame)\n#     frame = tracker.draw(frame, center, contour)\n#     cv2.imshow('Tracking', frame)\n#     if cv2.waitKey(1) == ord('q'): break\n\n# 静态测试\ntest_img = np.ones((400, 600, 3), dtype=np.uint8) * 180\ncv2.circle(test_img, (200, 200), 40, (0, 0, 200), -1)  # 红色圆\ncenter, contour = tracker.detect(test_img)\nprint(f'检测到{tracker.color}物体: 中心={center}')\nresult = tracker.draw(test_img, center, contour)\ncv2.imwrite('color_tracking.png', result)\nprint('已保存 color_tracking.png')\n```\n"
    },
    {
      "title": "目标检测 (YOLO/DETR)",
      "content": "### 目标检测：让机器人知道\"什么在哪里\"\n\n**目标检测**是计算机视觉的核心任务之一：在图像中找到所有物体，并输出每个物体的**类别**和**边界框**。\n\n```\n输入: 一张图像\n输出: [(类别:杯子, 位置:[x1,y1,x2,y2], 置信度:0.95),\n       (类别:手机, 位置:[x3,y3,x4,y4], 置信度:0.87),\n       ...]\n```\n\n### 检测算法的两大流派\n\n**两阶段（Two-stage）**：Faster R-CNN\n```\n图像 → 骨干网络 → 区域提议网络(RPN) → 候选框\n                                          ↓\n                                   ROI Pooling → 分类+回归\n```\n- 先找\"可能有物体\"的区域，再精确分类\n- 精度高，但速度慢（5-15 FPS）\n\n**单阶段（One-stage）**：YOLO / SSD / DETR\n```\n图像 → 骨干网络 → 直接预测所有框的类别+位置\n```\n- 一步到位，不需要候选框\n- 速度快（30-100+ FPS），精度略低\n\n### YOLO 系列\n\nYOLO（You Only Look Once）是最流行的实时检测器：\n\n| 版本 | 年份 | 特点 | FPS | mAP |\n|------|------|------|-----|-----|\n| YOLOv5 | 2020 | PyTorch实现，易用 | 140 | 50.7 |\n| YOLOv8 | 2023 | Ultralytics，当前主流 | 150 | 53.9 |\n| YOLO11 | 2024 | 最新版本 | 160 | 54.7 |\n\n### DETR（DEtection TRansformer）\n\nDETR 用 Transformer 做检测，不需要 NMS（非极大值抑制）等手工后处理：\n\n```\n图像 → CNN骨干 → Transformer Encoder → Transformer Decoder → 直接输出N个预测\n                                             ↑\n                                        学习的查询向量\n                                       (每个查询负责一个物体)\n```\n\n### 在机器人中怎么用？\n\n1. **物体检测 → 抓取**：检测到杯子 → 估计位姿 → 规划抓取\n2. **Grounding DINO**：语言指令 \"把红色杯子给我\" → 检测红色杯子\n3. **SAM（Segment Anything）**：点击/框选 → 精确分割物体轮廓\n\n```python\nfrom ultralytics import YOLO\n\n# 加载预训练模型\nmodel = YOLO('yolov8n.pt')  # nan\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> pip install opencv-python\n> ```\n\no 版本，最快\n\n# 单张图片推理\nresults = model('scene.jpg')\n\n# 解析结果\nfor r in results:\n    for box in r.boxes:\n        x1, y1, x2, y2 = box.xyxy[0].tolist()\n        conf = box.conf[0].item()\n        cls = int(box.cls[0].item())\n        name = model.names[cls]\n        print(f'{name}: ({x1:.0f},{y1:.0f})-({x2:.0f},{y2:.0f}) conf={conf:.2f}')\n\n# 实时视频检测\nimport cv2\ncap = cv2.VideoCapture(0)  # 摄像头\nwhile True:\n    ret, frame = cap.read()\n    if not ret: break\n    results = model(frame, verbose=False)\n    annotated = results[0].plot()  # 画框\n    cv2.imshow('YOLO', annotated)\n    if cv2.waitKey(1) == ord('q'): break\ncap.release()\n```\n\n### Grounding DINO（开放词汇检测）\n\n输入任意文字描述就能检测对应物体，不需要预定义类别。\n\n```python\n# 用 GroundingDINO 检测\"红色杯子\"\nfrom groundingdino.util.inference import load_model, predict\n\nmodel = load_model('config.py', 'weights.pth')\nboxes, logits, phrases = predict(\n    model, image, caption='red cup . bottle . plate',\n    box_threshold=0.3, text_threshold=0.25)\n# 返回每个检测到的物体的边界框和对应文字\n```\n\n### SAM (Segment Anything)\n\nMeta 的通用分割模型，zero-shot 分割任何物体：\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> pip install segment-anything\n> ```\n\n```python\nfrom segment_anything import SamPredictor, sam_model_registry\n\nsam = sam_model_registry['vit_h'](checkpoint='sam_vit_h.pth')\npredictor = SamPredictor(sam)\n\npredictor.set_image(image)\n# 给一个点提示\nmasks, scores, _ = predictor.predict(\n    point_coords=np.array([[500, 300]]),\n    point_labels=np.array([1]),  # 1=前景\n    multimask_output=True)\n\nbest_mask = masks[scores.argmax()]\n```\n\n### 机器人视觉 Pipeline\n\n```\nRealSense 图像 → YOLO 检测物体 → SAM 精确分割 → 点云提取 → 6DoF 位姿估计 → 抓取规划\n```\n\n[video:https://www.youtube.com/watch?v=ag3DLKsl2vk:YOLOv8 实时目标检测演示]\n\n\n### 目标检测模型选择指南\n\n| 模型 | 速度 | 精度 | 特点 | 适用场景 |\n|------|:----:|:----:|------|---------|\n| YOLOv8n | 1ms | 37.3 mAP | 极快 | 嵌入式/实时 |\n| YOLOv8l | 10ms | 52.9 mAP | 平衡 | 通用检测 |\n| DETR | 40ms | 42 mAP | 端到端 | 不需要NMS |\n| Grounding DINO | 50ms | — | 开放词汇 | 零样本检测 |\n| YOLO-World | 5ms | — | 开放词汇+快 | 实时零样本 |\n\n### 机器人视觉Pipeline选择\n\n```\n已知物体类别? ──→ 是 ──→ YOLOv8 (训练自定义检测器)\n      │ 否                    速度快、部署简单\n      ▼\n需要实时? ──→ 是 ──→ YOLO-World (开放词汇)\n      │ 否              文字描述即可检测任意物体\n      ▼\nGrounding DINO + SAM    ← 最灵活：检测+精确分割\n```\n### 练习\n1. 用 YOLOv8 训练自定义物体检测器（桌面常见物品，10 个类别）\n2. 组合 Grounding DINO + SAM：输入\"要抓的物体描述\"→ 输出精确分割 mask\n3. 实现完整视觉抓取 Pipeline：检测 → 分割 → 提取最高点 → 规划抓取\n\n### 参考答案\n\n**1. 用 YOLOv8 训练自定义物体检测器**\n\n> **🔧 环境准备：** `pip install ultralytics`\n\n```python\n# yolo_custom_train.py - 训练自定义检测器\n# 步骤1: 准备数据集 (YOLO格式)\n# dataset/\n#   images/train/  ← 训练图片\n#   images/val/    ← 验证图片\n#   labels/train/  ← 标注文件 (每行: class cx cy w h, 归一化)\n#   labels/val/\n\nimport os\n\n# 创建 dataset.yaml\ndataset_yaml = \"\"\"\npath: ./dataset\ntrain: images/train\nval: images/val\n\nnames:\n  0: cup\n  1: bottle\n  2: phone\n  3: keyboard\n  4: mouse\n  5: book\n  6: pen\n  7: scissors\n  8: remote\n  9: charger\n\"\"\"\n\nwith open('dataset.yaml', 'w') as f:\n    f.write(dataset_yaml)\n\nprint('数据集配置已创建: dataset.yaml')\nprint('\\n标注格式（每行一个物体）:')\nprint('  class_id  center_x  center_y  width  height')\nprint('  0         0.45      0.32      0.12   0.08')\nprint('  3         0.71      0.55      0.25   0.15')\nprint('\\n所有坐标归一化到 [0, 1]')\n\n# 步骤2: 训练\nprint('\\n训练命令:')\nprint('from ultralytics import YOLO')\nprint('model = YOLO(\"yolov8n.pt\")  # 从预训练权重开始')\nprint('model.train(')\nprint('    data=\"dataset.yaml\",')\nprint('    epochs=100,')\nprint('    imgsz=640,')\nprint('    batch=16,')\nprint('    lr0=0.01,')\nprint('    name=\"desktop_objects\"')\nprint(')')\n\n# 步骤3: 推理\nprint('\\n推理:')\nprint('model = YOLO(\"runs/detect/desktop_objects/weights/best.pt\")')\nprint('results = model(\"test_image.jpg\")')\nprint('results[0].show()  # 可视化')\n\n# 推荐标注工具\nprint('\\n推荐标注工具:')\nprint('  - Label Studio: https://labelstud.io/')\nprint('  - Roboflow: https://roboflow.com/ (在线标注+自动增强)')\nprint('  - CVAT: https://cvat.ai/')\n```\n\n**2. 组合 Grounding DINO + SAM：文字描述 → 精确分割 mask**\n\n```python\nimport numpy as np\n\ndef grounding_dino_sam_pipeline(image, text_prompt):\n    \"\"\"Grounding DINO + SAM 的完整 Pipeline\n    \n    流程: 文字描述 → 检测框 → 精确分割\n    这是最灵活的零样本物体分割方案。\n    \"\"\"\n    print(f'=== 文字引导分割 Pipeline ===')\n    print(f'输入文字: \\\"{text_prompt}\\\"')\n    \n    # Step 1: Grounding DINO 检测\n    # from groundingdino.util.inference import load_model, predict\n    # gd_model = load_model('GroundingDINO_SwinT_OGC.py', 'groundingdino_swint_ogc.pth')\n    # boxes, logits, phrases = predict(gd_model, image, text_prompt,\n    #                                   box_threshold=0.3, text_threshold=0.25)\n    \n    # 模拟检测结果\n    boxes = np.array([[0.3, 0.2, 0.5, 0.6]])  # xyxy 归一化\n    logits = np.array([0.85])\n    phrases = [text_prompt.split('.')[0].strip()]\n    \n    print(f'\\nStep 1 - Grounding DINO:')\n    for i, (box, score, phrase) in enumerate(zip(boxes, logits, phrases)):\n        print(f'  检测到 \\\"{phrase}\\\": box={box.round(3)}, conf={score:.2f}')\n    \n    # Step 2: SAM 精确分割\n    # from segment_anything import SamPredictor, sam_model_registry\n    # sam = sam_model_registry['vit_h'](checkpoint='sam_vit_h.pth')\n    # predictor = SamPredictor(sam)\n    # predictor.set_image(image)\n    # \n    # H, W = image.shape[:2]\n    # for box in boxes:\n    #     box_pixel = box * np.array([W, H, W, H])\n    #     masks, scores, _ = predictor.predict(\n    #         box=box_pixel, multimask_output=True)\n    #     best_mask = masks[scores.argmax()]\n    \n    print(f'\\nStep 2 - SAM 分割:')\n    mask_area = 0.15  # 模拟\n    print(f'  生成精确分割 mask')\n    print(f'  mask 面积: 图像的 {mask_area*100:.0f}%')\n    print(f'  mask 形状: 像素级精确轮廓')\n    \n    # Step 3: 后处理\n    print(f'\\nStep 3 - 后处理:')\n    print(f'  提取 mask 内的点云 → 估计抓取点')\n    print(f'  mask 边界框 → 物体中心位置')\n    \n    return {'boxes': boxes, 'masks': None, 'phrases': phrases}\n\n# 使用\nimage = np.zeros((480, 640, 3), dtype=np.uint8)  # 模拟图像\nresult = grounding_dino_sam_pipeline(image, 'red cup . blue bottle')\n\nprint('\\n安装命令:')\nprint('pip install groundingdino-py segment-anything')\nprint('# 下载权重: https://github.com/IDEA-Research/GroundingDINO')\nprint('# SAM权重: https://segment-anything.com/')\n```\n\n**3. 完整视觉抓取 Pipeline**\n\n```python\nimport numpy as np\n\nclass VisionGraspPipeline:\n    \"\"\"完整的视觉抓取流程\n    \n    RGB-D图像 → 检测 → 分割 → 点云 → 抓取点 → 执行\n    \"\"\"\n    def __init__(self):\n        self.camera_K = np.array([[600,0,320],[0,600,240],[0,0,1]])  # 内参\n    \n    def detect(self, rgb, target='cup'):\n        \"\"\"YOLO 检测目标物体\"\"\"\n        # results = yolo_model(rgb)\n        bbox = np.array([200, 150, 350, 300])  # 模拟\n        print(f'  [检测] 发现 \\\"{target}\\\": bbox={bbox}')\n        return bbox\n    \n    def segment(self, rgb, bbox):\n        \"\"\"SAM 精确分割\"\"\"\n        # 生成物体mask\n        mask = np.zeros((480, 640), dtype=bool)\n        mask[bbox[1]:bbox[3], bbox[0]:bbox[2]] = True\n        pixel_count = mask.sum()\n        print(f'  [分割] mask 像素数: {pixel_count}')\n        return mask\n    \n    def extract_pointcloud(self, depth, mask):\n        \"\"\"从 depth + mask 提取物体点云\"\"\"\n        # 模拟深度图\n        if depth is None:\n            depth = np.ones((480, 640)) * 0.5  # 50cm\n        \n        ys, xs = np.where(mask)\n        if len(xs) == 0:\n            return np.array([])\n        \n        # 像素 → 3D (反投影)\n        fx, fy = self.camera_K[0,0], self.camera_K[1,1]\n        cx, cy = self.camera_K[0,2], self.camera_K[1,2]\n        Z = depth[ys, xs]\n        X = (xs - cx) * Z / fx\n        Y = (ys - cy) * Z / fy\n        points = np.stack([X, Y, Z], axis=1)\n        \n        print(f'  [点云] 提取 {len(points)} 个3D点')\n        print(f'    中心: ({points[:,0].mean():.3f}, {points[:,1].mean():.3f}, {points[:,2].mean():.3f})m')\n        return points\n    \n    def compute_grasp(self, points):\n        \"\"\"计算最优抓取位姿\"\"\"\n        if len(points) == 0:\n            return None\n        \n        center = points.mean(axis=0)\n        # 最高点作为顶部抓取点\n        top_idx = points[:, 1].argmin()  # y最小 = 最高\n        top_point = points[top_idx]\n        \n        # 抓取位姿: 从上方接近\n        grasp_pos = top_point + np.array([0, -0.05, 0])  # 上方5cm\n        grasp_approach = np.array([0, 1, 0])  # 垂直向下\n        \n        print(f'  [抓取] 顶部抓取点: {top_point.round(3)}m')\n        print(f'    接近方向: 垂直向下')\n        print(f'    预抓取位置: {grasp_pos.round(3)}m')\n        return {'position': grasp_pos, 'approach': grasp_approach}\n    \n    def execute(self, target='cup'):\n        \"\"\"执行完整Pipeline\"\"\"\n        print(f'\\n=== 视觉抓取 Pipeline: 目标=\"{target}\" ===')\n        \n        rgb = np.zeros((480, 640, 3), dtype=np.uint8)  # 模拟\n        depth = np.ones((480, 640)) * 0.5\n        \n        bbox = self.detect(rgb, target)\n        mask = self.segment(rgb, bbox)\n        points = self.extract_pointcloud(depth, mask)\n        grasp = self.compute_grasp(points)\n        \n        if grasp:\n            print(f'\\n  [执行] 移动到预抓取位 → 下降 → 闭合夹爪 → 抬升')\n            print(f'  ✅ 抓取成功!')\n        return grasp\n\npipeline = VisionGraspPipeline()\npipeline.execute('red cup')\n```\n"
    },
    {
      "title": "姿态估计",
      "content": "### 姿态估计：让机器人知道\"物体怎么摆的\"\n\n目标检测告诉我们物体在图像中的 2D 位置。但机器人要抓取物体，还需要知道物体在 3D 空间中的**精确位姿**（6DoF：3D 位置 + 3D 旋转）。\n\n```\n2D 检测:  \"杯子在图像的 (320, 240) 位置\"  → 不够！\n6DoF 姿态: \"杯子在机器人坐标系 (0.3, -0.1, 0.8)m，\n            绕 z 轴旋转 45°，杯口朝上\"      → 可以抓了！\n```\n\n### 姿态估计的方法分类\n\n| 方法 | 输入 | 原理 | 精度 | 速度 |\n|------|------|------|------|------|\n| **PnP + 特征点** | RGB | 检测关键点 → 求解 PnP | 中 | 快 |\n| **ICP 配准** | 点云 | 迭代最近点对齐 | 高 | 慢 |\n| **FoundationPose** | RGB-D | 深度学习端到端 | 高 | 中 |\n| **BundleSDF** | RGB-D序列 | 隐式表面+追踪 | 很高 | 慢 |\n| **ArUco 标记** | RGB | 已知标记图案 | 中高 | 很快 |\n\n### 在机器人操作中的典型 Pipeline\n\n```\nRGB-D图像\n    ↓\n目标检测(YOLO) → 目标区域裁剪\n    ↓\n姿态估计(FoundationPose) → 6DoF 位姿\n    ↓\n坐标变换(相机→机器人) → 机器人坐标系中的位姿\n    ↓\n抓取规划(GraspNet) → 最优抓取位姿\n    ↓\n运动规划(MoveIt2) → 关节轨迹\n    ↓\n执行\n```\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> pip install opencv-python\n> ```\n\n\n**📌 演示：ArUco 标记检测与位姿估计** — 使用 OpenCV 检测 ArUco 标记，计算其 6DoF 位姿：\n\n```python\nimport mediapipe as mp\nimport cv2\n\n# MediaPipe 手部姿态\nmp_hands = mp.solutions.hands\nhands = mp_hands.Hands(max_num_hands=2, min_detection_confidence=0.7)\nmp_draw = mp.solutions.drawing_utils\n\ncap = cv2.VideoCapture(0)\nwhile cap.isOpened():\n    ret, frame = cap.read()\n    rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)\n    results = hands.process(rgb)\n    \n    if results.multi_hand_landmarks:\n        for hand_lms in results.multi_hand_landmarks:\n            mp_draw.draw_landmarks(frame, hand_lms, mp_hands.HAND_CONNECTIONS)\n            # 21 个关键点\n            for i, lm in enumerate(hand_lms.landmark):\n                h, w, _ = frame.shape\n                cx, cy = int(lm.x * w), int(lm.y * h)\n    \n    cv2.imshow('Hands', frame)\n    if cv2.waitKey(1) == ord('q'): break\n```\n\n### 物体 6DoF 位姿估计\n\n已知物体 CAD 模型或参考图，估计它在 3D 空间中的位置和朝向。\n\n- **FoundationPose**（NVIDIA）：zero-shot，给一个 CAD 模型或 16 张参考图就能估计任意物体位姿\n- **MegaPose**：大规模预训练，泛化能力强\n- **GigaPose**：模板匹配 + 精细化\n\n### 在机器人中的应用\n\n- **遥操作**：用手部姿态控制机械臂\n- **模仿学习**：从人类视频中提取动作轨迹\n- **抓取规划**：知道物体 6DoF 位姿才能规划抓取\n\n### 练习\n1. 用 MediaPipe 实现手势识别：检测手部姿态 → 分类手势（握拳/张开/OK）\n2. 用手部姿态遥控一个仿真机械臂：手指张合 → 夹爪开合\n3. 用 FoundationPose 估计桌面物体的 6DoF 位姿\n\n### 参考答案\n\n**1. 用 MediaPipe 实现手势识别：握拳/张开/OK**\n\n```python\nimport numpy as np\n\nclass HandGestureClassifier:\n    \"\"\"基于 MediaPipe 手部关键点的手势分类器\n    \n    21个关键点的几何关系判断手势:\n    - 握拳: 所有手指尖低于指根\n    - 张开: 所有手指伸直\n    - OK:   拇指尖和食指尖距离近,其余伸直\n    \n    关键点索引:\n    0=手腕, 4=拇指尖, 8=食指尖, 12=中指尖, 16=无名指尖, 20=小指尖\n    5=食指根, 9=中指根, 13=无名指根, 17=小指根\n    \"\"\"\n    def __init__(self):\n        self.finger_tips = [4, 8, 12, 16, 20]\n        self.finger_bases = [2, 5, 9, 13, 17]\n    \n    def classify(self, landmarks):\n        \"\"\"landmarks: (21, 3) 归一化坐标 [x, y, z]\"\"\"\n        lm = np.array(landmarks)\n        \n        # 判断每根手指是否伸直 (tip 比 base 更靠上 = y更小)\n        fingers_up = []\n        # 拇指特殊: 用x方向判断\n        thumb_up = abs(lm[4][0] - lm[2][0]) > 0.05\n        fingers_up.append(thumb_up)\n        \n        for tip, base in zip(self.finger_tips[1:], self.finger_bases[1:]):\n            fingers_up.append(lm[tip][1] < lm[base][1])  # y越小越高\n        \n        n_up = sum(fingers_up)\n        \n        # 拇指-食指距离 (OK手势)\n        thumb_index_dist = np.linalg.norm(lm[4] - lm[8])\n        \n        # 分类\n        if n_up <= 1:\n            return 'fist', '✊ 握拳'\n        elif n_up >= 4:\n            if thumb_index_dist < 0.05:\n                return 'ok', '👌 OK'\n            return 'open', '✋ 张开'\n        elif fingers_up[1] and not fingers_up[2]:  # 只有食指\n            return 'point', '☝️ 指向'\n        elif fingers_up[1] and fingers_up[2]:  # 食指+中指\n            return 'peace', '✌️ 剪刀'\n        else:\n            return 'unknown', '❓ 未知'\n\n# 模拟测试\nclassifier = HandGestureClassifier()\n\n# 模拟张开手掌的关键点\nopen_hand = np.zeros((21, 3))\nfor i, tip in enumerate([4, 8, 12, 16, 20]):\n    open_hand[tip] = [0.1*i, 0.1, 0]  # tip 在上方(y小)\nfor i, base in enumerate([2, 5, 9, 13, 17]):\n    open_hand[base] = [0.1*i, 0.5, 0]  # base 在下方(y大)\n\ngesture, name = classifier.classify(open_hand)\nprint(f'手势: {name} ({gesture})')\n\n# 模拟握拳\nfist = np.zeros((21, 3))\nfor i, tip in enumerate([4, 8, 12, 16, 20]):\n    fist[tip] = [0.1*i, 0.6, 0]  # tip 在下方\nfor i, base in enumerate([2, 5, 9, 13, 17]):\n    fist[base] = [0.1*i, 0.4, 0]  # base 在上方\n\ngesture, name = classifier.classify(fist)\nprint(f'手势: {name} ({gesture})')\n\nprint('\\n完整代码需要 MediaPipe:')\nprint('pip install mediapipe')\nprint('使用 mp.solutions.hands 获取21个关键点坐标')\n```\n\n**2. 用手部姿态遥控仿真机械臂**\n\n```python\nimport numpy as np\n\nclass HandToRobotMapper:\n    \"\"\"手部姿态 → 机械臂控制映射\n    \n    映射规则:\n    - 手掌位置 → 末端执行器目标位置\n    - 手指张合度 → 夹爪开合\n    - 手掌朝向 → 末端姿态\n    \"\"\"\n    def __init__(self, workspace_bounds=([0.2,-0.3,0.0], [0.7,0.3,0.5])):\n        self.ws_min = np.array(workspace_bounds[0])\n        self.ws_max = np.array(workspace_bounds[1])\n    \n    def map_position(self, hand_pos):\n        \"\"\"归一化手部位置[0,1] → 机器人工作空间\"\"\"\n        return self.ws_min + hand_pos * (self.ws_max - self.ws_min)\n    \n    def compute_grip(self, landmarks):\n        \"\"\"从手部关键点计算夹爪开度\n        拇指尖(4)到食指尖(8)的距离 → 夹爪开度[0,1]\n        \"\"\"\n        lm = np.array(landmarks)\n        dist = np.linalg.norm(lm[4] - lm[8])\n        grip = np.clip(dist / 0.15, 0, 1)  # 归一化\n        return grip\n    \n    def get_command(self, landmarks):\n        \"\"\"生成完整的机器人控制命令\"\"\"\n        lm = np.array(landmarks)\n        wrist = lm[0]  # 手腕位置作为末端目标\n        \n        target_pos = self.map_position(wrist[:3])\n        grip = self.compute_grip(landmarks)\n        \n        return {\n            'target_position': target_pos,\n            'gripper_openness': grip,\n            'command_type': 'cartesian'\n        }\n\n# 使用\nmapper = HandToRobotMapper()\n\n# 模拟手部数据\nlandmarks = np.random.rand(21, 3) * 0.5 + 0.25\nlandmarks[4] = [0.3, 0.3, 0.5]  # 拇指尖\nlandmarks[8] = [0.4, 0.3, 0.5]  # 食指尖（张开状态）\n\ncmd = mapper.get_command(landmarks)\nprint(f'机器人控制命令:')\nprint(f'  目标位置: {cmd[\"target_position\"].round(3)}')\nprint(f'  夹爪开度: {cmd[\"gripper_openness\"]:.2f} (1=全开, 0=全闭)')\n\n# 模拟捏合\nlandmarks[8] = [0.31, 0.3, 0.5]  # 食指靠近拇指\ncmd = mapper.get_command(landmarks)\nprint(f'\\n捏合后夹爪: {cmd[\"gripper_openness\"]:.2f} (接近闭合)')\n\nprint('\\n完整流程:')\nprint('MediaPipe手部检测 → HandToRobotMapper → ROS2发布/cmd_vel或MoveIt2')\n```\n\n**3. FoundationPose 6DoF 位姿估计**\n\n```python\nimport numpy as np\n\ndef foundation_pose_pipeline():\n    \"\"\"FoundationPose 使用流程说明\n    \n    FoundationPose 是 NVIDIA 的 zero-shot 6DoF 位姿估计模型。\n    给一个 CAD 模型或 16 张参考图 → 估计物体在相机坐标系中的位姿。\n    \"\"\"\n    print('=== FoundationPose 6DoF 位姿估计 ===')\n    \n    print('\\n1. 安装:')\n    print('   git clone https://github.com/NVlabs/FoundationPose')\n    print('   pip install -r requirements.txt')\n    print('   # 需要 CUDA GPU')\n    \n    print('\\n2. 输入准备:')\n    print('   方式A: CAD模型 (.obj/.ply) — 最精确')\n    print('   方式B: 16张参考图 + 分割mask — 无需CAD')\n    \n    print('\\n3. 推理代码:')\n    code = \"\"\"\n    from estimater import FoundationPose\n    import cv2, trimesh\n    \n    # 加载模型\n    mesh = trimesh.load('cup.obj')  # CAD模型\n    estimator = FoundationPose(\n        model_pts=mesh.vertices,\n        model_normals=mesh.vertex_normals)\n    \n    # 输入 RGB-D\n    color = cv2.imread('color.png')\n    depth = cv2.imread('depth.png', cv2.IMREAD_UNCHANGED) / 1000.0  # mm→m\n    mask = cv2.imread('mask.png', 0) > 0  # 物体分割mask\n    \n    # 初始位姿估计（首帧）\n    pose = estimator.register(\n        color=color, depth=depth, \n        ob_mask=mask, K=camera_matrix)\n    \n    # 后续帧追踪\n    pose = estimator.track_one(\n        color=color, depth=depth, K=camera_matrix)\n    \n    # pose 是 4×4 齐次变换矩阵\n    # position = pose[:3, 3]      # 平移 (x, y, z) 米\n    # rotation = pose[:3, :3]     # 旋转矩阵\n    \"\"\"\n    print(code)\n    \n    # 模拟输出\n    pose = np.eye(4)\n    pose[:3, 3] = [0.35, -0.05, 0.52]  # 模拟位置\n    from scipy.spatial.transform import Rotation\n    pose[:3, :3] = Rotation.from_euler('xyz', [10, -5, 45], degrees=True).as_matrix()\n    \n    print(f'\\n4. 输出示例:')\n    print(f'   位置: {pose[:3, 3].round(3)} m')\n    euler = Rotation.from_matrix(pose[:3, :3]).as_euler('xyz', degrees=True)\n    print(f'   姿态: roll={euler[0]:.1f}°, pitch={euler[1]:.1f}°, yaw={euler[2]:.1f}°')\n    print(f'\\n   这个位姿可以直接用于抓取规划!')\n    print(f'   grasp_pose = T_base_camera @ object_pose @ T_grasp_offset')\n\nfoundation_pose_pipeline()\n```\n"
    }
  ],
  "resources": [
    {
      "name": "CS231n (Stanford)",
      "url": "https://cs231n.stanford.edu/",
      "type": "课程",
      "description": "计算机视觉经典课程，从图像分类到目标检测到分割。"
    },
    {
      "name": "OpenCV 官方教程",
      "url": "https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html",
      "type": "教程",
      "description": "OpenCV Python 教程，涵盖图像处理、特征匹配、相机标定。"
    },
    {
      "name": "Ultralytics YOLOv8",
      "url": "https://docs.ultralytics.com/",
      "type": "文档",
      "description": "YOLO 最新版本文档，支持检测/分割/跟踪/姿态估计。"
    },
    {
      "name": "Segment Anything (SAM)",
      "url": "https://segment-anything.com/",
      "type": "项目",
      "description": "Meta 的通用分割模型，zero-shot 分割任何物体。开源代码+demo。"
    },
    {
      "name": "FoundationPose",
      "url": "https://nvlabs.github.io/FoundationPose/",
      "type": "项目",
      "description": "NVIDIA 的 zero-shot 6DoF 位姿估计模型，给 CAD 或参考图即可。"
    },
    {
      "name": "CS231n Stanford 计算机视觉",
      "url": "https://www.youtube.com/playlist?list=PL3FW7Lu3i5JvHM8ljYj-zLfQRF3EO8sYv",
      "type": "视频",
      "description": "Stanford 经典 CV 课程，从图像分类到目标检测到分割，Fei-Fei Li/Karpathy 等主讲。"
    },
    {
      "name": "YOLO 系列讲解 (AI葵)",
      "url": "https://www.youtube.com/results?search_query=yolo+object+detection+explained",
      "type": "视频",
      "description": "从 YOLOv1 到 YOLOv8 的演进讲解，理解单阶段检测器的核心思想。"
    }
  ],
  "weeklyPlan": "**第1-2周**：OpenCV 图像处理 + 相机标定\n**第3周**：YOLO 目标检测训练与部署\n**第4-5周**：SAM 分割 + 6DoF 位姿估计\n**第6周**：集成到机器人抓取 Pipeline"
}