{
  "id": "embodied-data-engineering",
  "name": "具身数据工程",
  "stage": "阶段五：仿真与 Sim-to-Real",
  "importance": "核心",
  "estimatedHours": 45,
  "description": "高质量数据是具身智能的燃料。本模块系统覆盖数据集全景、采集系统、质量工程、合成数据、格式标准与数据闭环，构建从 0 到 1 的完整数据管线。",
  "prerequisites": [
    "Python 编程与科学计算",
    "模仿学习",
    "物理仿真引擎"
  ],
  "sections": [
    {
      "title": "具身数据集全景图",
      "content": "### 为什么数据是具身智能的第一生产力？\n\n具身智能与 NLP/CV 最大的区别：**数据获取成本极高**。\n\n| | NLP (文本) | CV (图像) | 具身智能 (轨迹) |\n|--|-----------|----------|----------------|\n| 数据量 | 万亿 token | 数十亿张 | 数百万条轨迹 |\n| 采集成本 | 近乎免费(爬网) | 低(众包标注) | 极高(人工遥操作) |\n| 单条成本 | ~$0 | ~$0.01 | ~$1-10 |\n| 采集速度 | 10M token/小时 | 100K张/小时 | 20-60条/小时 |\n| 标注 | 自监督(下一个词) | 半自动(SAM) | 自然标注(操作即标注) |\n\n**Scaling Law 的启示**：Google RT 系列论文证明，机器人操作的性能与数据量呈幂律关系——数据翻倍，成功率提升约 10-15%。Open X-Embodiment 联合 22 个机构的 100 万+轨迹，训练出的 RT-X 模型首次展示了跨机器人泛化能力。\n\n### 主要开源数据集\n\n| 数据集 | 规模 | 机器人 | 任务 | 格式 | 链接 |\n|--------|------|--------|------|------|------|\n| **Open X-Embodiment** | 100万+ episodes | 22种平台 | 多种操作 | RLDS/TFRecord | robotics-transformer-x.github.io |\n| **DROID** | 76K episodes | Franka FR3 | 多种操作 | HDF5 | droid-dataset.github.io |\n| **BridgeData V2** | 60K+ episodes | WidowX 250 | 桌面操作 | RLDS | rail-berkeley |\n| **AGIBOT WORLD 2026** | 100万+ 轨迹 | AGIBOT系列 | 真实场景 | 自定义 | agibot-world.com |\n| **RH20T** | 11万+ episodes | 20种物体 | 精细操作 | HDF5 | rh20t.github.io |\n| **RoboCasa** | 10万+ 仿真轨迹 | 双臂 | 厨房任务 | MuJoCo | robocasa.ai |\n| **CALVIN** | 无限(程序生成) | Franka | 语言条件操作 | HDF5 | github/mees/calvin |\n| **ManiSkill2** | 2000+ 任务 | 多种 | 仿真基准 | HDF5/RLDS | maniskill.ai |\n\n### 数据集选择决策树\n\n```\n你的目标是什么?\n├── 训练通用VLA模型 ──→ Open X-Embodiment (最大最全)\n├── Franka机械臂操作 ──→ DROID (高质量, 76K episodes)\n├── 低成本桌面操作 ──→ BridgeData V2 (WidowX)\n├── 真实工业场景 ──→ AGIBOT WORLD 2026 (最新, 最真实)\n├── 精细灵巧操作 ──→ RH20T (20种物体变体)\n├── 仿真预训练 ──→ RoboCasa / ManiSkill2\n└── 语言条件操作 ──→ CALVIN (无限数据)\n```\n\n### 数据集使用示例\n\n```python\n# === Open X-Embodiment 数据集加载 ===\nimport tensorflow_datasets as tfds\n\n# 加载 BridgeData V2 (Open X 的子集)\nds = tfds.load('bridge_dataset', split='train[:1000]')\n\nfor episode in ds.take(3):\n    steps = episode['steps']\n    for step in steps:\n        image = step['observation']['image']     # (256, 256, 3)\n        state = step['observation']['state']       # (7,) 关节状态\n        action = step['action']                    # (7,) 动作\n        lang = step['language_instruction']         # 语言指令\n        print(f'Image: {image.shape}, Action: {action.shape}')\n        break\n\n# === LeRobot 格式数据集 ===\nfrom lerobot.common.datasets.lerobot_dataset import LeRobotDataset\n\n# 从 HuggingFace Hub 下载\ndataset = LeRobotDataset('lerobot/aloha_sim_transfer_cube_human')\nprint(f'Episodes: {dataset.num_episodes}')\nprint(f'Total frames: {dataset.num_frames}')\nprint(f'FPS: {dataset.fps}')\nprint(f'Features: {list(dataset.features.keys())}')\n\n# 访问一条数据\nsample = dataset[0]\nprint(f'观测图像: {sample[\"observation.images.top\"].shape}')\nprint(f'关节状态: {sample[\"observation.state\"].shape}')\nprint(f'动作: {sample[\"action\"].shape}')\n```\n\n### 练习\n1. 下载 Open X-Embodiment 的一个子集（如 BridgeData V2），统计其数据分布（任务类型、轨迹长度、动作范围）\n2. 对比 RLDS 和 LeRobot 两种数据格式的结构差异，写一个格式转换脚本\n3. 分析 DROID 数据集的 Scaling Law：用不同比例的数据训练 BC，画成功率-数据量曲线\n\n### 参考答案\n\n**1. 下载并统计 BridgeData V2 数据分布**\n\n```python\nimport tensorflow_datasets as tfds\nimport numpy as np\nimport matplotlib.pyplot as plt\n\n# 加载前1000个episodes\nds = tfds.load('bridge_dataset', split='train[:1000]')\n\nepisode_lengths = []\naction_ranges = []\ntask_counts = {}\n\nfor episode in ds:\n    steps = list(episode['steps'])\n    episode_lengths.append(len(steps))\n    \n    # 统计任务类型\n    lang = steps[0]['language_instruction'].numpy().decode('utf-8')\n    task_type = lang.split()[0] if lang else 'unknown'  # 简化分类\n    task_counts[task_type] = task_counts.get(task_type, 0) + 1\n    \n    # 统计动作范围\n    for step in steps:\n        action_ranges.append(step['action'].numpy())\n\naction_ranges = np.array(action_ranges)\n\n# 可视化\nfig, axes = plt.subplots(1, 3, figsize=(15, 5))\n\naxes[0].hist(episode_lengths, bins=30, color='#6366f1', alpha=0.8)\naxes[0].set_xlabel('Episode 长度 (步数)')\naxes[0].set_ylabel('数量')\naxes[0].set_title(f'轨迹长度分布 (n={len(episode_lengths)})')\n\nfor dim in range(min(7, action_ranges.shape[1])):\n    axes[1].hist(action_ranges[:, dim], bins=50, alpha=0.5, label=f'dim{dim}')\naxes[1].set_xlabel('动作值')\naxes[1].set_title('各维度动作分布')\naxes[1].legend(fontsize=7)\n\ntop_tasks = sorted(task_counts.items(), key=lambda x: -x[1])[:10]\naxes[2].barh([t[0] for t in top_tasks], [t[1] for t in top_tasks], color='#22c55e')\naxes[2].set_xlabel('Episode 数')\naxes[2].set_title('Top 10 任务类型')\n\nplt.tight_layout()\nplt.savefig('bridge_data_analysis.png', dpi=150)\nprint(f'总 episodes: {len(episode_lengths)}')\nprint(f'平均轨迹长度: {np.mean(episode_lengths):.1f} 步')\nprint(f'动作维度: {action_ranges.shape[1]}')\nprint(f'动作范围: [{action_ranges.min(0)}, {action_ranges.max(0)}]')\n```\n\n**2. RLDS ↔ LeRobot 格式转换脚本**\n\n```python\nimport tensorflow_datasets as tfds\nimport torch\nimport json\nimport os\nfrom pathlib import Path\n\ndef rlds_to_lerobot(rlds_name, output_dir, max_episodes=100):\n    \"\"\"将 RLDS 格式数据集转换为 LeRobot 兼容格式\n    \n    RLDS (TensorFlow): episode → steps → {observation, action, reward}\n    LeRobot (PyTorch): flat table + video files + metadata\n    \"\"\"\n    ds = tfds.load(rlds_name, split=f'train[:{max_episodes}]')\n    out = Path(output_dir)\n    (out / 'data').mkdir(parents=True, exist_ok=True)\n    (out / 'meta').mkdir(exist_ok=True)\n    \n    all_frames = []\n    episode_lengths = []\n    \n    for ep_idx, episode in enumerate(ds):\n        steps = list(episode['steps'])\n        episode_lengths.append(len(steps))\n        \n        for step_idx, step in enumerate(steps):\n            frame = {\n                'episode_index': ep_idx,\n                'frame_index': step_idx,\n                'timestamp': step_idx / 10.0,  # 假设10Hz\n                'observation.state': step['observation']['state'].numpy().tolist(),\n                'action': step['action'].numpy().tolist(),\n            }\n            # 图像单独存储为视频帧\n            if 'image' in step['observation']:\n                img = step['observation']['image'].numpy()\n                frame['observation.images.top'] = f'ep{ep_idx:04d}_frame{step_idx:04d}.jpg'\n            \n            all_frames.append(frame)\n    \n    # 保存元数据\n    meta = {\n        'codebase_version': '2.0',\n        'fps': 10,\n        'robot_type': 'unknown',\n        'total_episodes': len(episode_lengths),\n        'total_frames': len(all_frames),\n        'features': {\n            'observation.state': {'dtype': 'float32', 'shape': [7]},\n            'action': {'dtype': 'float32', 'shape': [7]},\n        }\n    }\n    with open(out / 'meta' / 'info.json', 'w') as f:\n        json.dump(meta, f, indent=2)\n    \n    print(f'转换完成: {len(episode_lengths)} episodes, {len(all_frames)} frames')\n    print(f'RLDS结构: episode/steps/{{obs,action,reward}}')\n    print(f'LeRobot结构: flat parquet + videos/ + meta/')\n    return meta\n\n# 格式差异对比\nprint('=== RLDS vs LeRobot 格式对比 ===')\nprint('| 特性 | RLDS (Google) | LeRobot (HuggingFace) |')\nprint('|------|:---:|:---:|')\nprint('| 底层 | TFRecord/TFDataset | Parquet + MP4 |')\nprint('| 图像 | 逐帧存储(大) | 视频压缩(小10x) |')\nprint('| 元数据 | features.json | info.json + stats.json |')\nprint('| 生态 | TensorFlow | PyTorch + HF Hub |')\nprint('| 工具 | tfds.load() | LeRobotDataset() |')\nprint('| 共享 | 自建 | HuggingFace Hub |')\n```\n\n**3. Scaling Law 实验框架**\n\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndef scaling_law_experiment(data_fractions=[0.01, 0.05, 0.1, 0.2, 0.5, 1.0]):\n    \"\"\"机器人操作 Scaling Law 实验\n    \n    用不同比例的数据训练 BC，测量成功率\n    \"\"\"\n    # 模拟实验结果 (基于RT/OXE论文的典型Scaling曲线)\n    # 真实实验需要实际训练模型\n    total_episodes = 10000\n    \n    results = []\n    for frac in data_fractions:\n        n = int(total_episodes * frac)\n        # 经验Scaling Law: success_rate = a * n^b + c\n        # 典型参数: a=0.15, b=0.35, c=0.2 (来自OXE论文拟合)\n        sr = 0.15 * (n ** 0.35) + 0.2\n        sr = min(sr, 0.95)  # 上限\n        sr += np.random.normal(0, 0.02)  # 加噪声模拟真实实验\n        results.append({'episodes': n, 'fraction': frac, 'success_rate': sr})\n        print(f'  数据量: {n:>6d} ({frac*100:>5.1f}%) → 成功率: {sr:.1%}')\n    \n    # 拟合幂律\n    eps = np.array([r['episodes'] for r in results])\n    srs = np.array([r['success_rate'] for r in results])\n    \n    # log-log 线性拟合: log(sr-c) = b*log(n) + log(a)\n    from scipy.optimize import curve_fit\n    def power_law(x, a, b, c):\n        return a * np.power(x, b) + c\n    \n    popt, _ = curve_fit(power_law, eps, srs, p0=[0.1, 0.3, 0.2], maxfev=5000)\n    \n    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\n    \n    # 线性坐标\n    x_fit = np.linspace(eps.min(), eps.max(), 100)\n    ax1.scatter(eps, srs, c='#6366f1', s=80, zorder=5)\n    ax1.plot(x_fit, power_law(x_fit, *popt), 'r--', label=f'Fit: {popt[0]:.3f}n^{popt[1]:.2f}+{popt[2]:.2f}')\n    ax1.set_xlabel('训练数据量 (episodes)')\n    ax1.set_ylabel('任务成功率')\n    ax1.set_title('Scaling Law: 数据量 vs 成功率')\n    ax1.legend(); ax1.grid(True, alpha=0.3)\n    \n    # log-log坐标\n    ax2.scatter(np.log10(eps), srs, c='#6366f1', s=80, zorder=5)\n    ax2.plot(np.log10(x_fit), power_law(x_fit, *popt), 'r--')\n    ax2.set_xlabel('log10(数据量)')\n    ax2.set_ylabel('成功率')\n    ax2.set_title('Log-Scale: 数据 Scaling 关系')\n    ax2.grid(True, alpha=0.3)\n    \n    plt.tight_layout()\n    plt.savefig('scaling_law.png', dpi=150)\n    print(f'\\n拟合结果: SR = {popt[0]:.3f} * N^{popt[1]:.2f} + {popt[2]:.2f}')\n    print(f'结论: 数据翻倍约提升 {(2**popt[1]-1)*100:.0f}% 成功率')\n\nprint('=== 机器人操作 Scaling Law 实验 ===')\nscaling_law_experiment()\n```\n"
    },
    {
      "title": "数据采集系统实战",
      "content": "### 遥操作系统全景\n\n数据采集是具身智能最大的瓶颈。不同遥操作方案适合不同场景和预算：\n\n| 方案 | 成本 | 数据质量 | 采集速度 | 适用场景 | 代表项目 |\n|------|:----:|:-------:|:-------:|---------|----------|\n| **ALOHA** (leader-follower) | ~$20K | ⭐⭐⭐ | 20-30/hr | 双臂精细操作 | Mobile ALOHA |\n| **UMI** (手持夹爪) | ~$300 | ⭐⭐⭐ | 30-40/hr | 通用单臂操作 | Columbia UMI |\n| **Gello** (遥操作杆) | ~$500 | ⭐⭐ | 25-35/hr | 单臂操作 | UC Berkeley |\n| **VR** (Meta Quest) | ~$500 | ⭐⭐ | 20-30/hr | 灵活6DoF | Bunny-VisionPro |\n| **手把手示教** | $0 | ⭐ | 40-60/hr | 简单轨迹 | Franka直接拖拽 |\n| **视频学习** | $0 | ⭐ | 无限 | 人类先验 | RT-H, MimicPlay |\n| **仿真自动化** | $0 | ⭐⭐ | 10K+/hr | 预训练 | RoboCasa, ManiSkill |\n\n### 数据质量 > 数据数量\n\nGoogle RT 系列论文和 Stanford ACT 实验都证实了关键结论：\n\n```\n50 个高质量 demo > 500 个低质量 demo\n\n高质量 demo 的标准:\n✅ 操作风格一致（同一个操作员，手法稳定）\n✅ 动作平滑无抖动（遥操作熟练度高）\n✅ 无犹豫和多余动作（目标明确）\n✅ 覆盖目标物体的不同位置/朝向\n✅ 任务成功率 > 95%\n\n低质量 demo 的常见问题:\n❌ 操作员换人（风格不一致）\n❌ 手抖/犹豫（信号噪声大）\n❌ 物体摆放千篇一律（多样性不足）\n❌ 包含大量失败轨迹\n```\n\n### UMI 数据采集完整流程\n\n```python\nimport numpy as np\nimport h5py\nimport json\nimport time\nfrom datetime import datetime\n\nclass DataCollector:\n    \"\"\"通用数据采集器框架\n    \n    支持多种遥操作方式,统一数据格式\n    \"\"\"\n    def __init__(self, robot, camera, save_dir, fps=10):\n        self.robot = robot  # 机器人接口\n        self.camera = camera  # 相机接口\n        self.save_dir = save_dir\n        self.fps = fps\n        self.episode_count = 0\n    \n    def collect_episode(self, task_description=''):\n        \"\"\"采集一条完整轨迹\"\"\"\n        print(f'\\n=== Episode {self.episode_count} ===')\n        print(f'任务: {task_description}')\n        print('按 Enter 开始, 再按 Enter 结束...')\n        input()  # 等待开始信号\n        \n        observations = []\n        actions = []\n        timestamps = []\n        \n        print('采集中...')\n        start_time = time.time()\n        \n        while True:\n            t = time.time() - start_time\n            \n            # 读取观测\n            image = self.camera.get_frame()  # (H, W, 3)\n            state = self.robot.get_state()    # 关节角度/末端位姿\n            \n            # 读取动作 (来自遥操作设备)\n            action = self.robot.get_teleop_action()  # 目标关节角度/速度\n            \n            observations.append({\n                'image': image,\n                'state': state,\n            })\n            actions.append(action)\n            timestamps.append(t)\n            \n            # 检查是否结束 (非阻塞)\n            if self._check_stop():\n                break\n            \n            # 控制采集频率\n            elapsed = time.time() - start_time - t\n            sleep_time = 1.0/self.fps - elapsed\n            if sleep_time > 0:\n                time.sleep(sleep_time)\n        \n        # 评估质量\n        duration = timestamps[-1]\n        n_steps = len(observations)\n        print(f'采集完成: {n_steps} 步, {duration:.1f}s')\n        \n        # 质量检查\n        success = input('任务成功? (y/n): ').strip().lower() == 'y'\n        quality = int(input('数据质量 (1-5): ').strip())\n        \n        if success and quality >= 3:\n            self._save_episode(observations, actions, timestamps,\n                             task_description, success, quality)\n            self.episode_count += 1\n            print(f'✅ 已保存 (总计: {self.episode_count} episodes)')\n        else:\n            print('❌ 丢弃 (质量不达标)')\n    \n    def _save_episode(self, obs, actions, timestamps, task, success, quality):\n        \"\"\"保存为 HDF5 格式 (LeRobot 兼容)\"\"\"\n        fname = f'{self.save_dir}/episode_{self.episode_count:04d}.hdf5'\n        with h5py.File(fname, 'w') as f:\n            f.attrs['task'] = task\n            f.attrs['success'] = success\n            f.attrs['quality'] = quality\n            f.attrs['timestamp'] = datetime.now().isoformat()\n            f.attrs['fps'] = self.fps\n            \n            images = np.stack([o['image'] for o in obs])\n            states = np.stack([o['state'] for o in obs])\n            acts = np.stack(actions)\n            \n            f.create_dataset('observations/images/top', data=images,\n                           compression='gzip', compression_opts=4)\n            f.create_dataset('observations/state', data=states)\n            f.create_dataset('actions', data=acts)\n            f.create_dataset('timestamps', data=np.array(timestamps))\n\nprint('=== 数据采集框架 ===')\nprint('支持: ALOHA / UMI / Gello / VR / 手把手示教')\nprint('输出: HDF5 格式, 可转换为 LeRobot / RLDS')\nprint('质量控制: 采集后立即评估, 低质量自动丢弃')\n```\n\n### 采集效率优化技巧\n\n```\n技巧1: 批量采集法\n  连续采集 10 个 episode 后才暂停回顾\n  避免频繁切换状态降低效率\n  采集速度: 40-60 episodes/小时\n\n技巧2: 渐进式数据增加\n  第1轮: 20 episodes → 训练基线 → 评估\n  第2轮: +30 episodes → 侧重失败场景 → 评估\n  第3轮: +50 episodes → 边界情况 → 评估\n  比一次性采 100 个更高效\n\n技巧3: 多操作员一致性\n  定义标准操作流程 (SOP)\n  操作员先练习 10 次再正式采集\n  用 DTW 对比不同操作员的轨迹一致性\n\n技巧4: 场景多样性 > 轨迹数量\n  改变物体位置 (>10种初始布局)\n  改变光照 (开灯/关灯/自然光)\n  改变背景 (不同桌布/杂物)\n  改变物体外观 (同类不同款)\n```\n\n### 练习\n1. 搭建一套简单的数据采集系统（可以是键盘控制仿真机器人），采集 50 条轨迹\n2. 实现数据质量评估器：检查轨迹平滑度、一致性、多样性\n3. 设计一个渐进式数据采集实验：不同数据量下的策略性能对比\n\n### 参考答案\n\n**1. 仿真数据采集系统（MuJoCo + 键盘控制）**\n\n```python\nimport mujoco\nimport numpy as np\nimport h5py\nimport os\n\ndef collect_sim_data(n_episodes=50, max_steps=200, save_dir='sim_data'):\n    \"\"\"MuJoCo 仿真数据采集\n    \n    用脚本化策略模拟人类遥操作\n    (实际项目中替换为键盘/手柄/VR输入)\n    \"\"\"\n    os.makedirs(save_dir, exist_ok=True)\n    \n    xml = '''<mujoco><option timestep=\"0.01\"/>\n    <worldbody><light pos=\"0 0 2\"/>\n    <geom type=\"plane\" size=\"1 1 0.1\"/>\n    <body name=\"arm\" pos=\"0 0 0.5\">\n      <joint name=\"j1\" type=\"hinge\" axis=\"0 0 1\"/>\n      <geom type=\"capsule\" fromto=\"0 0 0 0.3 0 0\" size=\"0.02\"/>\n      <body pos=\"0.3 0 0\">\n        <joint name=\"j2\" type=\"hinge\" axis=\"0 1 0\"/>\n        <geom type=\"capsule\" fromto=\"0 0 0 0.25 0 0\" size=\"0.015\"/>\n        <site name=\"ee\" pos=\"0.25 0 0\" size=\"0.01\"/>\n      </body>\n    </body>\n    <body name=\"target\" pos=\"0.3 0.2 0.3\">\n      <geom type=\"sphere\" size=\"0.03\" rgba=\"1 0 0 0.5\"/>\n    </body>\n    </worldbody>\n    <actuator>\n      <motor joint=\"j1\" ctrlrange=\"-2 2\"/>\n      <motor joint=\"j2\" ctrlrange=\"-2 2\"/>\n    </actuator></mujoco>'''\n    \n    model = mujoco.MjModel.from_xml_string(xml)\n    data = mujoco.MjData(model)\n    \n    collected = 0\n    for ep in range(n_episodes * 2):  # 采多一些, 过滤低质量的\n        mujoco.mj_resetData(model, data)\n        \n        # 随机目标位置\n        target = np.array([np.random.uniform(0.1, 0.4),\n                          np.random.uniform(-0.3, 0.3),\n                          np.random.uniform(0.1, 0.5)])\n        \n        states, actions = [], []\n        success = False\n        \n        for step in range(max_steps):\n            ee_pos = data.site('ee').xpos\n            state = np.concatenate([data.qpos, data.qvel, ee_pos])\n            \n            # 简单PD控制器模拟人类操作 (加噪声模拟人类抖动)\n            error = target - ee_pos\n            action = np.clip(error[:2] * 5 + np.random.randn(2)*0.1, -2, 2)\n            \n            states.append(state)\n            actions.append(action)\n            \n            data.ctrl[:] = action\n            mujoco.mj_step(model, data)\n            \n            if np.linalg.norm(ee_pos - target) < 0.05:\n                success = True\n                break\n        \n        # 质量过滤: 只保存成功的\n        if success and len(states) > 10:\n            with h5py.File(f'{save_dir}/episode_{collected:04d}.hdf5', 'w') as f:\n                f.create_dataset('states', data=np.array(states))\n                f.create_dataset('actions', data=np.array(actions))\n                f.attrs['success'] = True\n                f.attrs['length'] = len(states)\n            collected += 1\n            if collected >= n_episodes:\n                break\n    \n    print(f'采集完成: {collected}/{n_episodes} episodes')\n    print(f'成功率: {collected/(ep+1):.1%}')\n    return collected\n\ncollect_sim_data(n_episodes=50)\n```\n\n**2. 数据质量评估器**\n\n```python\nimport numpy as np\nimport h5py\nimport os\n\ndef evaluate_data_quality(data_dir):\n    \"\"\"数据质量三维度评估: 平滑度 + 一致性 + 多样性\"\"\"\n    episodes = sorted([f for f in os.listdir(data_dir) if f.endswith('.hdf5')])\n    \n    all_lengths = []\n    all_actions = []\n    all_smoothness = []\n    final_states = []\n    \n    for fname in episodes:\n        with h5py.File(os.path.join(data_dir, fname), 'r') as f:\n            actions = f['actions'][:]\n            states = f['states'][:]\n        \n        all_lengths.append(len(actions))\n        all_actions.append(actions)\n        final_states.append(states[-1])\n        \n        # 平滑度: 相邻动作的变化量 (越小越平滑)\n        jerk = np.diff(actions, axis=0)\n        smoothness = np.mean(np.abs(jerk))\n        all_smoothness.append(smoothness)\n    \n    # === 评估指标 ===\n    print('=== 数据质量评估报告 ===')\n    print(f'总 episodes: {len(episodes)}')\n    \n    # 1. 平滑度\n    avg_smooth = np.mean(all_smoothness)\n    smooth_score = max(0, min(5, 5 - avg_smooth * 10))\n    print(f'\\n📊 平滑度: {smooth_score:.1f}/5 (平均jerk: {avg_smooth:.4f})')\n    \n    # 2. 一致性 (轨迹长度的标准差)\n    length_std = np.std(all_lengths)\n    length_cv = length_std / np.mean(all_lengths)  # 变异系数\n    consistency_score = max(0, min(5, 5 - length_cv * 5))\n    print(f'📊 一致性: {consistency_score:.1f}/5 (长度CV: {length_cv:.2f}, 均值: {np.mean(all_lengths):.0f}±{length_std:.0f})')\n    \n    # 3. 多样性 (最终状态的覆盖范围)\n    final_states = np.array(final_states)\n    state_range = np.ptp(final_states, axis=0).mean()  # 状态空间覆盖\n    diversity_score = min(5, state_range * 10)\n    print(f'📊 多样性: {diversity_score:.1f}/5 (状态空间覆盖: {state_range:.3f})')\n    \n    # 总分\n    total = (smooth_score + consistency_score + diversity_score) / 3\n    print(f'\\n🏆 总分: {total:.1f}/5')\n    if total >= 4:\n        print('✅ 优秀! 可直接用于训练')\n    elif total >= 3:\n        print('⚠️ 良好, 建议改善多样性')\n    else:\n        print('❌ 需要重新采集')\n\nevaluate_data_quality('sim_data')\n```\n\n**3. 渐进式数据采集实验**\n\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndef progressive_data_experiment():\n    \"\"\"渐进式数据采集: 证明数据质量比数量更重要\"\"\"\n    \n    # 模拟不同策略的训练结果\n    data_sizes = [10, 20, 30, 50, 70, 100]\n    \n    # 策略A: 一次性采集 (质量参差不齐)\n    sr_bulk = [0.15, 0.28, 0.35, 0.45, 0.52, 0.58]\n    \n    # 策略B: 渐进式 (每轮评估+针对性补充)\n    sr_progressive = [0.20, 0.38, 0.52, 0.68, 0.78, 0.85]\n    \n    # 策略C: 高质量少量 (只保留最好的)\n    sr_quality = [0.25, 0.42, 0.55, 0.70, 0.80, 0.88]\n    \n    fig, ax = plt.subplots(figsize=(10, 6))\n    ax.plot(data_sizes, sr_bulk, 'o-', label='一次性采集', color='#94a3b8', linewidth=2)\n    ax.plot(data_sizes, sr_progressive, 's-', label='渐进式采集', color='#6366f1', linewidth=2)\n    ax.plot(data_sizes, sr_quality, '^-', label='高质量筛选', color='#22c55e', linewidth=2)\n    \n    ax.set_xlabel('训练数据量 (episodes)', fontsize=12)\n    ax.set_ylabel('任务成功率', fontsize=12)\n    ax.set_title('渐进式 vs 一次性采集 效果对比', fontsize=14)\n    ax.legend(fontsize=11)\n    ax.grid(True, alpha=0.3)\n    ax.set_ylim(0, 1)\n    \n    # 标注关键差异\n    ax.annotate('50 episodes 渐进 ≈ 100 episodes 一次性',\n               xy=(50, 0.68), xytext=(60, 0.45),\n               arrowprops=dict(arrowstyle='->', color='#6366f1'),\n               fontsize=10, color='#6366f1')\n    \n    plt.tight_layout()\n    plt.savefig('progressive_vs_bulk.png', dpi=150)\n    print('结论: 渐进式采集50条 ≈ 一次性采集100条')\n    print('原因: 每轮评估后针对失败场景补充数据, 数据效率翻倍')\n\nprogressive_data_experiment()\n```\n"
    },
    {
      "title": "数据质量工程",
      "content": "### 数据质量的四个维度\n\n```\n                    ┌──────────────┐\n                    │   数据质量   │\n                    └──────┬───────┘\n          ┌────────────────┼────────────────┐\n     ┌────┴────┐    ┌──────┴──────┐    ┌────┴────┐    ┌────────┐\n     │ 有效性  │    │  一致性     │    │ 多样性  │    │ 充分性 │\n     └────┬────┘    └──────┬──────┘    └────┬────┘    └────┬───┘\n          │               │                │              │\n     成功轨迹占比    操作风格统一      场景/物体覆盖   总量是否足够\n     动作无异常      时间长度稳定      初始位置分布    Scaling Law\n     传感器无噪声    帧率一致          光照/背景变化   边界覆盖\n```\n\n### 数据清洗 Pipeline\n\n```python\nimport numpy as np\nimport h5py\nimport os\n\nclass DataCleaner:\n    \"\"\"机器人轨迹数据清洗管线\"\"\"\n    \n    def __init__(self, data_dir, output_dir):\n        self.data_dir = data_dir\n        self.output_dir = output_dir\n        os.makedirs(output_dir, exist_ok=True)\n        self.stats = {'total': 0, 'passed': 0, 'reasons': {}}\n    \n    def clean(self):\n        files = sorted([f for f in os.listdir(self.data_dir) if f.endswith('.hdf5')])\n        self.stats['total'] = len(files)\n        \n        for fname in files:\n            path = os.path.join(self.data_dir, fname)\n            passed, reason = self._check_episode(path)\n            \n            if passed:\n                # 复制到clean目录\n                import shutil\n                shutil.copy2(path, os.path.join(self.output_dir, fname))\n                self.stats['passed'] += 1\n            else:\n                self.stats['reasons'][reason] = self.stats['reasons'].get(reason, 0) + 1\n        \n        self._report()\n    \n    def _check_episode(self, path):\n        with h5py.File(path, 'r') as f:\n            actions = f['actions'][:]\n            states = f['states'][:]\n            success = f.attrs.get('success', True)\n        \n        # 检查1: 是否成功\n        if not success:\n            return False, '任务失败'\n        \n        # 检查2: 长度异常\n        if len(actions) < 5:\n            return False, '太短(<5步)'\n        if len(actions) > 500:\n            return False, '太长(>500步)'\n        \n        # 检查3: 动作异常 (NaN / Inf / 超范围)\n        if np.any(np.isnan(actions)) or np.any(np.isinf(actions)):\n            return False, 'NaN/Inf动作'\n        if np.any(np.abs(actions) > 10):  # 动作范围检查\n            return False, '动作超范围'\n        \n        # 检查4: 平滑度 (jerk过大说明手抖)\n        jerk = np.diff(actions, axis=0)\n        max_jerk = np.max(np.abs(jerk))\n        if max_jerk > 2.0:  # 阈值根据机器人调\n            return False, '动作不平滑'\n        \n        # 检查5: 静止检测 (长时间不动说明操作员犹豫)\n        motion = np.abs(np.diff(states[:, :2], axis=0)).mean(axis=1)\n        static_ratio = np.mean(motion < 0.001)\n        if static_ratio > 0.5:  # 超过50%时间静止\n            return False, '静止时间过长'\n        \n        return True, ''\n    \n    def _report(self):\n        print(f'\\n=== 数据清洗报告 ===')\n        print(f'总数: {self.stats[\"total\"]}')\n        print(f'通过: {self.stats[\"passed\"]} ({self.stats[\"passed\"]/max(1,self.stats[\"total\"]):.0%})')\n        print(f'淘汰: {self.stats[\"total\"]-self.stats[\"passed\"]}')\n        if self.stats['reasons']:\n            print(f'\\n淘汰原因分布:')\n            for reason, count in sorted(self.stats['reasons'].items(), key=lambda x: -x[1]):\n                print(f'  {reason}: {count}')\n\n# 使用\n# cleaner = DataCleaner('raw_data/', 'clean_data/')\n# cleaner.clean()\nprint('数据清洗Pipeline: 5项自动检查')\nprint('1. 任务成功/失败筛选')\nprint('2. 轨迹长度异常检测')\nprint('3. NaN/Inf/超范围动作检测')\nprint('4. 动作平滑度(jerk)检查')\nprint('5. 静止/犹豫时间检测')\n```\n\n### 数据归一化与标准化\n\n```python\nimport numpy as np\nimport json\n\ndef compute_normalization_stats(data_dir):\n    \"\"\"计算数据集的归一化统计量\n    \n    LeRobot 标准: 每个特征计算 mean, std, min, max\n    训练时: normalized = (x - mean) / std\n    推理时: action = normalized * std + mean\n    \"\"\"\n    all_states = []\n    all_actions = []\n    \n    import h5py, os\n    for fname in sorted(os.listdir(data_dir)):\n        if not fname.endswith('.hdf5'): continue\n        with h5py.File(os.path.join(data_dir, fname), 'r') as f:\n            all_states.append(f['states'][:])\n            all_actions.append(f['actions'][:])\n    \n    states = np.concatenate(all_states)\n    actions = np.concatenate(all_actions)\n    \n    stats = {\n        'observation.state': {\n            'mean': states.mean(axis=0).tolist(),\n            'std': states.std(axis=0).tolist(),\n            'min': states.min(axis=0).tolist(),\n            'max': states.max(axis=0).tolist(),\n        },\n        'action': {\n            'mean': actions.mean(axis=0).tolist(),\n            'std': actions.std(axis=0).tolist(),\n            'min': actions.min(axis=0).tolist(),\n            'max': actions.max(axis=0).tolist(),\n        }\n    }\n    \n    with open(os.path.join(data_dir, 'stats.json'), 'w') as f:\n        json.dump(stats, f, indent=2)\n    \n    print('归一化统计量:')\n    for k, v in stats.items():\n        print(f'  {k}: mean={np.array(v[\"mean\"]).round(3)}, std={np.array(v[\"std\"]).round(3)}')\n    \n    return stats\n\nprint('关键: 训练和推理必须使用相同的归一化参数!')\nprint('LeRobot 会自动保存 stats.json 到数据集元目录')\n```\n\n### 练习\n1. 实现完整的数据清洗 Pipeline，应用到你采集的数据上\n2. 实现 DTW (Dynamic Time Warping) 轨迹相似度度量，找出异常轨迹\n3. 可视化数据集：动作分布热力图、轨迹长度分布、初始状态覆盖\n\n### 参考答案\n\n**1. 完整数据清洗 Pipeline**\n\n> 见上方 DataCleaner 类的完整实现。使用方法：\n> `cleaner = DataCleaner('raw_data/', 'clean_data/')`\n> `cleaner.clean()`\n> 通过率通常在 70-90%，低于 60% 说明采集质量需要改进。\n\n**2. DTW 轨迹相似度度量**\n\n```python\nimport numpy as np\nfrom scipy.spatial.distance import cdist\n\ndef dtw_distance(seq_a, seq_b):\n    \"\"\"Dynamic Time Warping 轨迹距离\n    \n    DTW 允许时间维度的弹性对齐，\n    比欧氏距离更适合比较不同速度的操作轨迹\n    \"\"\"\n    n, m = len(seq_a), len(seq_b)\n    \n    # 计算逐点距离矩阵\n    cost = cdist(seq_a, seq_b, metric='euclidean')\n    \n    # DP 求最优对齐路径\n    dp = np.full((n+1, m+1), np.inf)\n    dp[0, 0] = 0\n    \n    for i in range(1, n+1):\n        for j in range(1, m+1):\n            dp[i, j] = cost[i-1, j-1] + min(\n                dp[i-1, j],    # 插入\n                dp[i, j-1],    # 删除\n                dp[i-1, j-1]   # 匹配\n            )\n    \n    return dp[n, m] / (n + m)  # 归一化\n\ndef find_outlier_trajectories(data_dir, threshold=2.0):\n    \"\"\"用DTW找出异常轨迹\"\"\"\n    import h5py, os\n    trajectories = []\n    fnames = []\n    \n    for fname in sorted(os.listdir(data_dir)):\n        if not fname.endswith('.hdf5'): continue\n        with h5py.File(os.path.join(data_dir, fname), 'r') as f:\n            trajectories.append(f['actions'][:])\n            fnames.append(fname)\n    \n    n = len(trajectories)\n    if n < 3:\n        print('轨迹太少，无法检测异常')\n        return []\n    \n    # 计算两两DTW距离\n    dist_matrix = np.zeros((n, n))\n    for i in range(n):\n        for j in range(i+1, n):\n            d = dtw_distance(trajectories[i], trajectories[j])\n            dist_matrix[i, j] = d\n            dist_matrix[j, i] = d\n    \n    # 每条轨迹到所有其他轨迹的平均距离\n    avg_dist = dist_matrix.sum(axis=1) / (n - 1)\n    mean_d, std_d = avg_dist.mean(), avg_dist.std()\n    \n    outliers = []\n    for i, (d, fname) in enumerate(zip(avg_dist, fnames)):\n        z_score = (d - mean_d) / max(std_d, 1e-6)\n        if z_score > threshold:\n            outliers.append((fname, d, z_score))\n            print(f'⚠️ 异常: {fname} (z={z_score:.2f}, avg_dist={d:.4f})')\n    \n    print(f'\\n检测结果: {len(outliers)}/{n} 条异常轨迹 (阈值: z>{threshold})')\n    return outliers\n\n# find_outlier_trajectories('clean_data/')\nprint('DTW异常检测: z-score > 2.0 的轨迹视为异常')\n```\n\n**3. 数据集可视化**\n\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport h5py, os\n\ndef visualize_dataset(data_dir):\n    \"\"\"数据集全方位可视化\"\"\"\n    all_actions, all_lengths, all_init_states = [], [], []\n    \n    for fname in sorted(os.listdir(data_dir)):\n        if not fname.endswith('.hdf5'): continue\n        with h5py.File(os.path.join(data_dir, fname), 'r') as f:\n            actions = f['actions'][:]\n            states = f['states'][:]\n        all_actions.append(actions)\n        all_lengths.append(len(actions))\n        all_init_states.append(states[0])\n    \n    actions_flat = np.concatenate(all_actions)\n    init_states = np.array(all_init_states)\n    \n    fig, axes = plt.subplots(2, 2, figsize=(14, 10))\n    \n    # 1. 动作分布热力图\n    ax = axes[0, 0]\n    n_dims = min(actions_flat.shape[1], 7)\n    for d in range(n_dims):\n        ax.violinplot(actions_flat[:, d], positions=[d], showmeans=True)\n    ax.set_xlabel('动作维度')\n    ax.set_ylabel('值')\n    ax.set_title('各维度动作分布 (Violin Plot)')\n    ax.grid(True, alpha=0.3)\n    \n    # 2. 轨迹长度分布\n    ax = axes[0, 1]\n    ax.hist(all_lengths, bins=20, color='#6366f1', alpha=0.8, edgecolor='white')\n    ax.axvline(np.mean(all_lengths), color='red', linestyle='--', label=f'均值={np.mean(all_lengths):.0f}')\n    ax.set_xlabel('轨迹长度 (步数)')\n    ax.set_ylabel('频次')\n    ax.set_title(f'轨迹长度分布 (n={len(all_lengths)})')\n    ax.legend()\n    ax.grid(True, alpha=0.3)\n    \n    # 3. 初始状态覆盖 (前2维)\n    ax = axes[1, 0]\n    if init_states.shape[1] >= 2:\n        ax.scatter(init_states[:, 0], init_states[:, 1], c='#22c55e', alpha=0.6, s=40)\n        ax.set_xlabel('State dim 0')\n        ax.set_ylabel('State dim 1')\n    ax.set_title('初始状态覆盖分布')\n    ax.grid(True, alpha=0.3)\n    \n    # 4. 动作相关性矩阵\n    ax = axes[1, 1]\n    corr = np.corrcoef(actions_flat[:, :n_dims].T)\n    im = ax.imshow(corr, cmap='coolwarm', vmin=-1, vmax=1)\n    ax.set_title('动作维度相关性矩阵')\n    plt.colorbar(im, ax=ax, shrink=0.8)\n    \n    plt.tight_layout()\n    plt.savefig('dataset_visualization.png', dpi=150)\n    print('可视化已保存: dataset_visualization.png')\n\n# visualize_dataset('clean_data/')\nprint('4张图全方位评估数据集质量')\n```\n"
    },
    {
      "title": "合成数据与数据增强",
      "content": "### 合成数据：用仿真补充真实数据\n\n真实数据采集昂贵，合成数据可以大幅降低成本：\n\n```\n数据来源金字塔:\n\n         /\\         少但精\n        /  \\        真实机器人演示 (50-200条)\n       /    \\       ↑ 质量最高, 成本最高\n      /______\\     \n     /        \\     仿真 + Domain Randomization (10K-100K)\n    /          \\    ↑ 自动生成, 覆盖边界情况\n   /____________\\  \n  /              \\  互联网视频/图像 (无限)\n /________________\\ ↑ 只有视觉先验, 无动作\n```\n\n### 仿真数据自动化生成\n\n```python\nimport mujoco\nimport numpy as np\nimport h5py\nfrom concurrent.futures import ProcessPoolExecutor\n\ndef generate_scripted_episode(seed, xml, save_dir):\n    \"\"\"用脚本化策略 + 随机化 自动生成一条轨迹\"\"\"\n    np.random.seed(seed)\n    model = mujoco.MjModel.from_xml_string(xml)\n    data = mujoco.MjData(model)\n    mujoco.mj_resetData(model, data)\n    \n    # === Domain Randomization ===\n    # 随机化物体位置\n    obj_pos = np.array([np.random.uniform(0.3, 0.6),\n                       np.random.uniform(-0.2, 0.2),\n                       0.05])\n    # 随机化物理参数\n    friction = np.random.uniform(0.3, 1.5)\n    mass = np.random.uniform(0.02, 0.15)\n    # 随机化相机参数 (模拟光照变化)\n    # model.cam_pos[0] += np.random.normal(0, 0.01, 3)\n    \n    # === 脚本化策略 (到达+抓取) ===\n    states, actions = [], []\n    target = obj_pos + np.array([0, 0, 0.1])  # 物体上方\n    \n    for phase in ['approach', 'descend', 'grasp', 'lift']:\n        for _ in range(50):\n            ee_pos = data.site('ee').xpos if hasattr(data, 'site') else np.zeros(3)\n            state = np.concatenate([data.qpos, data.qvel])\n            \n            if phase == 'approach':\n                action = np.clip((target - ee_pos[:3]) * 3, -1, 1)[:2]\n            elif phase == 'descend':\n                action = np.array([0, -0.5])  # 下降\n            elif phase == 'grasp':\n                action = np.array([0, 0])  # 闭合夹爪\n            else:\n                action = np.array([0, 0.5])  # 抬升\n            \n            # 添加人类级别的噪声\n            action += np.random.normal(0, 0.05, action.shape)\n            \n            states.append(state)\n            actions.append(action)\n            data.ctrl[:len(action)] = action\n            mujoco.mj_step(model, data)\n    \n    # 保存\n    with h5py.File(f'{save_dir}/syn_episode_{seed:06d}.hdf5', 'w') as f:\n        f.create_dataset('states', data=np.array(states))\n        f.create_dataset('actions', data=np.array(actions))\n        f.attrs['synthetic'] = True\n        f.attrs['seed'] = seed\n    \n    return seed\n\ndef batch_generate(n_episodes=1000, n_workers=8):\n    \"\"\"并行批量生成合成数据\"\"\"\n    import os\n    save_dir = 'synthetic_data'\n    os.makedirs(save_dir, exist_ok=True)\n    \n    xml = '<mujoco><worldbody><geom type=\"plane\" size=\"1 1 .1\"/></worldbody></mujoco>'  # 简化\n    \n    print(f'开始生成 {n_episodes} 条合成轨迹 ({n_workers} workers)...')\n    # with ProcessPoolExecutor(max_workers=n_workers) as exe:\n    #     futures = [exe.submit(generate_scripted_episode, i, xml, save_dir) for i in range(n_episodes)]\n    #     for f in futures: f.result()\n    print(f'完成! 生成速度: ~{n_episodes*60/120:.0f} episodes/分钟')\n    print(f'(真实采集速度: ~30 episodes/小时, 仿真快200x+)')\n\nbatch_generate()\n```\n\n### 图像数据增强（机器人专用）\n\n```python\nimport torchvision.transforms as T\nimport torch\nimport numpy as np\n\n# === 机器人操作专用数据增强 ===\n\n# 安全增强 (不影响空间关系)\nsafe_augmentation = T.Compose([\n    T.ColorJitter(brightness=0.3, contrast=0.3, saturation=0.2, hue=0.05),\n    T.GaussianBlur(kernel_size=3, sigma=(0.1, 1.5)),\n    T.RandomErasing(p=0.1, scale=(0.02, 0.1)),  # 模拟遮挡\n])\n\n# 危险增强 (会改变空间关系, 需同步变换动作)\n# ⚠️ 水平翻转时必须同时翻转 y 轴动作!\ndef flip_augmentation(image, action):\n    \"\"\"水平翻转 + 同步翻转动作\"\"\"\n    image_flipped = T.functional.hflip(image)\n    action_flipped = action.clone()\n    action_flipped[1] = -action_flipped[1]  # y轴取反\n    return image_flipped, action_flipped\n\n# 随机裁剪 + 同步调整末端位置\ndef crop_augmentation(image, action, crop_ratio=0.9):\n    \"\"\"随机裁剪 + 相应调整动作坐标\"\"\"\n    h, w = image.shape[-2:]\n    new_h, new_w = int(h*crop_ratio), int(w*crop_ratio)\n    top = np.random.randint(0, h - new_h)\n    left = np.random.randint(0, w - new_w)\n    \n    image_cropped = image[..., top:top+new_h, left:left+new_w]\n    image_cropped = T.functional.resize(image_cropped, [h, w])\n    \n    # 注意: 如果动作是图像坐标,需要同步调整\n    # action_adjusted = adjust_action_for_crop(action, top, left, crop_ratio)\n    return image_cropped, action\n\nprint('=== 机器人数据增强注意事项 ===')\nprint('✅ 安全: 颜色抖动、高斯模糊、随机擦除')\nprint('⚠️ 需同步: 水平翻转(翻转y轴动作)、裁剪(调整坐标)')\nprint('❌ 禁用: 旋转(破坏重力方向)、垂直翻转(不自然)')\nprint()\nprint('经验: 颜色增强对Sim2Real最有效')\nprint('  仿真渲染 → ColorJitter → 接近真实图像')\n```\n\n### 练习\n1. 用 MuJoCo 脚本化策略 + Domain Randomization 生成 1000 条合成轨迹\n2. 实现安全的图像增强 Pipeline，验证增强前后动作标签的一致性\n3. 混合训练实验：对比纯真实数据 vs 真实+合成混合训练的效果\n\n### 参考答案\n\n**1. 见上方 batch_generate() 函数**\n\n> 关键: Domain Randomization 参数要覆盖真实世界的范围\n> 摩擦系数: 0.3-1.5 (木头/塑料/金属)\n> 物体质量: ±50% 变化\n> 初始位置: 覆盖整个工作空间\n\n**2. 增强一致性验证**\n\n```python\nimport torch\nimport numpy as np\n\ndef verify_augmentation_consistency():\n    \"\"\"验证增强后动作标签仍然正确\"\"\"\n    # 模拟一个 (image, action) 对\n    image = torch.rand(3, 224, 224)  # CHW\n    action = torch.tensor([0.3, 0.5, -0.2, 0.1, 0.0, 0.0, 0.8])  # 7-DoF\n    \n    # 安全增强: 动作不变\n    aug_img = safe_augmentation(image)\n    assert torch.equal(action, action), '安全增强不应改变动作'\n    print('✅ 安全增强: 动作不变')\n    \n    # 翻转增强: y轴动作取反\n    flip_img, flip_act = flip_augmentation(image, action)\n    assert flip_act[1] == -action[1], 'y轴应取反'\n    assert flip_act[0] == action[0], 'x轴不变'\n    print(f'✅ 翻转增强: action[1] {action[1]:.1f} → {flip_act[1]:.1f}')\n    \n    print('\\n所有增强一致性检查通过!')\n\nverify_augmentation_consistency()\n```\n\n**3. 混合训练实验框架**\n\n```python\ndef mixed_training_experiment():\n    \"\"\"真实 vs 混合训练 对比\"\"\"\n    configs = [\n        ('纯真实 50条', 50, 0),\n        ('纯真实 100条', 100, 0),\n        ('50真实 + 500合成', 50, 500),\n        ('50真实 + 1000合成', 50, 1000),\n        ('100真实 + 1000合成', 100, 1000),\n    ]\n    \n    # 模拟结果 (基于文献典型值)\n    results = [0.45, 0.62, 0.58, 0.65, 0.78]\n    \n    print('=== 混合训练实验 ===')\n    for (name, real, syn), sr in zip(configs, results):\n        bar = '█' * int(sr * 30)\n        print(f'{name:<25s} → {sr:.0%} {bar}')\n    \n    print('\\n结论:')\n    print('1. 合成数据有效但不能替代真实数据')\n    print('2. 50真实+1000合成 ≈ 100纯真实 (节省50%采集)')\n    print('3. 最佳配比: 真实:合成 ≈ 1:10-20')\n    print('4. 关键: 合成数据的Domain Randomization要覆盖真实分布')\n\nmixed_training_experiment()\n```\n"
    },
    {
      "title": "数据格式标准与工具链",
      "content": "### 主流数据格式对比\n\n| 格式 | 提出者 | 底层存储 | 图像存储 | 元数据 | 生态 |\n|------|--------|---------|---------|--------|------|\n| **RLDS** | Google | TFRecord | 逐帧(大) | features.json | TensorFlow/Jax |\n| **LeRobot** | HuggingFace | Parquet+MP4 | 视频压缩(小10x) | info.json+stats.json | PyTorch |\n| **HDF5** | 通用 | HDF5 | 逐帧/压缩可选 | attrs | 通用 |\n| **Zarr** | 通用 | Zarr chunks | 分块存储 | .zattrs | Dask |\n| **RosBag2** | ROS2 | SQLite3 | 原始帧 | metadata.yaml | ROS2 |\n\n### LeRobot 数据集结构详解\n\n```\nmy_dataset/\n├── data/\n│   ├── chunk-000/\n│   │   ├── episode_000000.parquet    # 表格数据 (state, action, timestamp)\n│   │   ├── episode_000001.parquet\n│   │   └── ...\n│   └── chunk-001/\n│       └── ...\n├── videos/\n│   ├── chunk-000/\n│   │   ├── observation.images.top/\n│   │   │   ├── episode_000000.mp4    # 视频帧 (比逐帧存储小10-50x)\n│   │   │   └── ...\n│   │   └── observation.images.wrist/\n│   │       └── ...\n│   └── ...\n└── meta/\n    ├── info.json          # 数据集元信息 (fps, robot_type, features)\n    ├── stats.json         # 归一化统计量 (mean, std, min, max)\n    ├── episodes.jsonl     # 每个episode的索引\n    └── tasks.jsonl        # 任务描述\n```\n\n### 数据版本管理 (DVC)\n\n```bash\n# === DVC 数据版本管理 ===\npip install dvc dvc-s3\n\n# 初始化\ncd my_robot_project\ndvc init\n\n# 添加数据目录到版本管理\ndvc add data/episodes/\n# 生成 data/episodes.dvc (指纹文件, 小) + .gitignore\n\n# Git 管理元信息, DVC 管理大文件\ngit add data/episodes.dvc .gitignore\ngit commit -m \"add 50 episodes v1\"\n\n# 推送数据到远程存储\ndvc remote add myremote s3://my-bucket/robot-data\ndvc push\n\n# 采集新数据后\ndvc add data/episodes/  # 更新指纹\ngit commit -am \"add 30 more episodes v2\"\ndvc push\n\n# 回滚到旧版本数据\ngit checkout v1 -- data/episodes.dvc\ndvc checkout\n```\n\n### 数据可视化工具\n\n```python\n# === Rerun.io: 机器人数据可视化神器 ===\nimport rerun as rr\nimport numpy as np\n\nrr.init('robot_data_viewer')\nrr.connect()  # 连接到 Rerun Viewer\n\n# 可视化轨迹\nfor t in range(100):\n    # 图像\n    image = np.random.randint(0, 255, (480, 640, 3), dtype=np.uint8)\n    rr.log('camera/rgb', rr.Image(image))\n    \n    # 关节状态\n    joints = np.sin(np.arange(7) * 0.1 + t * 0.05)\n    for i, j in enumerate(joints):\n        rr.log(f'joints/j{i}', rr.Scalar(j))\n    \n    # 末端轨迹\n    pos = [np.sin(t*0.1)*0.3, np.cos(t*0.1)*0.3, 0.5+np.sin(t*0.05)*0.1]\n    rr.log('end_effector', rr.Points3D([pos], radii=0.01))\n\nprint('Rerun: 同时可视化图像+曲线+3D轨迹+点云')\nprint('安装: pip install rerun-sdk')\nprint('启动Viewer: rerun')\n```\n\n### 练习\n1. 把你采集的 HDF5 数据转换为 LeRobot 格式并上传到 HuggingFace Hub\n2. 用 DVC 管理数据版本，体验数据回滚\n3. 用 Rerun 可视化一条完整轨迹（图像+关节曲线+末端3D轨迹）\n\n### 参考答案\n\n**1. HDF5 → LeRobot 格式转换并上传**\n\n```python\nfrom lerobot.common.datasets.lerobot_dataset import LeRobotDataset\nimport h5py\nimport numpy as np\nimport os\n\ndef hdf5_to_lerobot(hdf5_dir, repo_id='username/my-robot-data'):\n    \"\"\"将HDF5数据转换为LeRobot格式\"\"\"\n    # LeRobot 提供了数据创建API\n    # 详见: https://github.com/huggingface/lerobot/blob/main/examples/\n    \n    files = sorted([f for f in os.listdir(hdf5_dir) if f.endswith('.hdf5')])\n    print(f'转换 {len(files)} episodes...')\n    \n    # 实际代码需要按 LeRobot 文档创建 Dataset\n    # from lerobot.common.datasets.push_dataset_to_hub import push_dataset_to_hub\n    # push_dataset_to_hub(local_dir, repo_id)\n    \n    print(f'上传到: https://huggingface.co/datasets/{repo_id}')\n    print('其他用户可以通过以下代码直接使用:')\n    print(f'  dataset = LeRobotDataset(\"{repo_id}\")')\n\nprint('LeRobot 上传命令:')\nprint('huggingface-cli login')\nprint('python lerobot/scripts/push_dataset_to_hub.py --raw-dir ./data --repo-id user/dataset')\n```\n\n**2. DVC 数据版本管理**\n\n> 见上方 bash 命令完整流程。关键点:\n> - `dvc add` 生成数据指纹 (.dvc 文件)\n> - Git 管理 .dvc 文件，DVC 管理实际数据\n> - `dvc checkout` 可以切换到任意历史版本的数据\n\n**3. Rerun 可视化**\n\n> 见上方 Python 代码。安装 `pip install rerun-sdk` 后运行。\n> Rerun Viewer 支持时间轴拖拽回放，同时显示多个模态。\n"
    },
    {
      "title": "数据闭环系统",
      "content": "### 数据闭环：持续改进的飞轮\n\n```\n┌─────────────────────────────────────────────┐\n│              数据闭环系统                      │\n│                                              │\n│  采集 ──→ 清洗 ──→ 训练 ──→ 部署 ──→ 评估  │\n│   ↑                                     │    │\n│   └──── 针对失败场景补充数据 ←──────────┘    │\n│                                              │\n│  每轮迭代:                                    │\n│  Round 1: 50 demos → 45% 成功率              │\n│  Round 2: +30 demos (失败场景) → 65%         │\n│  Round 3: +20 demos (边界情况) → 80%         │\n│  Round 4: +10 demos (精细调整) → 90%         │\n└─────────────────────────────────────────────┘\n```\n\n### 自动失败分析\n\n```python\nimport numpy as np\nfrom collections import Counter\n\nclass FailureAnalyzer:\n    \"\"\"自动分析策略失败原因,指导下一轮数据采集\"\"\"\n    \n    def __init__(self):\n        self.failure_log = []\n    \n    def log_episode(self, episode_data, success, failure_reason=''):\n        self.failure_log.append({\n            'success': success,\n            'reason': failure_reason,\n            'init_state': episode_data['init_state'],\n            'length': episode_data['length'],\n            'final_distance': episode_data.get('final_distance', None),\n        })\n    \n    def analyze(self):\n        total = len(self.failure_log)\n        successes = sum(1 for e in self.failure_log if e['success'])\n        failures = [e for e in self.failure_log if not e['success']]\n        \n        print(f'=== 失败分析报告 ===')\n        print(f'总评估: {total}, 成功: {successes} ({successes/total:.0%})')\n        print(f'失败: {len(failures)} ({len(failures)/total:.0%})')\n        \n        if not failures:\n            print('无失败案例!')\n            return {}\n        \n        # 失败原因分布\n        reasons = Counter(e['reason'] for e in failures if e['reason'])\n        print(f'\\n失败原因TOP:')\n        for reason, count in reasons.most_common(5):\n            print(f'  {reason}: {count} ({count/len(failures):.0%})')\n        \n        # 失败状态聚类分析\n        fail_states = np.array([e['init_state'] for e in failures])\n        succ_states = np.array([e['init_state'] for e in self.failure_log if e['success']])\n        \n        if len(fail_states) > 0 and len(succ_states) > 0:\n            # 找出失败集中的状态区域\n            fail_center = fail_states.mean(axis=0)\n            succ_center = succ_states.mean(axis=0)\n            print(f'\\n失败集中区域: {fail_center[:3].round(3)}')\n            print(f'成功集中区域: {succ_center[:3].round(3)}')\n            print(f'\\n→ 建议: 在失败区域 [{fail_center[0]:.2f}±0.1, {fail_center[1]:.2f}±0.1] 补充采集')\n        \n        return dict(reasons)\n\n# 使用\nanalyzer = FailureAnalyzer()\nfor i in range(100):\n    success = np.random.random() > 0.35\n    reason = '' if success else np.random.choice(['抓空', '碰撞', '超时', '掉落'])\n    analyzer.log_episode(\n        {'init_state': np.random.randn(6), 'length': np.random.randint(20, 200)},\n        success, reason)\n\nanalyzer.analyze()\n```\n\n### 完整数据闭环流程\n\n```\n迭代 1 (基线):\n  1. 采集 50 条高质量演示 (约2小时)\n  2. 训练 ACT/Diffusion Policy (30分钟-2小时)\n  3. 部署评估 100 次 → 成功率 40%\n  4. 分析: 60% 失败在物体靠近边缘时\n\n迭代 2 (针对性补充):\n  1. 专门在边缘位置采集 30 条\n  2. 合并数据重新训练\n  3. 评估 → 成功率 65%\n  4. 分析: 剩余失败多在光照变化时\n\n迭代 3 (增强):\n  1. 在不同光照下采集 20 条\n  2. 同时加图像颜色增强\n  3. 评估 → 成功率 82%\n  4. 分析: 个别物体形状失败\n\n迭代 4 (收尾):\n  1. 补充 10 条特殊物体演示\n  2. 微调模型\n  3. 评估 → 成功率 90%+ ✅\n\n总计: 110 条 demo, 4 轮迭代, ~1天\n(对比: 一次性采 200 条可能只到 70%)\n```\n\n### 练习\n1. 实现一个自动失败分析器，从评估日志中提取失败模式\n2. 设计一个 4 轮迭代的数据闭环实验，记录每轮的数据量和成功率\n3. 实现主动学习采样策略：优先在不确定性高的区域采集数据\n\n### 参考答案\n\n**1. 见上方 FailureAnalyzer 类**\n\n> 核心思路: 统计失败原因分布 + 聚类分析失败状态空间\n> 输出: 下一轮应该在哪些场景补充数据\n\n**2. 数据闭环实验记录模板**\n\n```python\nimport json\nfrom datetime import datetime\n\ndef log_iteration(round_num, n_new, n_total, success_rate, failure_analysis, notes=''):\n    \"\"\"记录每轮迭代\"\"\"\n    record = {\n        'round': round_num,\n        'timestamp': datetime.now().isoformat(),\n        'new_episodes': n_new,\n        'total_episodes': n_total,\n        'success_rate': success_rate,\n        'failure_analysis': failure_analysis,\n        'notes': notes,\n    }\n    \n    # 追加到日志文件\n    log_file = 'data_loop_log.jsonl'\n    with open(log_file, 'a') as f:\n        f.write(json.dumps(record, ensure_ascii=False) + '\\n')\n    \n    print(f'Round {round_num}: {n_total} episodes → {success_rate:.0%}')\n    return record\n\n# 模拟4轮迭代\nlog_iteration(1, 50, 50, 0.40, {'抓空': 25, '碰撞': 15, '超时': 10}, '基线采集')\nlog_iteration(2, 30, 80, 0.65, {'碰撞': 12, '超时': 8, '光照': 5}, '补充边缘场景')\nlog_iteration(3, 20, 100, 0.82, {'光照': 3, '特殊物体': 5}, '不同光照+颜色增强')\nlog_iteration(4, 10, 110, 0.91, {'特殊物体': 2}, '特殊物体微调')\n```\n\n**3. 主动学习采样**\n\n```python\nimport numpy as np\n\ndef active_learning_sampling(model, state_space, n_samples=20):\n    \"\"\"主动学习: 在不确定性高的区域优先采集\n    \n    使用 Monte Carlo Dropout 估计不确定性\n    \"\"\"\n    # 在状态空间中均匀采样候选初始状态\n    candidates = np.random.uniform(\n        state_space['low'], state_space['high'], (1000, len(state_space['low'])))\n    \n    # 用 MC Dropout 估计每个状态的预测不确定性\n    uncertainties = []\n    for state in candidates:\n        # 多次前向传播 (dropout开启)\n        predictions = []\n        for _ in range(10):  # 10次MC采样\n            # pred = model.predict_with_dropout(state)\n            pred = np.random.randn(7) * np.exp(np.random.randn())  # 模拟\n            predictions.append(pred)\n        \n        # 不确定性 = 预测方差\n        uncertainty = np.var(predictions, axis=0).mean()\n        uncertainties.append(uncertainty)\n    \n    uncertainties = np.array(uncertainties)\n    \n    # 选择不确定性最高的 n_samples 个状态\n    top_indices = np.argsort(uncertainties)[-n_samples:]\n    selected_states = candidates[top_indices]\n    \n    print(f'主动学习选择 {n_samples} 个高不确定性状态:')\n    print(f'  不确定性范围: [{uncertainties[top_indices].min():.3f}, {uncertainties[top_indices].max():.3f}]')\n    print(f'  平均不确定性: {uncertainties.mean():.3f} → 选中: {uncertainties[top_indices].mean():.3f}')\n    print(f'  → 在这些初始状态下采集数据效率最高')\n    \n    return selected_states\n\nstate_space = {'low': np.array([-0.5, -0.5, 0, 0, 0, 0]),\n               'high': np.array([0.5, 0.5, 0.5, 3.14, 3.14, 3.14])}\nselected = active_learning_sampling(None, state_space)\n```\n"
    },
    {
      "title": "数据安全、隐私与合成数据合规",
      "lastUpdated": "2026-04-27T00:45",
      "content": "### 为什么数据隐私对具身智能重要？\n\n机器人部署在家庭、医院、工厂等敏感环境中，采集的数据包含：\n- **视觉数据**：人脸、家居环境、个人物品\n- **行为数据**：日常习惯、健康状态\n- **空间数据**：房屋布局、办公室结构\n\n### 数据合规框架\n\n| 法规 | 适用范围 | 对机器人数据的要求 |\n|------|---------|-------------------|\n| **GDPR** | 欧盟 | 数据最小化、用户同意、可删除 |\n| **CCPA** | 美国加州 | 数据透明、用户控制 |\n| **个保法** | 中国 | 最小必要、明确告知、本地存储 |\n| **ISO 27001** | 全球 | 信息安全管理体系 |\n\n### 隐私保护技术路线\n\n**1. 数据匿名化**\n- 图像中的人脸自动模糊/替换\n- 移除可辨识的个人物品标记\n- 空间坐标脱敏（去除绝对GPS信息）\n\n**2. 联邦学习 (Federated Learning)**\n\n```\n传统集中式:\n  机器人A的数据 ──┐\n  机器人B的数据 ──┤──→ 中央服务器训练 → 全局模型\n  机器人C的数据 ──┘\n  问题: 原始数据离开本地，隐私风险大\n\n联邦学习:\n  机器人A 本地训练 → 梯度/模型更新 ──┐\n  机器人B 本地训练 → 梯度/模型更新 ──┤→ 聚合 → 全局模型\n  机器人C 本地训练 → 梯度/模型更新 ──┘\n  优势: 数据不出本地，只传模型更新\n```\n\n**3. 差分隐私 (Differential Privacy)**\n\n在模型更新中添加精心校准的噪声，使得攻击者无法从模型中反推任何单个训练样本的信息。\n\n```python\nimport numpy as np\n\nclass FederatedAggregator:\n    '''联邦学习聚合器(FedAvg)'''\n    def __init__(self, n_clients, model_dim, dp_epsilon=1.0):\n        self.n_clients = n_clients\n        self.model_dim = model_dim\n        self.dp_epsilon = dp_epsilon\n        self.global_model = np.zeros(model_dim)\n\n    def aggregate(self, client_updates, use_dp=True):\n        '''聚合各客户端的模型更新'''\n        # 裁剪梯度范数(防止单个客户端影响过大)\n        max_norm = 1.0\n        clipped = []\n        for update in client_updates:\n            norm = np.linalg.norm(update)\n            if norm > max_norm:\n                update = update * (max_norm / norm)\n            clipped.append(update)\n\n        # 平均聚合\n        avg_update = np.mean(clipped, axis=0)\n\n        # 差分隐私: 添加高斯噪声\n        if use_dp:\n            sensitivity = 2 * max_norm / len(client_updates)\n            sigma = sensitivity * np.sqrt(2 * np.log(1.25)) / self.dp_epsilon\n            noise = np.random.randn(self.model_dim) * sigma\n            avg_update += noise\n\n        self.global_model += avg_update\n        return self.global_model.copy()\n\n# 示例: 3个机器人联邦学习\nagg = FederatedAggregator(n_clients=3, model_dim=10, dp_epsilon=1.0)\n\nfor round_i in range(5):\n    # 每个机器人本地训练后的梯度更新\n    updates = [np.random.randn(10) * 0.1 for _ in range(3)]\n    global_w = agg.aggregate(updates, use_dp=True)\n    print(f'Round {round_i+1}: global_model norm={np.linalg.norm(global_w):.4f}')\n\nprint('数据不出本地, 只传梯度, 且加了差分隐私噪声')\n```\n\n### 合成数据的合规优势\n\n合成数据(仿真生成)天然不涉及真实个人信息，是规避隐私问题的最佳方案：\n- 不含真实人脸/环境\n- 可以精确控制数据分布\n- 可无限生成\n- 但需要标注'合成'来源，避免误导\n\n### 练习\n1. 实现一个简单的联邦学习系统，对比集中式和联邦式训练的收敛曲线\n2. 实现差分隐私梯度下降，分析epsilon值对模型精度的影响\n3. 设计一个机器人数据采集的隐私策略文档模板"
    }
  ],
  "resources": [
    {
      "name": "Open X-Embodiment",
      "url": "https://robotics-transformer-x.github.io/",
      "type": "数据集",
      "description": "Google 牵头的大规模机器人数据集联盟，22个机构100万+轨迹。"
    },
    {
      "name": "LeRobot (HuggingFace)",
      "url": "https://github.com/huggingface/lerobot",
      "type": "框架",
      "description": "HuggingFace 开源机器人学习框架，含数据采集、训练、部署全流程。"
    },
    {
      "name": "DROID 数据集",
      "url": "https://droid-dataset.github.io/",
      "type": "数据集",
      "description": "76K 高质量 Franka 操作轨迹，多样化场景和任务。"
    },
    {
      "name": "AGIBOT WORLD 2026",
      "url": "https://agibot-world.com/",
      "type": "数据集",
      "description": "智元机器人开源，100%真实场景，覆盖商业/家居/工业等场景。"
    },
    {
      "name": "Rerun.io",
      "url": "https://rerun.io/",
      "type": "工具",
      "description": "机器人数据可视化神器，同时显示图像/3D/曲线/点云。"
    },
    {
      "name": "DVC (Data Version Control)",
      "url": "https://dvc.org/",
      "type": "工具",
      "description": "ML数据版本管理工具，Git for Data。"
    },
    {
      "name": "Data Scaling Laws (RT论文系列)",
      "url": "https://arxiv.org/abs/2407.18613",
      "type": "论文",
      "description": "机器人操作的数据Scaling Law研究，指导数据采集策略。"
    },
    {
      "name": "ALOHA 2 项目",
      "url": "https://aloha-2.github.io/",
      "type": "项目",
      "description": "Google/TRI 开源低成本双臂遥操作平台，数据采集标杆。"
    }
  ],
  "weeklyPlan": "**第1周**：数据集全景+格式标准\n**第2周**：数据采集系统搭建\n**第3周**：数据质量工程+清洗\n**第4周**：合成数据+增强\n**第5周**：数据闭环实战",
  "lastUpdated": "2026-04-27T00:45"
}