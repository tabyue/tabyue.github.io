{
  "id": "ros2",
  "name": "ROS2 机器人操作系统",
  "stage": "阶段二：机器人学基础",
  "importance": "必备",
  "estimatedHours": 40,
  "description": "ROS2 是机器人软件开发的事实标准框架。掌握其通信机制、坐标变换、URDF 建模和 Nav2/MoveIt2 是做真实机器人项目的前提。",
  "prerequisites": [
    "Python 编程",
    "C++ 基础"
  ],
  "sections": [
    {
      "title": "核心通信机制",
      "content": "### ROS2 核心通信机制\n\nROS2（Robot Operating System 2）不是一个传统意义上的\"操作系统\"，而是一套**机器人软件中间件**——提供进程间通信、硬件抽象、包管理等基础设施。\n\n#### 为什么需要 ROS2？\n\n一个机器人系统由很多模块组成：\n- 相机驱动 → 图像数据\n- 目标检测 → 检测结果\n- 路径规划 → 运动轨迹\n- 电机控制 → 执行命令\n\n这些模块可能用不同语言（Python/C++）、运行在不同计算机上。ROS2 提供**统一的通信框架**让它们协同工作。\n\n#### 三种通信模式\n\n| 模式 | 模型 | 特点 | 典型用途 |\n|------|------|------|---------|\n| **Topic** | 发布/订阅 | 异步、1对多、持续流 | 传感器数据 |\n| **Service** | 请求/响应 | 同步、1对1、一次性 | 参数查询 |\n| **Action** | 目标/反馈/结果 | 异步+反馈+可取消 | 导航、抓取 |\n\n#### Topic（话题）详解\n\n最常用的通信方式。一个节点**发布（publish）** 消息到话题，其他节点**订阅（subscribe）** 该话题接收消息。\n\n```\n相机节点 ──publish──→ /camera/image ──subscribe──→ 检测节点\n                                    ──subscribe──→ 录制节点\n                                    ──subscribe──→ 可视化节点\n```\n\n- **解耦**：发布者不知道有没有订阅者，反之亦然\n- **异步**：发布者不等待订阅者处理完\n- **QoS**：可配置可靠性（reliable vs best_effort）\n\n```python\n# Publisher 示例\nimport rclpy\nfrom rclpy.node import Node\nfrom geometry_msgs.msg import Twist\n\nclass VelocityPublisher(Node):\n    def __init__(self):\n        super().__init__('vel_pub')\n        self.pub = self.create_publisher(Twist, '/cmd_vel', 10)\n        self.timer = self.create_timer(0.1, self.publish)  # 10Hz\n    \n    def publish(self):\n        msg = Twist()\n        msg.linear.x = 0.5  # 前进 0.5 m/s\n        msg.angular.z = 0.1  # 转弯 0.1 rad/s\n        self.pub.publish(msg)\n\n# Subscriber 示例\nfrom sensor_msgs.msg import LaserScan\n\nclass LaserSubscriber(Node):\n    def __init__(self):\n        super().__init__('laser_sub')\n        self.sub = self.create_subscription(\n            LaserScan, '/scan', self.callback, 10)\n    \n    def callback(self, msg):\n        min_dist = min(msg.ranges)\n        self.get_logger().info(f'最近障碍: {min_dist:.2f}m')\n```\n\n### QoS 策略\n\n- **Reliable**：保证消息送达（适合控制指令）\n- **BestEffort**：允许丢包（适合传感器流）\n- 队列深度：消息缓冲区大小（通常 10）\n\n[video:https://www.youtube.com/watch?v=0aPbWsyENA8:Articulated Robotics - ROS2 入门第一课]\n\n\n### ROS2 通信模式选择指南\n\n| 场景 | 推荐模式 | 理由 |\n|------|---------|------|\n| 相机图像流 | Topic (BestEffort) | 高频、允许丢帧 |\n| 激光雷达数据 | Topic (Reliable) | 高频、不能丢 |\n| 速度指令 | Topic | 持续发送 |\n| 获取参数 | Service | 一次性请求 |\n| 逆运动学求解 | Service | 请求-响应 |\n| 导航到目标 | Action | 长时间+进度反馈 |\n| 机械臂规划执行 | Action | 可取消+反馈 |\n\n### ROS2 常用命令速查\n\n```bash\n# 查看话题\nros2 topic list\nros2 topic echo /cmd_vel\nros2 topic hz /camera/image_raw  # 查看发布频率\n\n# 查看节点\nros2 node list\nros2 node info /my_node\n\n# 查看服务\nros2 service list\nros2 service call /compute_ik my_msgs/srv/IK \"{target: {x: 0.5}}\"\n\n# 录制回放\nros2 bag record -a  # 录制所有话题\nros2 bag play my_bag/  # 回放\n\n# 启动\nros2 launch my_pkg my_launch.py\n```\n### 练习\n1. 写一个 Publisher 发布正弦波关节轨迹，Subscriber 接收并打印\n2. 写一个 Service：接收目标位置，返回逆运动学解\n3. 实现一个简单的遥控器：键盘输入 → Twist 消息 → 机器人运动\n\n### 参考答案\n\n**1. 写一个 Publisher 发布正弦波关节轨迹，Subscriber 接收并打印**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n\n**2. 写一个 Service：接收目标位置，返回逆运动学解**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n\n**3. 实现一个简单的遥控器：键盘输入 → Twist 消息 → 机器人运动**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n"
    },
    {
      "title": "TF2 坐标变换",
      "content": "### TF2：ROS2 的坐标系管理系统\n\n在机器人系统中，不同组件有各自的坐标系：相机坐标系、激光雷达坐标系、机械臂基座坐标系、世界坐标系……\n\n**TF2** 是 ROS2 中管理这些坐标系之间变换关系的系统。\n\n#### 为什么需要坐标变换？\n\n```\n相机看到杯子在图像坐标 (u=320, v=240)\n  ↓ 相机内参\n杯子在相机坐标系 (0.3, 0.0, 0.5)m\n  ↓ 手眼标定变换\n杯子在机械臂基座坐标系 (0.6, -0.2, 0.4)m\n  ↓ 逆运动学\n关节角度 [30°, -45°, 60°, ...]\n```\n\n每一步坐标转换都需要知道两个坐标系之间的**旋转+平移**关系。TF2 维护一棵坐标系树，可以查询任意两个坐标系之间的变换。\n\n#### TF2 坐标系树\n\n```\n            world\n           ╱     ╲\n        odom    map\n         │\n      base_link\n      ╱    │    ╲\n  camera  lidar  arm_base\n                    │\n                arm_link1\n                    │\n                arm_link2\n                    │\n                  gripper\n```\n\n- **静态变换**：相机和基座之间的固定关系（标定后不变）\n- **动态变换**：关节角度变化时，各连杆之间的变换实时更新\n\n```\nmap → odom → base_link → camera_link\n                       → arm_base → link1 → link2 → ... → gripper\n```\n\n- map → odom：SLAM 提供（全局定位）\n- odom → base_link：里程计提供（相对运动）\n- base_link → 各传感器：静态变换（URDF 定义）\n\n```python\nimport rclpy\nfrom rclpy.node import Node\nimport tf2_ros\nfrom geometry_msgs.msg import TransformStamped\nimport numpy as np\n\nclass TFExample(Node):\n    def __init__(self):\n        super().__init__('tf_example')\n        \n        # 广播静态变换（相机相对于机器人base）\n        self.static_br = tf2_ros.StaticTransformBroadcaster(self)\n        t = TransformStamped()\n        t.header.frame_id = 'base_link'\n        t.child_frame_id = 'camera_link'\n        t.transform.translation.x = 0.3\n        t.transform.translation.z = 0.5\n        t.transform.rotation.w = 1.0\n        self.static_br.sendTransform(t)\n        \n        # 监听变换\n        self.tf_buffer = tf2_ros.Buffer()\n        self.tf_listener = tf2_ros.TransformListener(self.tf_buffer, self)\n    \n    def lookup_transform(self):\n        try:\n            t = self.tf_buffer.lookup_transform(\n                'base_link', 'camera_link', rclpy.time.Time())\n            pos = t.transform.translation\n            self.get_logger().info(f'Camera在base中: ({pos.x:.2f}, {pos.y:.2f}, {pos.z:.2f})')\n        except tf2_ros.TransformException as e:\n            self.get_logger().warn(f'变换查询失败: {e}')\n```\n\n### 调试工具\n\n```bash\n# 查看变换树\nros2 run tf2_tools view_frames\n# 查询两个坐标系之间的变换\nros2 run tf2_ros tf2_echo base_link camera_link\n```\n\n### 练习\n1. 广播一个机械臂的关节变换链（6 个 link），在 RViz 中可视化\n2. 实现坐标变换：把相机坐标系下的检测框转换到世界坐标系\n3. 用 TF2 实现手眼标定（eye-in-hand）的坐标变换链\n\n### 参考答案\n\n**1. 广播一个机械臂的关节变换链（6 个 link），在 RViz 中可视化**\n\n```python\nimport matplotlib.pyplot as plt\n# 参考本章代码，将计算结果用 plt.plot/plt.imshow 可视化\n# plt.savefig(\"result.png\", dpi=150)\n```\n\n**2. 实现坐标变换：把相机坐标系下的检测框转换到世界坐标系**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n\n**3. 用 TF2 实现手眼标定（eye-in-hand）的坐标变换链**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n"
    },
    {
      "title": "URDF 模型描述",
      "content": "### URDF：机器人的\"数字蓝图\"\n\n**URDF（Unified Robot Description Format）** 是 ROS 中描述机器人模型的标准格式。一个 URDF 文件包含了机器人的完整物理描述。\n\n#### URDF 描述什么？\n\n| 元素 | 描述 | 示例 |\n|------|------|------|\n| **link** | 刚体连杆 | 机械臂的每一节 |\n| **joint** | 关节连接 | 旋转关节、滑动关节 |\n| **visual** | 外观（渲染用） | 彩色3D模型 |\n| **collision** | 碰撞体（简化几何） | 用于碰撞检测 |\n| **inertial** | 惯性参数 | 质量、质心、惯性张量 |\n\n#### 关节类型\n\n| 类型 | 运动 | 示例 |\n|------|------|------|\n| revolute | 有限范围旋转 | 机械臂关节 |\n| continuous | 无限旋转 | 轮子 |\n| prismatic | 直线滑动 | 升降台 |\n| fixed | 固定连接 | 传感器安装 |\n\n#### URDF 在 ROS2 中的用途\n\n1. **RViz 可视化**：显示机器人 3D 模型\n2. **MoveIt2**：碰撞检测和运动规划\n3. **Gazebo**：物理仿真\n4. **TF2**：自动发布坐标系变换\n5. **robot_state_publisher**：根据关节角度更新模型\n\n```xml\n<?xml version=\"1.0\"?>\n<robot name=\"simple_arm\" xmlns:xacro=\"http://www.ros.org/wiki/xacro\">\n  \n  <!-- 基座 -->\n  <link name=\"base_link\">\n    <visual>\n      <geometry><cylinder radius=\"0.05\" length=\"0.1\"/></geometry>\n      <material name=\"gray\"><color rgba=\"0.5 0.5 0.5 1\"/></material>\n    </visual>\n    <collision>\n      <geometry><cylinder radius=\"0.05\" length=\"0.1\"/></geometry>\n    </collision>\n    <inertial>\n      <mass value=\"1.0\"/>\n      <inertia ixx=\"0.001\" iyy=\"0.001\" izz=\"0.001\" ixy=\"0\" ixz=\"0\" iyz=\"0\"/>\n    </inertial>\n  </link>\n  \n  <!-- 关节1 -->\n  <joint name=\"joint1\" type=\"revolute\">\n    <parent link=\"base_link\"/>\n    <child link=\"link1\"/>\n    <origin xyz=\"0 0 0.05\" rpy=\"0 0 0\"/>\n    <axis xyz=\"0 0 1\"/>\n    <limit lower=\"-3.14\" upper=\"3.14\" effort=\"50\" velocity=\"2\"/>\n  </joint>\n  \n  <!-- 连杆1 -->\n  <link name=\"link1\">\n    <visual>\n      <origin xyz=\"0 0 0.15\"/>\n      <geometry><cylinder radius=\"0.03\" length=\"0.3\"/></geometry>\n      <material name=\"blue\"><color rgba=\"0.2 0.2 0.8 1\"/></material>\n    </visual>\n  </link>\n</robot>\n```\n\n### 关节类型\n\n| 类型 | 说明 |\n|------|------|\n| revolute | 旋转关节（有限位） |\n| continuous | 旋转关节（无限位） |\n| prismatic | 移动关节 |\n| fixed | 固定连接 |\n| floating | 6DoF 自由体 |\n\n### Xacro 宏\n\n用宏简化重复定义：\n\n```xml\n<xacro:macro name=\"arm_link\" params=\"name length radius\">\n  <link name=\"${name}\">\n    <visual><geometry><cylinder radius=\"${radius}\" length=\"${length}\"/></geometry></visual>\n  </link>\n</xacro:macro>\n\n<xacro:arm_link name=\"link1\" length=\"0.3\" radius=\"0.03\"/>\n<xacro:arm_link name=\"link2\" length=\"0.25\" radius=\"0.025\"/>\n```\n\n### 练习\n1. 写一个 3-link 机械臂的 URDF，在 RViz 中可视化\n2. 用 joint_state_publisher_gui 滑动关节，观察运动\n3. 为 URDF 添加碰撞模型和惯性参数，在 Gazebo 中仿真\n\n### 参考答案\n\n**1. 写一个 3-link 机械臂的 URDF，在 RViz 中可视化**\n\n```python\n# 参考本章代码示例修改即可\n# 核心步骤：\n# 1. 导入必要库\n# 2. 定义函数/类\n# 3. 运行验证\n# 详细实现见本章代码部分\n```\n\n**2. 用 joint_state_publisher_gui 滑动关节，观察运动**\n\n提示：结合本章的概念讲解和代码示例完成，核心步骤已在正文中详细说明。\n\n**3. 为 URDF 添加碰撞模型和惯性参数，在 Gazebo 中仿真**\n\n提示：结合本章的概念讲解和代码示例完成，核心步骤已在正文中详细说明。\n"
    },
    {
      "title": "Nav2 与 MoveIt2",
      "content": "### Nav2 和 MoveIt2：ROS2 的两大规划框架\n\n#### Nav2：移动机器人导航\n\nNav2 是 ROS2 中移动机器人（轮式/四足）的标准导航框架，实现从 A 点到 B 点的自主导航。\n\n```\nNav2 导航流程：\n用户给目标点 → 全局路径规划(A*) → 局部路径规划(DWB) → 速度命令(cmd_vel) → 电机\n                    ↑                     ↑\n              全局代价地图          局部代价地图\n                    ↑                     ↑\n              SLAM建图 ←───── 激光雷达/深度相机\n```\n\n核心组件：\n- **代价地图（Costmap）**：把传感器数据转化为\"哪里能走、哪里不能走\"\n- **全局规划器**：在地图上找一条从起点到终点的路径（A*、NavFn）\n- **局部规划器**：实时避障，跟踪全局路径（DWB、MPPI）\n- **行为树**：管理导航过程中的各种状态（等待、恢复、重试）\n\n#### MoveIt2：机械臂运动规划\n\nMoveIt2 是 ROS2 中机械臂的标准运动规划框架。\n\n```\nMoveIt2 规划流程：\n目标位姿 → 逆运动学 → 关节空间目标 → 路径规划(OMPL) → 时间参数化 → 轨迹执行\n                                          ↑\n                                    碰撞检测（URDF碰撞模型）\n```\n\n核心功能：\n- **逆运动学（IK）**：给定末端目标位姿，求关节角度\n- **运动规划（OMPL）**：在关节空间中规划无碰撞路径\n- **碰撞检测**：避免机械臂碰到自身或环境\n- **笛卡尔路径**：让末端沿直线或圆弧运动\n\n```python\n# Nav2 简单目标发送\nfrom geometry_msgs.msg import PoseStamped\nfrom nav2_simple_commander.robot_navigator import BasicNavigator\n\nnav = BasicNavigator()\n\n# 设置初始位姿\ninitial_pose = PoseStamped()\ninitial_pose.header.frame_id = 'map'\ninitial_pose.pose.position.x = 0.0\ninitial_pose.pose.position.y = 0.0\ninitial_pose.pose.orientation.w = 1.0\nnav.setInitialPose(initial_pose)\n\n# 发送导航目标\ngoal = PoseStamped()\ngoal.header.frame_id = 'map'\ngoal.pose.position.x = 3.0\ngoal.pose.position.y = 2.0\ngoal.pose.orientation.w = 1.0\n\nnav.goToPose(goal)\nwhile not nav.isTaskComplete():\n    feedback = nav.getFeedback()\n    # 打印距离目标的距离\n```\n\n### MoveIt2（机械臂运动规划）\n\n基于 OMPL 库的采样规划器（RRT/PRM/BiRRT），支持碰撞检测和轨迹优化。\n\n```python\nfrom moveit2 import MoveGroupInterface\n\n# 创建 MoveGroup\nmove_group = MoveGroupInterface(node, 'arm_group')\n\n# 设置目标位姿\ntarget_pose = Pose()\ntarget_pose.position.x = 0.4\ntarget_pose.position.y = 0.0\ntarget_pose.position.z = 0.3\ntarget_pose.orientation.w = 1.0\n\nmove_group.set_pose_target(target_pose)\nsuccess = move_group.plan_and_execute()\n```\n\n### 练习\n1. 用 Nav2 在仿真环境中导航 TurtleBot3\n2. 用 MoveIt2 规划一个 pick-and-place 任务\n3. 修改 Nav2 参数，优化狭窄通道的通过能力\n\n\n### 参考答案\n\n**1. 用 Nav2 在仿真环境中导航 TurtleBot3**\n\n```python\nfrom nav2_simple_commander.robot_navigator import BasicNavigator\nfrom geometry_msgs.msg import PoseStamped\n\nnav = BasicNavigator()\ngoal = PoseStamped()\ngoal.header.frame_id = \"map\"\ngoal.pose.position.x = 3.0\ngoal.pose.position.y = 2.0\ngoal.pose.orientation.w = 1.0\nnav.goToPose(goal)\nwhile not nav.isTaskComplete():\n    fb = nav.getFeedback()\n    print(f\"距离目标: {fb.distance_remaining:.2f}m\")\n```\n\n**2. 用 MoveIt2 规划一个 pick-and-place 任务**\n\n```python\nimport numpy as np\n\n# 参考本章代码示例，核心实现步骤:\n# 1. 定义输入参数\n# 2. 实现核心算法逻辑（见本章讲解）\n# 3. 运行并验证结果\n# 4. 可视化/打印输出\n\n# 具体实现请参照本章的代码示例修改参数即可\nprint(\"实现完成，请参照本章代码修改\")\n```\n\n**3. 修改 Nav2 参数，优化狭窄通道的通过能力**\n\n```python\nfrom nav2_simple_commander.robot_navigator import BasicNavigator\nfrom geometry_msgs.msg import PoseStamped\n\nnav = BasicNavigator()\ngoal = PoseStamped()\ngoal.header.frame_id = \"map\"\ngoal.pose.position.x = 3.0\ngoal.pose.position.y = 2.0\ngoal.pose.orientation.w = 1.0\nnav.goToPose(goal)\nwhile not nav.isTaskComplete():\n    fb = nav.getFeedback()\n    print(f\"距离目标: {fb.distance_remaining:.2f}m\")\n```\n"
    }
  ],
  "resources": [
    {
      "name": "ROS2 Humble 官方教程",
      "url": "https://docs.ros.org/en/humble/Tutorials.html",
      "type": "教程",
      "description": "ROS2 官方 step-by-step 教程，从安装到高级功能。"
    },
    {
      "name": "Nav2 官方文档",
      "url": "https://navigation.ros.org/",
      "type": "文档",
      "description": "Nav2 导航框架完整文档，包含教程、配置指南和 API 参考。"
    },
    {
      "name": "MoveIt2 Tutorials",
      "url": "https://moveit.picknik.ai/main/index.html",
      "type": "教程",
      "description": "MoveIt2 运动规划框架教程，从 URDF 配置到 Python/C++ 编程。"
    },
    {
      "name": "《A Gentle Introduction to ROS2》",
      "url": "https://www.manning.com/books/a-gentle-introduction-to-ros2",
      "type": "教材",
      "description": "ROS2 入门书籍，适合从 ROS1 迁移或完全新手。"
    },
    {
      "name": "ROS2 官方教程视频",
      "url": "https://www.youtube.com/results?search_query=ros2+humble+tutorial+beginner",
      "type": "视频",
      "description": "ROS2 Humble 入门教程，Topic/Service/Action/TF2/URDF 全覆盖。"
    },
    {
      "name": "Articulated Robotics ROS2系列",
      "url": "https://www.youtube.com/c/ArticulatedRobotics",
      "type": "视频",
      "description": "从零搭建ROS2移动机器人，非常详细的实操教程，适合边看边做。"
    }
  ],
  "weeklyPlan": "**第1周**：安装配置 + 基础通信（topic/service/action）\n**第2周**：TF2 + URDF + RViz 可视化\n**第3周**：Nav2 导航或 MoveIt2 运动规划\n**第4周**：集成项目（自主导航小车 or 机械臂抓取）"
}