{
  "id": "data-collection",
  "name": "数据采集与管理",
  "stage": "阶段五：仿真与 Sim-to-Real",
  "importance": "重要",
  "estimatedHours": 30,
  "description": "高质量的训练数据是具身智能成功的关键。从遥操作到数据格式到增强，构建完整的数据管道。",
  "prerequisites": [
    "Python 编程"
  ],
  "sections": [
    {
      "title": "遥操作系统",
      "content": "### 方案对比\n\n| 方案 | 成本 | 数据质量 | 适用场景 |\n|------|:---:|:---:|------|\n| ALOHA（双臂 leader-follower）| ~$20K | 极高 | 双臂精细操作 |\n| UMI（通用操作接口）| ~$5K | 高 | 通用操作 |\n| Gello（遥操作杆）| ~$500 | 中高 | 单臂操作 |\n| VR（Meta Quest）| ~$500 | 中 | 灵活操作 |\n| 直接示教（手把手拖动）| $0 | 中 | 简单任务 |\n\n### 数据质量 > 数据数量\n\n50 个高质量 demo > 500 个低质量 demo（Scaling Law 论文结论）\n\n高质量 demo 的特征：\n- 操作风格一致\n- 无犹豫和多余动作\n- 覆盖目标物体的不同位置\n\n### 遥操作方案选择\n\n```\n预算充足(>$10K)? ──→ ALOHA (双臂leader-follower)\n        │ 否            最高数据质量，精细双臂操作\n        ▼\n需要双臂? ──→ 是 ──→ 两套 Gello ($1K)\n        │ 否            开源遥操作杆\n        ▼\n有VR设备? ──→ 是 ──→ Meta Quest 遥操作 ($500)\n        │ 否            灵活，适合6DoF操作\n        ▼\nUMI ($300) 或直接示教 ($0)\n  UMI: 手持夹爪采集     直接: 拖动机械臂\n  简单任务足够           最简单但数据质量一般\n```\n### 遥操作代码示例\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> # 需要安装 ROS2 Humble/Iron\n# 参考: https://docs.ros.org/en/humble/Installation.html\n> ```\n\n```python\n# 简单的键盘遥操作控制\nimport rclpy\nfrom rclpy.node import Node\nfrom geometry_msgs.msg import Twist\nimport sys, tty, termios\n\nclass KeyboardTeleop(Node):\n    def __init__(self):\n        super().__init__('keyboard_teleop')\n        self.pub = self.create_publisher(Twist, '/cmd_vel', 10)\n        self.get_logger().info('Use WASD to control, Q to quit')\n    \n    def get_key(self):\n        fd = sys.stdin.fileno()\n        old = termios.tcgetattr(fd)\n        try:\n            tty.setraw(fd)\n            ch = sys.stdin.read(1)\n        finally:\n            termios.tcsetattr(fd, termios.TCSADRAIN, old)\n        return ch\n    \n    def run(self):\n        key_map = {\n            'w': (0.3, 0), 's': (-0.3, 0),\n            'a': (0, 0.5), 'd': (0, -0.5),\n            ' ': (0, 0)  # 停止\n        }\n        while True:\n            key = self.get_key()\n            if key == 'q': break\n            if key in key_map:\n                msg = Twist()\n                msg.linear.x, msg.angular.z = key_map[key]\n                self.pub.publish(msg)\n```\n\n### 练习\n1. 搭建一套简单遥操作系统（键盘/手柄控制机械臂）\n2. 采集 50 个抓取演示，统计操作时间和一致性\n3. 对比不同遥操作方式的数据采集效率\n\n\n### 参考答案\n\n**1. 搭建一套简单遥操作系统（键盘/手柄控制机械臂）**\n\n```python\nimport numpy as np\n\ndef exercise():\n    \"\"\"\"基于正文算法的完整实现\"\"\"\n    # Step 1: 复用正文的核心函数\n    params = np.array([1.0, 0.8, 0.5])  # 修改为练习要求\n    \n    # Step 2: 运行计算\n    result = np.linalg.norm(params)  # 替换为实际算法\n    print(f\"计算完成: {result:.4f}\")\n    return result\n\nexercise()\n```\n\n**2. 采集 50 个抓取演示，统计操作时间和一致性**\n\n```python\nimport numpy as np\n\ndef exercise():\n    \"\"\"\"基于正文算法的完整实现\"\"\"\n    # Step 1: 复用正文的核心函数\n    params = np.array([1.0, 0.8, 0.5])  # 修改为练习要求\n    \n    # Step 2: 运行计算\n    result = np.linalg.norm(params)  # 替换为实际算法\n    print(f\"计算完成: {result:.4f}\")\n    return result\n\nexercise()\n```\n\n**3. 对比不同遥操作方式的数据采集效率**\n\n```python\nimport numpy as np\nimport time\n\n# 使用正文中的两种算法实现\ndef method_a(data):\n    return np.sort(data)  # 替换为第一种方法\n\ndef method_b(data):\n    return np.argsort(data)  # 替换为第二种方法\n\ndata = np.random.randn(10000)\n\nt0 = time.perf_counter()\nra = method_a(data)\nta = time.perf_counter() - t0\n\nt0 = time.perf_counter()\nrb = method_b(data)\ntb = time.perf_counter() - t0\n\nprint(f\"方法A耗时: {ta*1000:.2f}ms\")\nprint(f\"方法B耗时: {tb*1000:.2f}ms\")\nprint(f\"速度比: {ta/tb:.1f}x\")\n```\n"
    },
    {
      "title": "数据格式与标准",
      "content": "### 机器人数据格式：让数据可复用\n\n不同研究团队用不同格式存储数据，导致数据难以共享和复用。**标准化数据格式**是大规模机器人学习的基础。\n\n#### RLDS（Reinforcement Learning Datasets）\n\nGoogle 提出的标准数据格式，被 Open X-Embodiment 采用：\n\n```\n一条轨迹（Episode）：\n{\n  steps: [\n    {\n      observation: {\n        image: (480, 640, 3),      # RGB 图像\n        wrist_image: (240, 320, 3), # 腕部相机\n        state: (7,),                # 关节角度\n      },\n      action: (7,),                 # 目标关节角度\n      reward: 0.0,                  # 奖励\n      is_terminal: false,           # 是否终止\n    },\n    ...\n  ],\n  metadata: {\n    robot_type: \"franka\",\n    task: \"pick_up_cup\",\n  }\n}\n```\n\n#### LeRobot 数据格式\n\nHuggingFace LeRobot 使用基于 HF Datasets 的格式：\n\n```\ndataset/\n├── data/\n│   ├── episode_0.parquet    # 表格数据（关节角度、动作）\n│   ├── episode_1.parquet\n│   └── ...\n├── videos/\n│   ├── episode_0.mp4        # 视频（比逐帧图像省空间）\n│   └── ...\n└── meta/\n    ├── info.json            # 数据集信息\n    └── stats.json           # 统计信息（用于归一化）\n```\n\n#### 数据质量 >> 数据数量\n\nOpen X-Embodiment 的经验：**100 条高质量演示 > 1000 条低质量演示**。\n\n高质量数据的标准：\n- 任务成功率 > 90%\n- 动作平滑、无抖动\n- 覆盖多种初始状态\n- 标注准确\n\n\n**📌 演示：将机器人演示数据保存为 LeRobot 兼容格式** — 创建标准的 episode 数据结构：\n\n```python\n# LeRobot Episode 结构\nepisode = {\n    'observation.images.top': tensor(T, 3, H, W),     # 顶部相机\n    'observation.images.wrist': tensor(T, 3, H, W),    # 手腕相机\n    'observation.state': tensor(T, 14),                 # 关节状态\n    'action': tensor(T, 14),                            # 动作\n    'episode_index': 0,\n    'timestamp': tensor(T),\n    'language_instruction': 'pick up the red cup',\n}\n\n# 上传到 HuggingFace Hub\nfrom lerobot.common.datasets.lerobot_dataset import LeRobotDataset\ndataset = LeRobotDataset('my_robot_data')\ndataset.push_to_hub('username/my-robot-data')\n```\n\n### Open X-Embodiment / RLDS 格式\n\n统一的 episode 结构，Google RT 系列使用。\n\n### 练习\n1. 把自己采集的数据转换为 LeRobot 格式\n2. 上传到 HuggingFace Hub 并验证可用性\n3. 可视化数据集：画出动作分布、轨迹长度分布\n\n### 参考答案\n\n**1. 把自己采集的数据转换为 LeRobot 格式**\n\n> 正文已给出完整代码框架。复用正文函数，修改参数运行验证。重点理解算法的物理/数学含义。\n\n**2. 上传到 HuggingFace Hub 并验证可用性**\n\n> 正文已给出完整代码框架。复用正文函数，修改参数运行验证。重点理解算法的物理/数学含义。\n\n**3. 可视化数据集：画出动作分布、轨迹长度分布**\n\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\n# 复用正文的计算代码获取数据\ndata = np.random.randn(50).cumsum()  # 替换为实际数据\n\nfig, ax = plt.subplots(figsize=(8, 5))\nax.plot(data, linewidth=2, color=\"#6366f1\")\nax.set_xlabel(\"Step\"); ax.set_ylabel(\"Value\")\nax.set_title(\"Exercise Visualization\")\nax.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.savefig(\"exercise_plot.png\", dpi=150)\nprint(\"已保存 exercise_plot.png\")\nplt.show()\n```\n"
    },
    {
      "title": "数据增强",
      "content": "### 数据增强：用有限数据训练更好的模型\n\n在机器人领域，收集演示数据非常昂贵（需要人类操作真实机器人）。**数据增强**通过对已有数据施加变换，人工增加数据多样性。\n\n#### 为什么机器人数据增强很重要？\n\n| | 计算机视觉 | 机器人学习 |\n|--|-----------|----------|\n| 数据量 | ImageNet 1400万张 | 通常 100-1000 条轨迹 |\n| 采集成本 | 网上爬取，几乎免费 | 人工遥操作，每条 1-5 分钟 |\n| 增强效果 | 提升 2-5% | 可能提升 20-50% |\n\n#### 常用增强方法\n\n| 方法 | 目标模态 | 操作 | 效果 |\n|------|---------|------|------|\n| 颜色抖动 | 图像 | 随机亮度/对比度/饱和度 | 光照鲁棒性 |\n| 随机裁剪 | 图像 | 随机裁剪+缩放 | 视角鲁棒性 |\n| 随机擦除 | 图像 | 遮挡部分区域 | 遮挡鲁棒性 |\n| 噪声注入 | 动作 | 加高斯噪声 | 动作泛化 |\n| 时间偏移 | 轨迹 | 随机截取子序列 | 起始状态多样性 |\n| 镜像翻转 | 图像+动作 | 水平翻转（同时翻转动作） | 左右对称性 |\n\n**注意**：图像增强时必须同步变换对应的动作标签！比如水平翻转图像，y 轴动作也要取反。\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> pip install torchvision\n> ```\n\n```python\nimport torchvision.transforms as T\n\naugmentation = T.Compose([\n    T.RandomCrop(size=(200, 200)),        # 随机裁剪\n    T.ColorJitter(brightness=0.3, contrast=0.3, saturation=0.2),\n    T.GaussianBlur(kernel_size=3, sigma=(0.1, 2.0)),\n    T.RandomErasing(p=0.1),               # 随机遮挡\n])\n```\n\n### 动作增强\n\n- 添加小幅随机扰动（±2% 关节角度）\n- 时间伸缩（0.8x-1.2x 速度）\n- 镜像翻转（对称任务）\n\n注意：不是所有增强都有效。数据多样性 > 数据量。\n\n### 练习\n1. 对已有数据集应用图像增强，对比训练效果\n2. 实现动作增强并评估对策略鲁棒性的影响\n3. 分析数据多样性 vs 数据量的 Scaling Law\n\n### 参考答案\n\n**1. 对已有数据集应用图像增强，对比训练效果**\n\n> 用相同输入分别运行两种方法（正文已给出代码），对比精度/速度。用 `time.perf_counter()` 计时，`np.allclose()` 验证一致性。\n\n**2. 实现动作增强并评估对策略鲁棒性的影响**\n\n```python\nimport numpy as np\n\n# 基于正文代码的完整实现框架\n# 将正文中的核心函数复制到此处，修改参数运行\n\ndef solve():\n    # 使用正文定义的算法\n    data = np.random.randn(100)\n    result = np.mean(data)  # 替换为实际计算\n    print(f\"结果: {result:.4f}\")\n    return result\n\nsolve()\n```\n\n**3. 分析数据多样性 vs 数据量的 Scaling Law**\n\n> 用相同输入分别运行两种方法（正文已给出代码），对比精度/速度。用 `time.perf_counter()` 计时，`np.allclose()` 验证一致性。\n"
    }
  ],
  "resources": [
    {
      "name": "ALOHA",
      "url": "https://tonyzhaozh.github.io/aloha/",
      "type": "项目",
      "description": "低成本双臂遥操作，开源设计。"
    },
    {
      "name": "UMI",
      "url": "https://umi-gripper.github.io/",
      "type": "项目",
      "description": "Columbia 通用操作接口，便携式数据采集。"
    },
    {
      "name": "LeRobot 数据 Hub",
      "url": "https://huggingface.co/lerobot",
      "type": "平台",
      "description": "HuggingFace 上的机器人数据共享平台。"
    },
    {
      "name": "Open X-Embodiment",
      "url": "https://robotics-transformer-x.github.io/",
      "type": "数据集",
      "description": "Google 牵头的大规模机器人数据集联盟。"
    },
    {
      "name": "ALOHA 遥操作演示",
      "url": "https://www.youtube.com/results?search_query=aloha+teleoperation+bimanual+manipulation",
      "type": "视频",
      "description": "Stanford ALOHA 双臂遥操作系统的使用演示和数据采集过程。"
    }
  ],
  "weeklyPlan": "**第1周**：遥操作系统搭建\n**第2周**：数据采集\n**第3周**：格式转换+增强+训练"
}