{
  "title": "ros2_control 与硬件抽象",
  "lastUpdated": "2026-04-21T19:32",
  "content": "### ros2_control：连接算法与真实电机的桥梁\n\n前面我们学了 Nav2（导航）和 MoveIt2（运动规划），但它们输出的都是**期望运动**（速度指令或关节轨迹）。谁来把这些指令变成真实电机的 PWM 信号？\n\n**ros2_control** 就是这个桥梁——它定义了一套**硬件抽象层（HAL）**，让上层算法不需要关心底层硬件细节。\n\n### 为什么需要 ros2_control？\n\n```\n没有 ros2_control:\n  MoveIt2 → 自己写电机驱动代码 → 不同机器人完全不通用\n  Nav2    → 自己写底盘驱动代码 → 换个机器人全部重写\n\n有 ros2_control:\n  MoveIt2 ─┐\n  Nav2   ──┤─→ ros2_control (统一接口) → HardwareInterface → 电机\n  自定义  ──┘\n                                         可以换不同硬件，上层不用改\n```\n\n### 核心架构\n\n```\n┌─────────────────────────────────────────────┐\n│               Controller Manager            │\n│  ┌─────────────┐  ┌──────────────────────┐  │\n│  │ Joint        │  │ Diff Drive           │  │\n│  │ Trajectory   │  │ Controller           │  │\n│  │ Controller   │  │ (底盘控制)           │  │\n│  │ (机械臂控制) │  │                      │  │\n│  └──────┬──────┘  └──────────┬───────────┘  │\n│         │                     │              │\n│  ═══════╪═════════════════════╪══════════════│\n│         │    Command/State Interface         │\n│  ═══════╪═════════════════════╪══════════════│\n│         │                     │              │\n│  ┌──────┴─────────────────────┴───────────┐  │\n│  │         Hardware Interface (HAL)        │  │\n│  │  ┌──────────┐ ┌──────────┐ ┌────────┐  │  │\n│  │  │ System   │ │ Actuator │ │ Sensor │  │  │\n│  │  │ (多关节) │ │ (单关节) │ │ (IMU)  │  │  │\n│  │  └──────────┘ └──────────┘ └────────┘  │  │\n│  └─────────────────────────────────────────┘  │\n└─────────────────────────────────────────────┘\n         ↕            ↕            ↕\n     CAN Bus      EtherCAT      USB/SPI\n         ↕            ↕            ↕\n     达妙电机     Robstride      IMU\n```\n\n### 三层抽象\n\n| 层 | 组件 | 职责 | 示例 |\n|----|------|------|------|\n| **控制器层** | Controller | 算法逻辑 | JointTrajectoryController, DiffDriveController |\n| **接口层** | Command/State Interface | 统一数据交换 | position, velocity, effort |\n| **硬件层** | HardwareInterface | 真实硬件驱动 | CAN 通信, EtherCAT, GPIO |\n\n### 硬件接口类型\n\n| 类型 | 说明 | 适用 |\n|------|------|------|\n| **SystemInterface** | 多关节联动 | 机械臂（6轴共享一个EtherCAT总线）|\n| **ActuatorInterface** | 单关节 | 独立电机 |\n| **SensorInterface** | 只读传感器 | IMU、力传感器 |\n\n### 接口类型\n\n| 接口 | 方向 | 含义 |\n|------|------|------|\n| `position` command | 下行 | 期望位置 |\n| `velocity` command | 下行 | 期望速度 |\n| `effort` command | 下行 | 期望力矩 |\n| `position` state | 上行 | 实际位置（编码器）|\n| `velocity` state | 上行 | 实际速度 |\n| `effort` state | 上行 | 实际力矩（电流反馈）|\n\n### 编写自定义 HardwareInterface\n\n以一个简单的 CAN 总线电机为例：\n\n```cpp\n// my_robot_hardware.hpp\n#include <hardware_interface/system_interface.hpp>\n#include <hardware_interface/types/hardware_interface_type_values.hpp>\n\nclass MyRobotHardware : public hardware_interface::SystemInterface\n{\npublic:\n  // 初始化: 读取URDF参数, 打开CAN设备\n  CallbackReturn on_init(const hardware_interface::HardwareInfo & info) override\n  {\n    // 从 URDF 的 <ros2_control> 标签读取参数\n    // 例如: CAN 设备名、波特率、电机ID\n    can_device_ = info.hardware_parameters.at(\"can_device\");\n    n_joints_ = info.joints.size();\n    \n    hw_positions_.resize(n_joints_, 0.0);\n    hw_velocities_.resize(n_joints_, 0.0);\n    hw_commands_.resize(n_joints_, 0.0);\n    \n    return CallbackReturn::SUCCESS;\n  }\n  \n  // 导出状态接口 (上层可以读到什么)\n  std::vector<hardware_interface::StateInterface> export_state_interfaces() override\n  {\n    std::vector<hardware_interface::StateInterface> interfaces;\n    for (size_t i = 0; i < n_joints_; i++) {\n      interfaces.emplace_back(info_.joints[i].name, \"position\", &hw_positions_[i]);\n      interfaces.emplace_back(info_.joints[i].name, \"velocity\", &hw_velocities_[i]);\n    }\n    return interfaces;\n  }\n  \n  // 导出命令接口 (上层可以写什么)\n  std::vector<hardware_interface::CommandInterface> export_command_interfaces() override\n  {\n    std::vector<hardware_interface::CommandInterface> interfaces;\n    for (size_t i = 0; i < n_joints_; i++) {\n      interfaces.emplace_back(info_.joints[i].name, \"position\", &hw_commands_[i]);\n    }\n    return interfaces;\n  }\n  \n  // 读取硬件状态 (每个控制周期调用一次)\n  return_type read(const rclcpp::Time &, const rclcpp::Duration &) override\n  {\n    // 从 CAN 总线读取编码器数据\n    for (size_t i = 0; i < n_joints_; i++) {\n      // can_read(motor_ids_[i], &hw_positions_[i], &hw_velocities_[i]);\n      hw_positions_[i] = hw_commands_[i];  // 简化: 假设立即到位\n    }\n    return return_type::OK;\n  }\n  \n  // 写入硬件命令 (每个控制周期调用一次)\n  return_type write(const rclcpp::Time &, const rclcpp::Duration &) override\n  {\n    // 通过 CAN 总线发送位置指令\n    for (size_t i = 0; i < n_joints_; i++) {\n      // can_write(motor_ids_[i], hw_commands_[i]);\n    }\n    return return_type::OK;\n  }\n\nprivate:\n  std::string can_device_;\n  size_t n_joints_;\n  std::vector<double> hw_positions_, hw_velocities_, hw_commands_;\n};\n```\n\n### URDF 中的 ros2_control 配置\n\n```xml\n<ros2_control name=\"my_robot\" type=\"system\">\n  <hardware>\n    <plugin>my_robot_hardware/MyRobotHardware</plugin>\n    <param name=\"can_device\">can0</param>\n    <param name=\"baud_rate\">1000000</param>\n  </hardware>\n  \n  <joint name=\"joint1\">\n    <command_interface name=\"position\">\n      <param name=\"min\">-3.14</param>\n      <param name=\"max\">3.14</param>\n    </command_interface>\n    <state_interface name=\"position\"/>\n    <state_interface name=\"velocity\"/>\n  </joint>\n  \n  <joint name=\"joint2\">\n    <command_interface name=\"position\"/>\n    <state_interface name=\"position\"/>\n    <state_interface name=\"velocity\"/>\n  </joint>\n</ros2_control>\n```\n\n### 控制器配置 (YAML)\n\n```yaml\ncontroller_manager:\n  ros__parameters:\n    update_rate: 500  # 控制频率 500Hz\n    \n    joint_state_broadcaster:\n      type: joint_state_broadcaster/JointStateBroadcaster\n    \n    arm_controller:\n      type: joint_trajectory_controller/JointTrajectoryController\n\narm_controller:\n  ros__parameters:\n    joints:\n      - joint1\n      - joint2\n      - joint3\n      - joint4\n      - joint5\n      - joint6\n    command_interfaces:\n      - position\n    state_interfaces:\n      - position\n      - velocity\n    # PID 增益 (位置控制模式)\n    gains:\n      joint1: {p: 100.0, i: 0.0, d: 10.0}\n      joint2: {p: 100.0, i: 0.0, d: 10.0}\n```\n\n### 控制器类型速查\n\n| 控制器 | 用途 | 输入 |\n|--------|------|------|\n| `joint_trajectory_controller` | 机械臂轨迹跟踪 | JointTrajectory (MoveIt2) |\n| `diff_drive_controller` | 差速底盘 | Twist (Nav2) |\n| `forward_command_controller` | 直接转发命令 | Float64MultiArray |\n| `joint_state_broadcaster` | 发布关节状态 | 自动读取 |\n| `gripper_action_controller` | 夹爪控制 | GripperCommand |\n| `admittance_controller` | 阻抗/导纳控制 | Wrench + Trajectory |\n\n### 常见硬件对接方案\n\n| 电机品牌 | 通信协议 | ros2_control 驱动 |\n|---------|----------|------------------|\n| Dynamixel | USB/TTL | dynamixel_hardware |\n| 达妙 (DM) | CAN | dm_ros2_control |\n| Robstride (宇树) | CAN | robstride_ros2 |\n| ODrive | CAN/USB | odrive_ros2_control |\n| EtherCAT 伺服 | EtherCAT | ethercat_driver_ros2 |\n| Gazebo 仿真 | 内存共享 | gz_ros2_control |\n\n### 从仿真到真机的切换\n\nros2_control 最大的好处：**上层控制器代码零修改**，只换 HardwareInterface：\n\n```\n仿真:\n  URDF中: <plugin>gz_ros2_control/GazeboSystem</plugin>\n  → MoveIt2 + arm_controller → Gazebo 仿真电机\n\n真机:\n  URDF中: <plugin>my_robot_hardware/MyRobotHardware</plugin>\n  → MoveIt2 + arm_controller → 真实 CAN 电机\n  \n  唯一改的: URDF 里的 <plugin> 一行!\n```\n\n### 启动流程\n\n```bash\n# 1. 启动 controller_manager (加载硬件接口)\nros2 launch my_robot_bringup robot.launch.py\n\n# 2. 加载并激活控制器\nros2 control load_controller --set-state active joint_state_broadcaster\nros2 control load_controller --set-state active arm_controller\n\n# 3. 查看控制器状态\nros2 control list_controllers\n# 输出:\n# joint_state_broadcaster  [active]\n# arm_controller           [active]\n\n# 4. 查看硬件接口\nros2 control list_hardware_interfaces\n# 输出:\n# command interfaces:\n#   joint1/position [claimed]\n#   joint2/position [claimed]\n# state interfaces:\n#   joint1/position [available]\n#   joint1/velocity [available]\n```\n\n### 练习\n1. 为一个 3-DOF 机械臂编写完整的 ros2_control URDF 配置和控制器 YAML\n2. 用 Gazebo + gz_ros2_control 验证 JointTrajectoryController 的轨迹跟踪\n3. 实现一个自定义 HardwareInterface：模拟一个简单的 2-DOF 电机（加入延迟和噪声）\n\n### 参考答案\n\n**1. 3-DOF 机械臂 ros2_control 完整配置**\n\n```xml\n<?xml version=\\\"1.0\\\"?>\n<!-- 保存为 arm_ros2_control.urdf.xacro -->\n<robot name=\\\"arm_3dof\\\" xmlns:xacro=\\\"http://www.ros.org/wiki/xacro\\\">\n\n  <!-- ... link/joint 定义省略（见 URDF 章节）... -->\n\n  <!-- ros2_control 硬件配置 -->\n  <ros2_control name=\\\"arm_hardware\\\" type=\\\"system\\\">\n    <hardware>\n      <!-- 仿真用 Gazebo 插件；真机换成自定义插件 -->\n      <plugin>gz_ros2_control/GazeboSystem</plugin>\n    </hardware>\n    \n    <joint name=\\\"joint1\\\">\n      <command_interface name=\\\"position\\\">\n        <param name=\\\"min\\\">-3.14</param>\n        <param name=\\\"max\\\">3.14</param>\n      </command_interface>\n      <command_interface name=\\\"velocity\\\"/>\n      <state_interface name=\\\"position\\\"/>\n      <state_interface name=\\\"velocity\\\"/>\n      <state_interface name=\\\"effort\\\"/>\n    </joint>\n    \n    <joint name=\\\"joint2\\\">\n      <command_interface name=\\\"position\\\">\n        <param name=\\\"min\\\">-2.0</param>\n        <param name=\\\"max\\\">2.0</param>\n      </command_interface>\n      <state_interface name=\\\"position\\\"/>\n      <state_interface name=\\\"velocity\\\"/>\n    </joint>\n    \n    <joint name=\\\"joint3\\\">\n      <command_interface name=\\\"position\\\">\n        <param name=\\\"min\\\">-2.5</param>\n        <param name=\\\"max\\\">2.5</param>\n      </command_interface>\n      <state_interface name=\\\"position\\\"/>\n      <state_interface name=\\\"velocity\\\"/>\n    </joint>\n  </ros2_control>\n</robot>\n```\n\n```yaml\n# controllers.yaml\ncontroller_manager:\n  ros__parameters:\n    update_rate: 500\n    joint_state_broadcaster:\n      type: joint_state_broadcaster/JointStateBroadcaster\n    arm_controller:\n      type: joint_trajectory_controller/JointTrajectoryController\n\narm_controller:\n  ros__parameters:\n    joints: [joint1, joint2, joint3]\n    command_interfaces: [position]\n    state_interfaces: [position, velocity]\n    state_publish_rate: 50.0\n    action_monitor_rate: 20.0\n    allow_partial_joints_goal: false\n    constraints:\n      stopped_velocity_tolerance: 0.01\n      goal_time: 0.0\n```\n\n**2. Gazebo 轨迹跟踪验证**\n\n```python\nimport rclpy\nfrom rclpy.node import Node\nfrom rclpy.action import ActionClient\nfrom control_msgs.action import FollowJointTrajectory\nfrom trajectory_msgs.msg import JointTrajectoryPoint\nfrom builtin_interfaces.msg import Duration\nimport math\n\nclass TrajectoryTest(Node):\n    def __init__(self):\n        super().__init__('trajectory_test')\n        self.client = ActionClient(\n            self, FollowJointTrajectory,\n            '/arm_controller/follow_joint_trajectory')\n        self.client.wait_for_server()\n        self.get_logger().info('控制器连接成功')\n    \n    def send_trajectory(self):\n        goal = FollowJointTrajectory.Goal()\n        goal.trajectory.joint_names = ['joint1', 'joint2', 'joint3']\n        \n        # 生成正弦波轨迹点\n        for i in range(50):\n            t = i * 0.1\n            point = JointTrajectoryPoint()\n            point.positions = [\n                0.5 * math.sin(t),\n                0.3 * math.sin(1.5 * t),\n                0.4 * math.sin(0.8 * t),\n            ]\n            point.velocities = [\n                0.5 * math.cos(t),\n                0.45 * math.cos(1.5 * t),\n                0.32 * math.cos(0.8 * t),\n            ]\n            point.time_from_start = Duration(sec=int(t), nanosec=int((t%1)*1e9))\n            goal.trajectory.points.append(point)\n        \n        future = self.client.send_goal_async(goal)\n        self.get_logger().info(f'发送 {len(goal.trajectory.points)} 个轨迹点')\n        return future\n\n# 启动: ros2 launch arm_bringup gazebo.launch.py\n# 然后: ros2 run arm_test trajectory_test\nprint('Gazebo + ros2_control 轨迹验证步骤:')\nprint('1. ros2 launch: 启动 Gazebo + controller_manager')\nprint('2. 激活控制器: joint_state_broadcaster + arm_controller')\nprint('3. 发送轨迹: FollowJointTrajectory action')\nprint('4. RViz 可视化: 对比期望 vs 实际关节角度')\n```\n\n**3. 自定义 HardwareInterface（模拟带延迟和噪声的电机）**\n\n```cpp\n// simulated_motor_hardware.cpp\n// 模拟真实电机的非理想特性: 延迟 + 噪声 + 速度限制\n\n#include <hardware_interface/system_interface.hpp>\n#include <random>\n#include <deque>\n\nclass SimulatedMotorHardware : public hardware_interface::SystemInterface\n{\npublic:\n  CallbackReturn on_init(const hardware_interface::HardwareInfo & info) override\n  {\n    n_joints_ = info.joints.size();\n    positions_.resize(n_joints_, 0.0);\n    velocities_.resize(n_joints_, 0.0);\n    commands_.resize(n_joints_, 0.0);\n    \n    // 非理想特性参数\n    delay_ms_ = std::stod(info.hardware_parameters.at(\"delay_ms\"));  // 通信延迟\n    noise_std_ = std::stod(info.hardware_parameters.at(\"noise_std\"));  // 编码器噪声\n    max_velocity_ = std::stod(info.hardware_parameters.at(\"max_velocity\"));  // 最大速度\n    \n    // 延迟缓冲区\n    delay_steps_ = static_cast<int>(delay_ms_ / 2.0);  // 假设 500Hz → 2ms/step\n    for (size_t i = 0; i < n_joints_; i++) {\n      command_buffer_.push_back(std::deque<double>(delay_steps_, 0.0));\n    }\n    \n    gen_ = std::mt19937(42);\n    noise_dist_ = std::normal_distribution<double>(0.0, noise_std_);\n    \n    return CallbackReturn::SUCCESS;\n  }\n  \n  return_type read(const rclcpp::Time &, const rclcpp::Duration & period) override\n  {\n    double dt = period.seconds();\n    for (size_t i = 0; i < n_joints_; i++) {\n      // 从延迟缓冲区取出命令\n      double delayed_cmd = command_buffer_[i].front();\n      command_buffer_[i].pop_front();\n      command_buffer_[i].push_back(commands_[i]);\n      \n      // 速度限制\n      double error = delayed_cmd - positions_[i];\n      double velocity = std::clamp(error / dt, -max_velocity_, max_velocity_);\n      \n      // 更新位置\n      positions_[i] += velocity * dt;\n      velocities_[i] = velocity;\n      \n      // 加编码器噪声\n      positions_[i] += noise_dist_(gen_);\n    }\n    return return_type::OK;\n  }\n  \n  // ... export_state/command_interfaces, write 同前 ...\n\nprivate:\n  size_t n_joints_;\n  double delay_ms_, noise_std_, max_velocity_;\n  int delay_steps_;\n  std::vector<double> positions_, velocities_, commands_;\n  std::vector<std::deque<double>> command_buffer_;\n  std::mt19937 gen_;\n  std::normal_distribution<double> noise_dist_;\n};\n\n// URDF 配置:\n// <plugin>simulated_motor_hardware/SimulatedMotorHardware</plugin>\n// <param name=\\\"delay_ms\\\">10</param>      <!-- 10ms 通信延迟 -->\n// <param name=\\\"noise_std\\\">0.001</param>  <!-- 1mrad 编码器噪声 -->\n// <param name=\\\"max_velocity\\\">3.14</param> <!-- π rad/s 最大速度 -->\n```\n\n```python\n# Python 等效实现 (用于快速测试)\nimport numpy as np\nfrom collections import deque\n\nclass SimulatedMotor:\n    def __init__(self, delay_ms=10, noise_std=0.001, max_vel=3.14, dt=0.002):\n        self.delay_steps = int(delay_ms / (dt * 1000))\n        self.noise_std = noise_std\n        self.max_vel = max_vel\n        self.dt = dt\n        self.position = 0.0\n        self.velocity = 0.0\n        self.cmd_buffer = deque([0.0] * self.delay_steps, maxlen=self.delay_steps)\n    \n    def command(self, target):\n        self.cmd_buffer.append(target)\n    \n    def step(self):\n        delayed_cmd = self.cmd_buffer[0]\n        error = delayed_cmd - self.position\n        self.velocity = np.clip(error / self.dt, -self.max_vel, self.max_vel)\n        self.position += self.velocity * self.dt\n        return self.position + np.random.randn() * self.noise_std\n\n# 测试\nmotor = SimulatedMotor(delay_ms=10, noise_std=0.001)\nfor t in np.arange(0, 2, 0.002):\n    motor.command(np.sin(t))\n    measured = motor.step()\n\nprint(f'模拟电机: 延迟={motor.delay_steps}步, 噪声={motor.noise_std}rad')\nprint(f'最终位置: 期望={np.sin(2):.4f}, 实际={motor.position:.4f}')\nprint(f'延迟导致的跟踪误差是 ros2_control 调参的主要挑战')\n```\n"
}