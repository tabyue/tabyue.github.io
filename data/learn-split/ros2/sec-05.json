{
  "title": "ROS2 Launch 系统与参数管理",
  "difficulty": "入门",
  "content": "## ROS2 Launch 系统与参数管理\n\n一个机器人系统需要同时启动十几个节点——手动一个个启动不现实。Launch 系统是 ROS2 的\"编排器\"。\n\n### 1. Launch 文件基础\n\n```python\n# launch/robot_bringup.launch.py\nfrom launch import LaunchDescription\nfrom launch_ros.actions import Node\nfrom launch.actions import DeclareLaunchArgument, IncludeLaunchDescription\nfrom launch.substitutions import LaunchConfiguration\n\ndef generate_launch_description():\n    # Declare arguments\n    use_sim = DeclareLaunchArgument('use_sim', default_value='false')\n    \n    # Nodes\n    robot_state_pub = Node(\n        package='robot_state_publisher',\n        executable='robot_state_publisher',\n        parameters=[{'robot_description': robot_description}]\n    )\n    \n    controller = Node(\n        package='my_robot_control',\n        executable='controller_node',\n        parameters=['config/controller.yaml'],\n        remappings=[('/cmd_vel', '/robot/cmd_vel')]\n    )\n    \n    camera = Node(\n        package='realsense2_camera',\n        executable='realsense2_camera_node',\n        parameters=['config/camera.yaml'],\n        condition=UnlessCondition(LaunchConfiguration('use_sim'))\n    )\n    \n    return LaunchDescription([\n        use_sim,\n        robot_state_pub,\n        controller,\n        camera,\n    ])\n```\n\n### 2. 参数管理\n\n**YAML 参数文件**：\n```yaml\n# config/controller.yaml\ncontroller_node:\n  ros__parameters:\n    control_frequency: 500.0\n    joint_names: [joint_1, joint_2, joint_3, joint_4, joint_5, joint_6]\n    pid:\n      joint_1: {p: 100.0, i: 0.1, d: 10.0}\n      joint_2: {p: 150.0, i: 0.2, d: 15.0}\n    safety:\n      max_velocity: 3.14\n      max_effort: 50.0\n```\n\n**动态参数修改**：\n```bash\n# Command line\nros2 param set /controller_node control_frequency 200.0\nros2 param get /controller_node pid.joint_1.p\n\n# From code\nself.declare_parameter('control_frequency', 500.0)\nfreq = self.get_parameter('control_frequency').value\n\n# Parameter callback\nself.add_on_set_parameters_callback(self.param_callback)\n```\n\n### 3. Composable Nodes（零拷贝节点组合）\n\n将多个节点运行在同一个进程中——避免序列化/反序列化开销。\n\n```python\nfrom launch_ros.actions import ComposableNodeContainer\nfrom launch_ros.descriptions import ComposableNode\n\ncontainer = ComposableNodeContainer(\n    name='perception_container',\n    namespace='',\n    package='rclcpp_components',\n    executable='component_container',\n    composable_node_descriptions=[\n        ComposableNode(\n            package='image_proc',\n            plugin='image_proc::RectifyNode',\n            name='rectify',\n        ),\n        ComposableNode(\n            package='depth_image_proc',\n            plugin='depth_image_proc::PointCloudXyzrgbNode',\n            name='pointcloud',\n        ),\n    ],\n)\n```\n\n### 4. 命名空间与重映射\n\n**命名空间**：隔离多机器人或多实例。\n```bash\nros2 run my_pkg my_node --ros-args -r __ns:=/robot1\n# 话题变为 /robot1/cmd_vel, /robot1/joint_states 等\n```\n\n**重映射**：改变话题/服务名称。\n```python\nNode(\n    package='my_pkg',\n    executable='my_node',\n    remappings=[\n        ('/input/image', '/camera/color/image_raw'),\n        ('/output/cmd', '/robot/cmd_vel'),\n    ]\n)\n```\n\n### 5. Launch 事件与条件\n\n```python\nfrom launch.actions import RegisterEventHandler, TimerAction\nfrom launch.event_handlers import OnProcessStart, OnProcessExit\n\n# Wait for one node before starting another\nwait_for_controller = RegisterEventHandler(\n    OnProcessStart(\n        target_action=controller,\n        on_start=[TimerAction(period=2.0, actions=[planner])]\n    )\n)\n\n# Restart on crash\nauto_restart = RegisterEventHandler(\n    OnProcessExit(\n        target_action=critical_node,\n        on_exit=[critical_node]  # Restart same node\n    )\n)\n```\n\n### 6. 最佳实践\n\n| 实践 | 说明 |\n|------|------|\n| 分层 Launch | bringup.launch → hardware.launch + perception.launch + control.launch |\n| 参数分离 | 配置文件独立于代码，便于不同机器人/环境切换 |\n| 条件启动 | `use_sim` 参数控制仿真/真机模式 |\n| 命名空间 | 多机器人用 `/robot1/` `/robot2/` 隔离 |\n| Composable | 同进程节点间零拷贝通信 |\n| 健康检查 | 关键节点崩溃时自动重启 |\n\n### 练习题\n\n1. **Launch 文件**：为你的机器人写一个完整的 bringup launch（包含驱动+控制+感知+可视化）。\n2. **参数文件**：将所有可调参数提取到 YAML，支持 `ros2 param set` 在线修改。\n3. **多机器人**：用命名空间在一个 Launch 中启动两台虚拟机器人，各自独立运行。\n4. **Composable**：将感知链路（rectify + pointcloud）放入同一个 container，测量延迟改善。\n5. **条件 Launch**：实现一个 Launch 文件，根据 `use_sim` 参数自动切换 Gazebo 仿真或真实硬件驱动。",
  "lastUpdated": "2026-04-30T10:32"
}