{
  "title": "TF2 坐标变换",
  "content": "### TF2：ROS2 的坐标系管理系统\n\n在机器人系统中，不同组件有各自的坐标系：相机坐标系、激光雷达坐标系、机械臂基座坐标系、世界坐标系……\n\n**TF2** 是 ROS2 中管理这些坐标系之间变换关系的系统。\n\n### 为什么需要坐标变换？\n\n```\n相机看到杯子在图像坐标 (u=320, v=240)\n  ↓ 相机内参\n杯子在相机坐标系 (0.3, 0.0, 0.5)m\n  ↓ 手眼标定变换\n杯子在机械臂基座坐标系 (0.6, -0.2, 0.4)m\n  ↓ 逆运动学\n关节角度 [30°, -45°, 60°, ...]\n```\n\n每一步坐标转换都需要知道两个坐标系之间的**旋转+平移**关系。TF2 维护一棵坐标系树，可以查询任意两个坐标系之间的变换。\n\n### TF2 坐标系树\n\n```\n            world\n           ╱     ╲\n        odom    map\n         │\n      base_link\n      ╱    │    ╲\n  camera  lidar  arm_base\n                    │\n                arm_link1\n                    │\n                arm_link2\n                    │\n                  gripper\n```\n\n- **静态变换**：相机和基座之间的固定关系（标定后不变）\n- **动态变换**：关节角度变化时，各连杆之间的变换实时更新\n\n```\nmap → odom → base_link → camera_link\n                       → arm_base → link1 → link2 → ... → gripper\n```\n\n- map → odom：SLAM 提供（全局定位）\n- odom → base_link：里程计提供（相对运动）\n- base_link → 各传感器：静态变换（URDF 定义）\n\n\n\n> **🔧 环境准备** — 运行此代码需要安装以下依赖：\n>\n> ```bash\n> # 需要安装 ROS2 Humble/Iron\n# 参考: https://docs.ros.org/en/humble/Installation.html\n> ```\n\n```python\nimport rclpy\nfrom rclpy.node import Node\nimport tf2_ros\nfrom geometry_msgs.msg import TransformStamped\nimport numpy as np\n\nclass TFExample(Node):\n    def __init__(self):\n        super().__init__('tf_example')\n        \n        # 广播静态变换（相机相对于机器人base）\n        self.static_br = tf2_ros.StaticTransformBroadcaster(self)\n        t = TransformStamped()\n        t.header.frame_id = 'base_link'\n        t.child_frame_id = 'camera_link'\n        t.transform.translation.x = 0.3\n        t.transform.translation.z = 0.5\n        t.transform.rotation.w = 1.0\n        self.static_br.sendTransform(t)\n        \n        # 监听变换\n        self.tf_buffer = tf2_ros.Buffer()\n        self.tf_listener = tf2_ros.TransformListener(self.tf_buffer, self)\n    \n    def lookup_transform(self):\n        try:\n            t = self.tf_buffer.lookup_transform(\n                'base_link', 'camera_link', rclpy.time.Time())\n            pos = t.transform.translation\n            self.get_logger().info(f'Camera在base中: ({pos.x:.2f}, {pos.y:.2f}, {pos.z:.2f})')\n        except tf2_ros.TransformException as e:\n            self.get_logger().warn(f'变换查询失败: {e}')\n```\n\n### 调试工具\n\n```bash\n# 查看变换树\nros2 run tf2_tools view_frames\n# 查询两个坐标系之间的变换\nros2 run tf2_ros tf2_echo base_link camera_link\n```\n\n### 练习\n1. 广播一个机械臂的关节变换链（6 个 link），在 RViz 中可视化\n2. 实现坐标变换：把相机坐标系下的检测框转换到世界坐标系\n3. 用 TF2 实现手眼标定（eye-in-hand）的坐标变换链\n\n### 参考答案\n\n**1. 广播一个机械臂的关节变换链（6 个 link），在 RViz 中可视化**\n\n```python\nimport rclpy\nfrom rclpy.node import Node\nimport tf2_ros\nfrom geometry_msgs.msg import TransformStamped\nfrom sensor_msgs.msg import JointState\nimport numpy as np\nimport math\n\nclass ArmTFBroadcaster(Node):\n    '''6-DOF机械臂TF广播: 根据关节角度发布完整变换链\n    \n    坐标系树: base_link → link1 → link2 → ... → link6 → gripper\n    '''\n    def __init__(self):\n        super().__init__('arm_tf_broadcaster')\n        self.br = tf2_ros.TransformBroadcaster(self)\n        \n        # DH参数 (简化6-DOF臂)\n        # [a, d, alpha] for each joint (theta来自关节角)\n        self.dh_params = [\n            (0, 0.333, -math.pi/2),   # J1\n            (0, 0, math.pi/2),         # J2\n            (0, 0.316, math.pi/2),     # J3\n            (0, 0, -math.pi/2),        # J4\n            (0, 0.384, -math.pi/2),    # J5\n            (0, 0, math.pi/2),         # J6\n        ]\n        \n        # 订阅关节状态\n        self.sub = self.create_subscription(\n            JointState, '/joint_states', self.on_joints, 10)\n        \n        # 也发布演示用的正弦波关节轨迹\n        self.timer = self.create_timer(0.02, self.publish_demo)\n        self.t = 0.0\n        self.get_logger().info('6-DOF 机械臂 TF 广播器就绪')\n    \n    def publish_demo(self):\n        '''发布演示关节角度'''\n        joints = [\n            0.5*math.sin(0.5*self.t),\n            -0.3 + 0.3*math.sin(0.3*self.t),\n            0.4*math.sin(0.4*self.t),\n            0.6*math.sin(0.6*self.t),\n            0.3*math.sin(0.8*self.t),\n            0.2*math.sin(1.0*self.t),\n        ]\n        self.broadcast_arm_tf(joints)\n        self.t += 0.02\n    \n    def on_joints(self, msg):\n        if len(msg.position) >= 6:\n            self.broadcast_arm_tf(list(msg.position[:6]))\n    \n    def broadcast_arm_tf(self, joint_angles):\n        now = self.get_clock().now().to_msg()\n        transforms = []\n        \n        for i, (a, d, alpha) in enumerate(self.dh_params):\n            t = TransformStamped()\n            t.header.stamp = now\n            t.header.frame_id = f'link{i}' if i > 0 else 'base_link'\n            t.child_frame_id = f'link{i+1}'\n            \n            theta = joint_angles[i]\n            # DH变换: Rz(theta) * Tz(d) * Tx(a) * Rx(alpha)\n            t.transform.translation.x = a * math.cos(theta)\n            t.transform.translation.y = a * math.sin(theta)\n            t.transform.translation.z = d\n            \n            # 四元数 (简化: 绕Z轴旋转theta + 绕X轴旋转alpha)\n            # q = qz(theta) * qx(alpha)\n            ct, st = math.cos(theta/2), math.sin(theta/2)\n            ca, sa = math.cos(alpha/2), math.sin(alpha/2)\n            t.transform.rotation.w = ct*ca\n            t.transform.rotation.x = ct*sa\n            t.transform.rotation.y = st*sa\n            t.transform.rotation.z = st*ca\n            \n            transforms.append(t)\n        \n        # 末端执行器\n        t_ee = TransformStamped()\n        t_ee.header.stamp = now\n        t_ee.header.frame_id = 'link6'\n        t_ee.child_frame_id = 'gripper'\n        t_ee.transform.translation.z = 0.1\n        t_ee.transform.rotation.w = 1.0\n        transforms.append(t_ee)\n        \n        for tf in transforms:\n            self.br.sendTransform(tf)\n\n# 可视化: ros2 run rviz2 rviz2 → Add TF display\n# 或: ros2 run tf2_tools view_frames → 生成坐标系树PDF\n```\n\n**2. 实现坐标变换：把相机坐标系下的检测框转换到世界坐标系**\n\n```python\nimport rclpy\nfrom rclpy.node import Node\nimport tf2_ros\nimport tf2_geometry_msgs\nfrom geometry_msgs.msg import PointStamped, TransformStamped\nimport numpy as np\n\nclass CameraToWorldTransformer(Node):\n    '''将相机坐标系下的3D检测结果转换到世界坐标系\n    \n    流程: 像素(u,v) + 深度d → 相机坐标 → base_link坐标 → world坐标\n    '''\n    def __init__(self):\n        super().__init__('cam_to_world')\n        self.tf_buffer = tf2_ros.Buffer()\n        self.tf_listener = tf2_ros.TransformListener(self.tf_buffer, self)\n        \n        # 相机内参 (RealSense D435 典型值)\n        self.fx, self.fy = 615.0, 615.0\n        self.cx, self.cy = 320.0, 240.0\n    \n    def pixel_to_camera(self, u, v, depth_m):\n        '''像素坐标 + 深度 → 相机坐标系3D点'''\n        x = (u - self.cx) * depth_m / self.fx\n        y = (v - self.cy) * depth_m / self.fy\n        z = depth_m\n        return x, y, z\n    \n    def camera_to_world(self, x_cam, y_cam, z_cam):\n        '''相机坐标 → 世界坐标 (通过TF2)'''\n        point_cam = PointStamped()\n        point_cam.header.frame_id = 'camera_color_optical_frame'\n        point_cam.header.stamp = self.get_clock().now().to_msg()\n        point_cam.point.x = x_cam\n        point_cam.point.y = y_cam\n        point_cam.point.z = z_cam\n        \n        try:\n            # TF2 自动查找 camera → base_link → world 的完整变换链\n            point_world = self.tf_buffer.transform(\n                point_cam, 'world', timeout=rclpy.duration.Duration(seconds=1.0))\n            return (point_world.point.x, point_world.point.y, point_world.point.z)\n        except (tf2_ros.LookupException, tf2_ros.ExtrapolationException) as e:\n            self.get_logger().warn(f'TF变换失败: {e}')\n            return None\n    \n    def process_detection(self, bbox_center_uv, depth_m, label):\n        '''完整流程: 检测框 → 世界坐标'''\n        u, v = bbox_center_uv\n        \n        # Step 1: 像素 → 相机坐标\n        x_c, y_c, z_c = self.pixel_to_camera(u, v, depth_m)\n        self.get_logger().info(f'{label}: 相机坐标 ({x_c:.3f}, {y_c:.3f}, {z_c:.3f})')\n        \n        # Step 2: 相机 → 世界坐标 (通过TF2)\n        world_pos = self.camera_to_world(x_c, y_c, z_c)\n        if world_pos:\n            self.get_logger().info(f'{label}: 世界坐标 ({world_pos[0]:.3f}, {world_pos[1]:.3f}, {world_pos[2]:.3f})')\n        return world_pos\n\n# 使用示例:\n# node.process_detection((320, 240), 0.8, 'red_cup')\n```\n\n**3. 用 TF2 实现手眼标定（eye-in-hand）的坐标变换链**\n\n```python\nimport numpy as np\nfrom scipy.spatial.transform import Rotation as R\n\ndef eye_in_hand_calibration(T_base_to_ee_list, T_cam_to_target_list):\n    '''手眼标定 (Eye-in-Hand): AX = XB 问题\n    \n    相机安装在机械臂末端 (eye-in-hand)\n    A: 两次末端位姿之间的变换 (已知, 从运动学)\n    B: 两次标定板在相机中的变换 (已知, 从视觉检测)\n    X: 末端到相机的固定变换 (求解目标)\n    \n    使用 Tsai-Lenz 方法\n    '''\n    n = len(T_base_to_ee_list)\n    assert n >= 3, '至少需要3组数据'\n    \n    # 计算相邻位姿之间的变换\n    A_list, B_list = [], []\n    for i in range(n - 1):\n        # A = T_ee(i+1)^{-1} @ T_ee(i)\n        A = np.linalg.inv(T_base_to_ee_list[i+1]) @ T_base_to_ee_list[i]\n        # B = T_cam_target(i+1) @ T_cam_target(i)^{-1}\n        B = T_cam_to_target_list[i+1] @ np.linalg.inv(T_cam_to_target_list[i])\n        A_list.append(A)\n        B_list.append(B)\n    \n    # Tsai-Lenz: 先求旋转R，再求平移t\n    # 从旋转轴方程: (I - R_A) @ r_X = R_X @ r_B - r_A\n    # 其中 r 是旋转轴(Rodriguez向量)\n    \n    M = np.zeros((3*(n-1), 3))\n    rhs = np.zeros(3*(n-1))\n    \n    for i, (A, B) in enumerate(zip(A_list, B_list)):\n        Ra, Rb = A[:3,:3], B[:3,:3]\n        # Rodriguez 表示\n        ra = R.from_matrix(Ra).as_rotvec()\n        rb = R.from_matrix(Rb).as_rotvec()\n        \n        # 构建线性方程\n        skew_sum = _skew(ra + rb)\n        M[3*i:3*i+3] = skew_sum\n        rhs[3*i:3*i+3] = rb - ra\n    \n    # 最小二乘求解\n    Pcg, _, _, _ = np.linalg.lstsq(M, rhs, rcond=None)\n    \n    # 从 Pcg 恢复旋转矩阵\n    Pcg_norm = np.linalg.norm(Pcg)\n    Rcg = R.from_rotvec(2 * np.arctan(Pcg_norm) * Pcg / Pcg_norm).as_matrix()\n    \n    # 求平移\n    C = np.zeros((3*(n-1), 3))\n    d = np.zeros(3*(n-1))\n    for i, (A, B) in enumerate(zip(A_list, B_list)):\n        C[3*i:3*i+3] = A[:3,:3] - np.eye(3)\n        d[3*i:3*i+3] = Rcg @ B[:3,3] - A[:3,3]\n    \n    tcg, _, _, _ = np.linalg.lstsq(C, d, rcond=None)\n    \n    T_ee_to_cam = np.eye(4)\n    T_ee_to_cam[:3,:3] = Rcg\n    T_ee_to_cam[:3,3] = tcg\n    \n    return T_ee_to_cam\n\ndef _skew(v):\n    return np.array([[0, -v[2], v[1]], [v[2], 0, -v[0]], [-v[1], v[0], 0]])\n\n# === 测试 ===\nnp.random.seed(42)\n\n# 真实的手眼变换 (待求)\nT_true = np.eye(4)\nT_true[:3,:3] = R.from_euler('xyz', [10, -5, 3], degrees=True).as_matrix()\nT_true[:3,3] = [0.05, 0.02, 0.08]  # 相机偏移\n\n# 模拟5组标定数据\nT_base_ee, T_cam_target = [], []\nfor _ in range(5):\n    T_ee = np.eye(4)\n    T_ee[:3,:3] = R.random().as_matrix()\n    T_ee[:3,3] = np.random.uniform(-0.5, 0.5, 3)\n    \n    T_target = np.eye(4)  # 标定板在世界坐标系中固定\n    T_target[:3,3] = [0.5, 0, 0.3]\n    \n    T_cam = np.linalg.inv(T_true) @ np.linalg.inv(T_ee) @ T_target\n    \n    T_base_ee.append(T_ee)\n    T_cam_target.append(T_cam)\n\nT_solved = eye_in_hand_calibration(T_base_ee, T_cam_target)\nprint('=== 手眼标定结果 ===')\nprint(f'真实平移: {T_true[:3,3]}')\nprint(f'求解平移: {T_solved[:3,3]}')\nprint(f'平移误差: {np.linalg.norm(T_true[:3,3]-T_solved[:3,3])*1000:.2f} mm')\n```\n"
}