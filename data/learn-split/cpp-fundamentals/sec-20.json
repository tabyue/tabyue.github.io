{
  "title": "C++ 错误处理策略：异常 vs 错误码 vs Expected / std::optional 在机器人系统中的选择",
  "difficulty": "intermediate",
  "lastUpdated": "2026-05-29T10:45",
  "content": "# C++ 错误处理策略：异常 vs 错误码 vs Expected\n\n## 一、机器人系统中的错误处理为什么特殊？\n\n机器人系统对错误处理的要求比普通应用更严格：\n\n1. **实时性约束**：控制循环 1kHz 运行，异常展开可能导致 deadline miss\n2. **安全性要求**：错误必须被处理，忽略的错误 = 机器人失控\n3. **确定性要求**：错误路径的执行时间必须可预测（不能突然 GC / 堆分配）\n4. **恢复策略多样**：有些错误要重试、有些要降级、有些要急停\n\n### 1.1 三种主流策略对比\n\n| 策略 | 优点 | 缺点 | 适用场景 |\n|------|------|------|---------|\n| **异常 (exception)** | 语法简洁、自动传播 | 不可预测开销、可能 miss deadline | 初始化 / 配置阶段 |\n| **错误码 (return code)** | 确定性好、零开销 | 容易被忽略、嵌套地狱 | 实时控制循环 |\n| **std::expected / optional** | 类型安全、强制处理 | C++23 才标准化 | 现代 C++ 项目 |\n\n## 二、异常（Exception）\n\n### 2.1 什么时候用异常？\n\n```cpp\n// ✅ 适合：初始化阶段（非实时路径）\nclass FrankaInterface {\npublic:\n    FrankaInterface(const std::string& ip) {\n        robot_ = std::make_unique<franka::Robot>(ip);\n        if (!robot_->serverVersion().isCompatible()) {\n            throw std::runtime_error(\n                \"Franka firmware version incompatible! \"\n                \"Expected >= 5.7.2, got \" + robot_->serverVersion().toString()\n            );\n        }\n    }\n};\n\n// 调用方\ntry {\n    FrankaInterface robot(\"192.168.1.100\");\n} catch (const franka::NetworkException& e) {\n    std::cerr << \"网络连接失败: \" << e.what() << \"\\n\";\n    // 重试 / 报警 / 退出\n} catch (const std::runtime_error& e) {\n    std::cerr << \"初始化失败: \" << e.what() << \"\\n\";\n}\n```\n\n### 2.2 什么时候不用异常？\n\n```cpp\n// ❌ 不适合：1kHz 控制循环\nvoid control_loop() {\n    while (running) {\n        auto start = high_resolution_clock::now();\n        \n        // 这里如果 throw 异常：\n        // 1. 堆上构造 exception 对象 → 不确定性分配\n        // 2. 栈展开 → 执行时间不可预测\n        // 3. 可能 miss 1ms deadline\n        auto state = robot.readState();  // 不能 throw！\n        auto torques = controller.compute(state);\n        robot.sendTorques(torques);\n        \n        auto end = high_resolution_clock::now();\n        // 保证循环时间 <= 1ms\n    }\n}\n```\n\n### 2.3 ROS2 的异常策略\n\nROS2 官方建议：\n- **节点初始化**：可以 throw（LifecycleNode `on_configure` 返回 FAILURE 就行）\n- **实时回调**：不 throw，用错误码 + 日志\n- **rclcpp 内部**：不抛异常到用户代码\n\n## 三、错误码（Return Code）\n\n### 3.1 经典 C 风格\n\n```cpp\nenum class RobotError {\n    OK = 0,\n    TIMEOUT,\n    COLLISION,\n    JOINT_LIMIT,\n    COMMUNICATION_LOST,\n    EMERGENCY_STOP\n};\n\nRobotError send_torques(const std::array<double, 7>& torques) {\n    if (!is_connected_) return RobotError::COMMUNICATION_LOST;\n    if (is_estopped_) return RobotError::EMERGENCY_STOP;\n    \n    for (int i = 0; i < 7; i++) {\n        if (std::abs(torques[i]) > torque_limits_[i]) {\n            return RobotError::JOINT_LIMIT;\n        }\n    }\n    \n    if (!hw_interface_.write(torques)) {\n        return RobotError::TIMEOUT;\n    }\n    return RobotError::OK;\n}\n\n// 调用方——必须检查！\nauto err = robot.send_torques(cmd);\nif (err != RobotError::OK) {\n    switch (err) {\n        case RobotError::COLLISION:\n            emergency_stop();\n            break;\n        case RobotError::TIMEOUT:\n            retry(cmd);\n            break;\n        default:\n            log_error(err);\n    }\n}\n```\n\n### 3.2 问题：错误码容易被忽略\n\n```cpp\n// ❌ 危险：忽略了返回值\nrobot.send_torques(cmd);  // 编译器不报错！但如果失败了呢？\n\n// ✅ C++17 [[nodiscard]] 属性\n[[nodiscard]] RobotError send_torques(const std::array<double, 7>& torques);\n// 现在忽略返回值 → 编译器 warning\n```\n\n## 四、std::expected / std::optional（现代 C++ 推荐）\n\n### 4.1 std::optional：可能没有值\n\n```cpp\n#include <optional>\n\n// 返回一个可能不存在的值\nstd::optional<Pose> detect_object(const Image& img) {\n    auto detections = yolo.detect(img);\n    if (detections.empty()) {\n        return std::nullopt;  // 没检测到目标\n    }\n    return detections[0].pose;\n}\n\n// 调用方——强制检查\nauto target = detect_object(camera.capture());\nif (target.has_value()) {\n    move_to(target.value());\n} else {\n    // 没检测到目标，继续搜索\n    search_pattern();\n}\n\n// 或者用 value_or 提供默认值\nPose safe_target = detect_object(img).value_or(home_pose);\n```\n\n### 4.2 std::expected (C++23)：值或错误\n\n```cpp\n#include <expected>  // C++23\n\n// 返回成功值 OR 错误信息\nstd::expected<JointState, RobotError> read_state() {\n    if (!is_connected_) {\n        return std::unexpected(RobotError::COMMUNICATION_LOST);\n    }\n    auto data = hw_interface_.read();\n    if (!data.valid) {\n        return std::unexpected(RobotError::TIMEOUT);\n    }\n    return JointState{data.positions, data.velocities, data.torques};\n}\n\n// 调用方\nauto result = robot.read_state();\nif (result.has_value()) {\n    auto& state = result.value();\n    controller.update(state);\n} else {\n    handle_error(result.error());\n}\n\n// 链式调用（类似 Rust 的 ? 运算符）\nauto plan_result = read_state()\n    .and_then([](auto& state) { return compute_action(state); })\n    .and_then([](auto& action) { return validate_action(action); });\n```\n\n### 4.3 C++23 之前的替代：tl::expected\n\n```cpp\n// 使用 tl::expected（header-only 库）\n// https://github.com/TartanLlama/expected\n#include \"tl/expected.hpp\"\n\ntl::expected<JointState, RobotError> read_state();\n```\n\n## 五、机器人项目的最佳实践\n\n### 5.1 分层错误处理策略\n\n```\n[实时层 1kHz]\n  → 错误码 / std::expected（零开销、确定性）\n  → 关键错误 → 急停信号（原子变量）\n  \n[非实时层 10-100Hz]\n  → std::expected / optional（类型安全）\n  → 可以用 RAII 做资源清理\n  \n[初始化/配置层（一次性）]\n  → 异常 OK（简洁、自动传播）\n  → try-catch 在最外层\n```\n\n### 5.2 错误恢复策略矩阵\n\n| 错误类型 | 严重度 | 恢复策略 | 代码处理 |\n|---------|--------|---------|---------|\n| 通信超时 | 低 | 重试 3 次 | 错误码 + retry loop |\n| 关节超限 | 中 | 钳制到限位 | 错误码 + clamp |\n| 碰撞检测 | 高 | 立即停止 + 回退 | 错误码 + 急停 |\n| 通信丢失 | 严重 | 急停 + 报警 | atomic flag |\n| 硬件故障 | 致命 | 断电 + 人工介入 | 异常（反正已不实时） |\n\n### 5.3 完整示例：实时控制循环的错误处理\n\n```cpp\nclass RealtimeController {\npublic:\n    void run() {\n        while (running_.load()) {\n            auto loop_start = Clock::now();\n            \n            // Step 1: 读状态（可能失败）\n            auto state_result = robot_.read_state();\n            if (!state_result) {\n                handle_read_error(state_result.error());\n                continue;  // 跳过本帧\n            }\n            \n            // Step 2: 计算控制（不应该失败）\n            auto cmd = controller_.compute(state_result.value());\n            \n            // Step 3: 发送命令（可能失败）\n            auto send_result = robot_.send_torques(cmd);\n            if (!send_result) {\n                handle_send_error(send_result.error());\n            }\n            \n            // Step 4: 等待下一个周期\n            auto elapsed = Clock::now() - loop_start;\n            if (elapsed > period_) {\n                overrun_count_++;\n                RCLCPP_WARN(logger_, \"Control loop overrun: %ld us\",\n                    duration_cast<microseconds>(elapsed).count());\n            } else {\n                std::this_thread::sleep_until(loop_start + period_);\n            }\n        }\n    }\n    \nprivate:\n    void handle_read_error(RobotError err) {\n        consecutive_errors_++;\n        if (consecutive_errors_ > 3) {\n            emergency_stop();\n        }\n    }\n    \n    void handle_send_error(RobotError err) {\n        if (err == RobotError::COLLISION) {\n            emergency_stop();\n        }\n    }\n    \n    void emergency_stop() {\n        running_.store(false);\n        robot_.send_zero_torques();\n        RCLCPP_ERROR(logger_, \"EMERGENCY STOP triggered!\");\n    }\n    \n    std::atomic<bool> running_{true};\n    int consecutive_errors_{0};\n    int overrun_count_{0};\n    Duration period_{milliseconds(1)};  // 1kHz\n};\n```\n\n## 六、本节小结\n\n**核心决策规则**：\n1. **1kHz 实时路径** → 错误码 / std::expected（零开销）\n2. **非实时服务层** → std::expected / optional（类型安全）\n3. **初始化 / 配置** → 异常（简洁）\n4. **永远标记 [[nodiscard]]** → 防止忽略错误\n5. **关键错误通过 atomic flag** → 跨线程传递急停信号\n\n**延伸阅读**：\n- [本模块 sec-10 RAII 与资源管理](/learn/cpp-fundamentals/10)\n- [本模块 sec-11 多线程编程](/learn/cpp-fundamentals/11)\n- [实时系统编程与 RT-Linux](/learn/cpp-fundamentals/15)\n"
}