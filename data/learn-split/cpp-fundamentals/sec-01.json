{
  "title": "C++ 性能优化：从微基准到 SIMD",
  "difficulty": "高级",
  "content": "## C++ 性能优化\n\nC++ 的核心优势是性能。但\"写 C++\"不等于\"快\"——本节讲解真正让 C++ 代码跑得飞快的技术。\n\n### 1. 测量先于优化\n\n**核心原则**：Don't guess, measure.\n\n**微基准**（Google Benchmark）：\n```cpp\n#include <benchmark/benchmark.h>\n#include <vector>\n#include <Eigen/Dense>\n\nstatic void BM_MatrixMultiply(benchmark::State& state) {\n    int n = state.range(0);\n    Eigen::MatrixXd A = Eigen::MatrixXd::Random(n, n);\n    Eigen::MatrixXd B = Eigen::MatrixXd::Random(n, n);\n    \n    for (auto _ : state) {\n        Eigen::MatrixXd C = A * B;\n        benchmark::DoNotOptimize(C);\n    }\n    state.SetComplexityN(n);\n}\nBENCHMARK(BM_MatrixMultiply)->RangeMultiplier(2)->Range(8, 1024)->Complexity();\nBENCHMARK_MAIN();\n```\n\n**性能分析工具**：\n- `perf`（Linux）：CPU 性能计数器\n- Intel VTune / AMD uProf：详细 profiling\n- `valgrind --tool=callgrind`：调用图\n- `gprof`：传统 profiler\n\n### 2. 编译器优化\n\n**优化级别**：\n| Flag | 说明 | 典型加速 |\n|------|------|:-------:|\n| -O0 | 无优化 | baseline |\n| -O1 | 基础优化 | 2-3x |\n| -O2 | 标准生产 | 3-5x |\n| -O3 | 激进（含 SIMD）| 4-8x |\n| -Ofast | 违反严格浮点 | 5-10x |\n| -march=native | CPU 特定指令 | +20-50% |\n| -flto | 链接时优化 | +5-15% |\n\n**生产推荐**：`-O2 -march=native -flto -DNDEBUG`\n\n### 3. 缓存友好性\n\nCPU 缓存层级：\n- L1 缓存：~32KB，延迟 4 周期\n- L2 缓存：~256KB，延迟 12 周期\n- L3 缓存：~8MB，延迟 40 周期\n- 主内存：GB 级，延迟 200+ 周期\n\n**缓存行**：64 字节，访问时整行加载。\n\n**坏例子**（列主序访问行主序数组）：\n```cpp\n// 每次跳 N*sizeof(double) 字节 → 缓存不命中\nfor (int j = 0; j < N; ++j)\n    for (int i = 0; i < N; ++i)\n        sum += matrix[i][j];\n```\n\n**好例子**：\n```cpp\nfor (int i = 0; i < N; ++i)\n    for (int j = 0; j < N; ++j)\n        sum += matrix[i][j];  // Sequential access\n```\n\n### 4. 数据布局优化\n\n**AoS vs SoA**：\n```cpp\n// Array of Structures (AoS) - 对象编程自然\nstruct Point { double x, y, z, w; };\nstd::vector<Point> points(N);\n\n// Structure of Arrays (SoA) - SIMD 友好\nstruct Points {\n    std::vector<double> x, y, z, w;\n    void reserve(size_t n) { x.reserve(n); y.reserve(n); z.reserve(n); w.reserve(n); }\n};\n```\n\n**向量化 SoA**：编译器可自动生成 AVX/SSE 指令：\n```cpp\n// 编译器自动向量化（AVX2 处理 4 个 double）\nfor (size_t i = 0; i < N; ++i) {\n    result[i] = points.x[i] * 2.0 + points.y[i];\n}\n```\n\n### 5. SIMD（单指令多数据）\n\n手写 SIMD 指令（AVX2 / AVX-512）：\n```cpp\n#include <immintrin.h>\n\nvoid add_vectors_avx(const double* a, const double* b, double* c, size_t n) {\n    size_t i = 0;\n    for (; i + 3 < n; i += 4) {\n        __m256d va = _mm256_loadu_pd(a + i);\n        __m256d vb = _mm256_loadu_pd(b + i);\n        __m256d vc = _mm256_add_pd(va, vb);\n        _mm256_storeu_pd(c + i, vc);\n    }\n    // Tail\n    for (; i < n; ++i) c[i] = a[i] + b[i];\n}\n```\n\n一次处理 4 个 double（AVX2）或 8 个（AVX-512）→ 4-8x 加速。\n\n### 6. 移动语义与完美转发\n\n**避免不必要的拷贝**：\n```cpp\n// BAD: 两次拷贝\nstd::string func() {\n    std::string result = \"hello\";\n    return result;  // (RVO 通常能避免)\n}\n\n// GOOD: 显式移动\nstd::string func() {\n    std::string result = \"hello\";\n    return std::move(result);\n}\n\n// 接收参数：传值 + 移动\nclass Container {\n    std::vector<int> data_;\npublic:\n    void set_data(std::vector<int> d) {  // 值传递，调用者可移动传入\n        data_ = std::move(d);\n    }\n};\n```\n\n### 7. 常量传播与内联\n\n```cpp\n// constexpr - 编译期计算\nconstexpr double dh_matrix_det(double a, double alpha) {\n    return cos(alpha) - a * sin(alpha);  // Computed at compile time\n}\n\n// inline - 消除函数调用开销（热点函数）\ninline double dot(const double* a, const double* b, int n) {\n    double sum = 0;\n    for (int i = 0; i < n; ++i) sum += a[i] * b[i];\n    return sum;\n}\n```\n\n### 8. 机器人中的性能场景\n\n| 场景 | 关键优化 |\n|------|---------|\n| 正运动学计算 | 内联 + 小矩阵专用代码 |\n| 控制循环（1kHz）| 避免内存分配 / 缓存友好 |\n| 点云处理 | SIMD + SoA |\n| 碰撞检测 | BVH + 并行 |\n| 图像处理 | OpenCV (内部已优化) |\n| 稀疏线代 | 压缩存储 + 并行 |\n\n### 练习题\n\n1. **微基准**：用 Google Benchmark 测量矩阵乘法在 n=16/64/256/1024 下的性能。\n2. **缓存实验**：对比行主序 vs 列主序遍历 1024x1024 矩阵的时间差。\n3. **SIMD**：实现一个 AVX2 版本的向量点积，对比标量版本。\n4. **AoS vs SoA**：将 1M 个 3D 点用 AoS 和 SoA 存储，测量遍历+累加性能。\n5. **编译选项**：同一份代码分别用 -O0/-O2/-O3/-march=native 编译，对比 benchmark。",
  "lastUpdated": "2026-04-30T11:33"
}