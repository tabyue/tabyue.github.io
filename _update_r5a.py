# -*- coding: utf-8 -*-
import json, datetime

TIMESTAMP = datetime.datetime.now().strftime("%Y-%m-%dT%H:%M")
BASE = "data/learn/"

UPDATES = [
    {
        "file": "3d-vision-pointcloud.json",
        "title": "透明/反光物体的深度感知与补全",
        "content": "### 深度相机的致命弱点\n\n结构光和ToF深度相机在透明物体（玻璃杯、塑料瓶）和反光物体（金属件、镜面）上会产生严重的深度缺失或错误。这在机器人抓取中是高频问题——厨房场景中30%+物体涉及透明/反光。\n\n### 问题根源\n\n```\n结构光(RealSense D435):\n  原理: 投射红外点阵 → 观测畸变 → 三角测量\n  透明物体: 红外光穿透 → 无反射 → 深度=0\n  反光物体: 镜面反射 → 点阵偏移 → 深度错误\n\nToF(Azure Kinect):\n  原理: 发射调制光 → 测量相位差 → 计算距离\n  透明物体: 光穿透 → 测到后面物体的深度\n  反光物体: 多路径反射 → 深度噪声大\n```\n\n### 解决方案\n\n| 方法 | 原理 | 精度 | 速度 |\n|------|------|------|------|\n| **深度补全网络** | RGB引导补全缺失深度 | 高 | 实时 |\n| **偏振相机** | 偏振光反射特性推理 | 中 | 实时 |\n| **多视角融合** | 多角度观察消除遮挡 | 高 | 慢 |\n| **触觉探测** | 接触后获取精确形状 | 最高 | 最慢 |\n| **NeRF/3DGS** | 神经隐式重建 | 高 | 离线 |\n\n```python\nimport numpy as np\n\nclass DepthCompletionSimulator:\n    '''深度补全模拟器\n    \n    模拟透明物体场景下的深度缺失和补全\n    '''\n    def __init__(self, H=480, W=640):\n        self.H, self.W = H, W\n    \n    def create_scene(self):\n        '''创建带透明物体的模拟场景'''\n        depth_gt = np.ones((self.H, self.W)) * 0.8  # 桌面0.8m\n        # 不透明杯子 (有深度)\n        cy, cx, r = 200, 200, 40\n        Y, X = np.ogrid[:self.H, :self.W]\n        mask_cup = (Y-cy)**2 + (X-cx)**2 < r**2\n        depth_gt[mask_cup] = 0.5  # 杯子表面0.5m\n        \n        # 透明玻璃杯 (深度相机看不到!)\n        cy2, cx2, r2 = 200, 400, 35\n        mask_glass = (Y-cy2)**2 + (X-cx2)**2 < r2**2\n        depth_gt[mask_glass] = 0.5  # 真实深度0.5m\n        \n        # 模拟深度相机观测: 透明物体区域深度缺失\n        depth_obs = depth_gt.copy()\n        depth_obs[mask_glass] = 0  # 透明区域=0 (缺失)\n        noise = np.random.randn(self.H, self.W) * 0.002\n        depth_obs = np.where(depth_obs > 0, depth_obs + noise, 0)\n        \n        return depth_gt, depth_obs, mask_glass\n    \n    def rgb_guided_completion(self, depth_obs, rgb_edges):\n        '''RGB引导的深度补全 (简化版)\n        \n        思路: 用RGB边缘信息指导深度插值\n        实际用TransCG/ClearGrasp等深度补全网络\n        '''\n        depth_filled = depth_obs.copy()\n        missing = depth_obs == 0\n        \n        # 简化: 用邻域非零深度的均值填充\n        from scipy.ndimage import uniform_filter\n        depth_sum = uniform_filter(np.where(missing, 0, depth_obs), size=15)\n        count = uniform_filter((~missing).astype(float), size=15)\n        count = np.maximum(count, 1e-6)\n        depth_filled[missing] = depth_sum[missing] / count[missing]\n        \n        return depth_filled\n\ntry:\n    from scipy.ndimage import uniform_filter\n    sim = DepthCompletionSimulator()\n    gt, obs, glass_mask = sim.create_scene()\n    filled = sim.rgb_guided_completion(obs, None)\n    \n    err_before = np.abs(obs[glass_mask] - gt[glass_mask]).mean()\n    err_after = np.abs(filled[glass_mask] - gt[glass_mask]).mean()\n    print(f'透明物体区域:')\n    print(f'  补全前误差: {err_before:.4f}m (深度=0, 完全缺失)')\n    print(f'  补全后误差: {err_after:.4f}m')\n    print(f'  缺失像素: {glass_mask.sum()} / {glass_mask.size}')\nexcept ImportError:\n    print('需要 scipy 运行此示例')\n    print('实际方案: ClearGrasp/TransCG深度补全网络')\n```\n\n### 练习\n1. 用RealSense拍摄透明物体，分析深度图缺失区域\n2. 实现基于RGB的深度补全（用预训练的补全网络）\n3. 对比补全前后的抓取规划成功率"
    },
    {
        "file": "cloud-edge-collaboration.json",
        "title": "WebRTC远程遥操与低延迟视频传输",
        "content": "### 为什么需要远程遥操？\n\n远程遥操作（Teleoperation）让操作员可以在千里之外控制机器人。应用场景：危险环境（核电站/深海）、远程医疗手术、远程数据采集（分布式众包）。\n\n### 延迟要求\n\n| 场景 | 最大容忍延迟 | 关键技术 |\n|------|-------------|----------|\n| 远程手术 | <50ms | 专线+5G |\n| 遥操作采集 | <100ms | WebRTC |\n| 远程监控 | <500ms | RTSP/HLS |\n| 远程规划 | <2s | HTTP API |\n\n### WebRTC遥操架构\n\n```\n操作端 (Web浏览器/VR)        机器人端\n┌──────────────┐         ┌──────────────┐\n│ 视频显示     │ ←WebRTC← │ 相机         │\n│ 控制输入     │ →DataCh→ │ 动作执行     │\n│ (手柄/键盘)  │         │ 状态上报     │\n└──────────────┘         └──────────────┘\n        ↕ STUN/TURN ↕\n     ┌──────────────────┐\n     │ 信令服务器        │\n     │ (WebSocket)       │\n     └──────────────────┘\n\nWebRTC优势:\n  - 浏览器原生支持, 无需安装客户端\n  - P2P直连, 延迟最低(同局域网<10ms)\n  - 自适应带宽, 动态调整视频质量\n  - DataChannel: 可靠/不可靠两种模式\n```\n\n```python\nimport time\nimport json\n\nclass TeleoperationServer:\n    '''遥操作服务器框架\n    \n    处理: 视频流编码 + 控制指令解码 + 状态上报\n    '''\n    def __init__(self, control_hz=50, video_fps=30):\n        self.control_hz = control_hz\n        self.video_fps = video_fps\n        self.latency_buffer = []  # 延迟监测\n    \n    def encode_state(self, joint_positions, gripper, force_feedback):\n        '''编码机器人状态 -> 发给操作端'''\n        state = {\n            'timestamp': time.time(),\n            'joints': list(joint_positions),\n            'gripper': gripper,\n            'force': list(force_feedback),\n        }\n        # 实际通过WebRTC DataChannel发送\n        return json.dumps(state)\n    \n    def decode_command(self, message):\n        '''解码操作端指令'''\n        cmd = json.loads(message)\n        # 测量往返延迟\n        if 'send_time' in cmd:\n            rtt = time.time() - cmd['send_time']\n            self.latency_buffer.append(rtt)\n            if len(self.latency_buffer) > 100:\n                self.latency_buffer.pop(0)\n        return cmd.get('action', [0]*7)\n    \n    def get_latency_stats(self):\n        if not self.latency_buffer:\n            return {}\n        import numpy as np\n        arr = np.array(self.latency_buffer) * 1000  # ms\n        return {\n            'mean_ms': float(np.mean(arr)),\n            'p95_ms': float(np.percentile(arr, 95)),\n            'p99_ms': float(np.percentile(arr, 99)),\n        }\n    \n    def adaptive_control(self, action, latency_ms):\n        '''延迟自适应控制\n        \n        高延迟时自动降低运动速度, 保证安全\n        '''\n        import numpy as np\n        action = np.array(action)\n        if latency_ms > 200:\n            scale = 0.3  # 高延迟: 慢速\n        elif latency_ms > 100:\n            scale = 0.6  # 中延迟: 中速\n        else:\n            scale = 1.0  # 低延迟: 全速\n        return (action * scale).tolist()\n\n# 演示\nserver = TeleoperationServer()\n\n# 模拟控制循环\nimport numpy as np\nfor i in range(50):\n    # 编码状态\n    state_msg = server.encode_state(\n        joint_positions=np.random.randn(7) * 0.1,\n        gripper=0.5,\n        force_feedback=np.random.randn(6) * 0.01\n    )\n    \n    # 模拟接收指令\n    cmd = json.dumps({\n        'action': (np.random.randn(7) * 0.01).tolist(),\n        'send_time': time.time() - np.random.uniform(0.01, 0.15)\n    })\n    action = server.decode_command(cmd)\n\nstats = server.get_latency_stats()\nprint(f'遥操作延迟统计:')\nprint(f'  平均: {stats[\"mean_ms\"]:.1f}ms')\nprint(f'  P95:  {stats[\"p95_ms\"]:.1f}ms')\nprint(f'  P99:  {stats[\"p99_ms\"]:.1f}ms')\n\n# 延迟自适应\nfor lat in [30, 120, 250]:\n    scaled = server.adaptive_control([0.1]*7, lat)\n    print(f'  延迟{lat}ms -> 速度缩放: {scaled[0]/0.1:.0%}')\n```\n\n### 练习\n1. 用WebRTC搭建浏览器遥操作界面, 实时查看机器人相机画面\n2. 实现延迟自适应控制: 高延迟时自动降速\n3. 比较WebRTC和ROS2 DDS在跨网络场景的延迟差异"
    },
    {
        "file": "computer-vision.json",
        "title": "开放词汇检测与Grounding：让机器人理解任意物体描述",
        "content": "### 传统检测 vs 开放词汇检测\n\n```\n传统目标检测 (YOLO/Faster R-CNN):\n  训练时定义类别: ['cup', 'plate', 'bottle', ...] (80类COCO)\n  只能检测训练过的类别\n  遇到新物体 → 无法检测\n\n开放词汇检测 (Grounding DINO / OWL-ViT):\n  输入: 图像 + 任意文本描述\n  输出: 对应物体的边界框\n  例: '红色的带把手的杯子' → 检测到!\n  例: '看起来脏了的盘子' → 检测到!\n  无需重新训练, 理解任意语言描述\n```\n\n### Grounding DINO\n\nGrounding DINO 是当前最强的开放词汇检测器，将DINO检测器与语言-视觉对齐结合：\n\n| 特性 | YOLO | Grounding DINO |\n|------|------|----------------|\n| 词汇量 | 固定(80/365类) | 无限(任意文本) |\n| 新类别 | 需要重新训练 | 零样本 |\n| 描述粒度 | 类别名称 | 自然语言描述 |\n| 速度 | 极快(2ms) | 较慢(50ms) |\n| 精度 | 高(已知类) | 高(通用) |\n\n### 机器人中的应用Pipeline\n\n```\n语音指令: '帮我拿桌上那个蓝色的水杯'\n     ↓\nGrounding DINO: '蓝色的水杯' → 检测到 bbox [x1,y1,x2,y2]\n     ↓\nSAM: bbox → 精确分割 mask\n     ↓\n深度图: mask区域 → 3D位置\n     ↓\n抓取规划: 3D位置 → 6DoF抓取位姿\n     ↓\n执行抓取\n```\n\n```python\nimport numpy as np\n\nclass OpenVocabDetector:\n    '''开放词汇检测器 (模拟Grounding DINO)\n    \n    实际使用:\n      from groundingdino.util.inference import load_model, predict\n      model = load_model('GroundingDINO_SwinT_OGC.py', 'groundingdino_swint_ogc.pth')\n      boxes, logits, phrases = predict(model, image, text_prompt, box_threshold=0.3)\n    '''\n    def __init__(self):\n        self.scene_objects = []  # 模拟场景中的物体\n    \n    def set_scene(self, objects):\n        '''设置场景物体 (模拟)'''\n        self.scene_objects = objects\n    \n    def detect(self, text_query, threshold=0.3):\n        '''开放词汇检测\n        \n        输入: 自然语言描述\n        输出: [(bbox, confidence, matched_text)]\n        '''\n        results = []\n        query_words = set(text_query.lower().split())\n        \n        for obj in self.scene_objects:\n            # 简化匹配 (实际用CLIP特征余弦相似度)\n            obj_words = set(obj['description'].lower().split())\n            overlap = len(query_words & obj_words)\n            score = overlap / max(len(query_words), 1)\n            \n            if score > threshold:\n                results.append({\n                    'bbox': obj['bbox'],\n                    'confidence': min(score, 0.99),\n                    'label': obj['name'],\n                })\n        \n        results.sort(key=lambda x: x['confidence'], reverse=True)\n        return results\n\n# 模拟场景\ndetector = OpenVocabDetector()\ndetector.set_scene([\n    {'name': 'red_cup', 'description': 'red cup with handle ceramic', 'bbox': [100,150,180,250]},\n    {'name': 'blue_bottle', 'description': 'blue water bottle plastic tall', 'bbox': [300,100,350,300]},\n    {'name': 'white_plate', 'description': 'white plate ceramic round dirty', 'bbox': [200,200,350,250]},\n    {'name': 'laptop', 'description': 'black laptop computer screen keyboard', 'bbox': [400,50,600,200]},\n])\n\nqueries = [\n    'red cup',\n    'blue bottle',\n    'dirty plate',\n    'something to drink from',  # 抽象描述\n    'electronic device',         # 类别级描述\n]\n\nprint('开放词汇检测结果:')\nfor q in queries:\n    results = detector.detect(q, threshold=0.1)\n    if results:\n        top = results[0]\n        print(f'  \"{q}\" -> {top[\"label\"]} (conf={top[\"confidence\"]:.2f})')\n    else:\n        print(f'  \"{q}\" -> 未检测到')\n\nprint(f'\\n核心优势: 无需训练, 输入任意文本即可检测')\nprint(f'机器人场景: 用户说什么就能找什么')\n```\n\n### 练习\n1. 安装Grounding DINO, 在真实图像上测试开放词汇检测\n2. 实现Grounding DINO + SAM的自动分割Pipeline\n3. 将开放词汇检测集成到机器人抓取系统中"
    },
    {
        "file": "vla-models.json",
        "title": "VLA评估基准与系统性测试方法",
        "content": "### 为什么需要标准化评估？\n\nVLA领域缺乏统一的评估标准，不同论文用不同的任务、机器人、指标，结果难以横向对比。\n\n### 主流评估基准\n\n| 基准 | 任务类型 | 环境 | 机器人 | 指标 |\n|------|---------|------|--------|------|\n| **SIMPLER** | 桌面操作 | 仿真(MuJoCo) | WidowX/Franka | 成功率 |\n| **RLBench** | 18个操作任务 | 仿真(CoppeliaSim) | Franka | 成功率 |\n| **CALVIN** | 长序列操作 | 仿真 | Franka | 链式成功率 |\n| **Language Table** | 语言条件推桌面 | 仿真+真机 | xArm | 成功率+语言对齐 |\n| **真机评估** | 自定义 | 真实 | 各种 | 成功率+安全 |\n\n### 系统性评估框架\n\n```python\nimport numpy as np\nfrom typing import Dict, List\n\nclass VLAEvaluator:\n    '''VLA模型系统性评估框架'''\n    \n    def __init__(self, model_name):\n        self.model_name = model_name\n        self.results = {}\n    \n    def eval_task(self, task_name, n_episodes=100, success_fn=None):\n        '''评估单个任务'''\n        successes = []\n        episode_lengths = []\n        \n        for ep in range(n_episodes):\n            # 模拟执行\n            length = np.random.randint(10, 100)\n            success = np.random.random() > 0.3  # 模拟70%成功率\n            successes.append(success)\n            episode_lengths.append(length)\n        \n        sr = np.mean(successes)\n        self.results[task_name] = {\n            'success_rate': sr,\n            'mean_length': np.mean(episode_lengths),\n            'n_episodes': n_episodes,\n        }\n        return sr\n    \n    def eval_generalization(self, base_task, variations):\n        '''评估泛化能力: 同一任务的不同变体'''\n        gen_results = {}\n        for var_name in variations:\n            sr = self.eval_task(f'{base_task}_{var_name}', n_episodes=50)\n            gen_results[var_name] = sr\n        \n        mean_sr = np.mean(list(gen_results.values()))\n        gen_gap = gen_results.get(variations[0], 0) - gen_results.get(variations[-1], 0)\n        return gen_results, mean_sr, gen_gap\n    \n    def eval_robustness(self, task_name, perturbations):\n        '''评估鲁棒性: 加入干扰后的性能衰减'''\n        baseline_sr = self.eval_task(task_name, n_episodes=50)\n        robust_results = {'baseline': baseline_sr}\n        \n        for pert_name, pert_level in perturbations.items():\n            # 模拟: 干扰越大, 成功率越低\n            degraded_sr = baseline_sr * (1 - pert_level * 0.5)\n            degraded_sr = max(0, degraded_sr + np.random.randn() * 0.05)\n            robust_results[pert_name] = degraded_sr\n        \n        return robust_results\n    \n    def report(self):\n        '''生成评估报告'''\n        print(f'\\n=== {self.model_name} 评估报告 ===')\n        print(f'{\"Task\":30s} {\"SR\":>8s} {\"Episodes\":>10s}')\n        print('-' * 52)\n        for task, res in self.results.items():\n            print(f'{task:30s} {res[\"success_rate\"]:>7.1%} {res[\"n_episodes\"]:>10d}')\n        \n        overall = np.mean([r['success_rate'] for r in self.results.values()])\n        print(f'{\"Overall\":30s} {overall:>7.1%}')\n\n# 评估示例\nevaluator = VLAEvaluator('OpenVLA-7B')\n\n# 基础任务评估\nfor task in ['pick_cube', 'place_on_plate', 'push_to_target', 'open_drawer']:\n    evaluator.eval_task(task, n_episodes=100)\n\n# 泛化评估\ngen_res, mean_sr, gap = evaluator.eval_generalization(\n    'pick_cube', \n    ['seen_object', 'unseen_color', 'unseen_shape', 'unseen_background']\n)\nprint(f'泛化测试: 平均SR={mean_sr:.1%}, 泛化Gap={gap:.1%}')\n\n# 鲁棒性评估\nrobust = evaluator.eval_robustness('pick_cube', {\n    'lighting_change': 0.3,\n    'camera_shift': 0.4,\n    'object_perturbation': 0.2,\n    'distractor_objects': 0.5,\n})\nprint(f'鲁棒性: {robust}')\n\nevaluator.report()\n```\n\n### 评估最佳实践\n\n```\n1. 统计显著性: 每个任务至少100 episodes\n2. 置信区间: 报告均值 +/- 标准误\n3. 消融实验: 逐个移除模块, 量化贡献\n4. 失败分析: 分类失败原因(检测错误/规划错误/执行错误)\n5. 计算效率: 报告推理延迟和吞吐量\n```\n\n### 练习\n1. 在SIMPLER基准上评估OpenVLA, 复现论文指标\n2. 设计一个新的评估任务, 测试长序列操作能力\n3. 实现自动失败分析: 用VLM判断失败原因"
    },
    {
        "file": "sim-to-real.json",
        "title": "自适应Domain Randomization：从固定随机化到学习随机化",
        "content": "### DR的核心困境\n\n传统Domain Randomization (DR) 的参数范围靠人工设定：\n- 范围太窄 → 不覆盖真实值 → 迁移失败\n- 范围太宽 → 策略过于保守 → 性能下降\n\n**Adaptive DR** 自动学习最优的随机化范围。\n\n### 三种自适应DR方法\n\n| 方法 | 原理 | 代表工作 |\n|------|------|----------|\n| **ADR** | 逐步扩大范围直到策略性能下降 | OpenAI (2019) |\n| **BayesSim** | 贝叶斯推断匹配真实数据 | Ramos (2019) |\n| **SimOpt** | 优化仿真参数匹配真实轨迹 | Chebotar (2019) |\n\n### ADR (Automatic Domain Randomization)\n\n```\nADR 算法:\n  初始: 每个参数 p 的范围 [lo, hi] 很窄\n  循环:\n    1. 在当前范围内随机采样训练\n    2. 评估策略在极端参数下的性能\n    3. 如果性能 > 阈值 → 扩大范围\n       如果性能 < 阈值 → 缩小范围\n    4. 重复\n  \n  结果: 自动找到策略能handle的最大DR范围\n  → 覆盖真实值的概率最大化\n```\n\n```python\nimport numpy as np\n\nclass AdaptiveDR:\n    '''自适应Domain Randomization (ADR)'''\n    \n    def __init__(self, param_names, initial_ranges, \n                 performance_threshold=0.8, expand_rate=0.05):\n        self.names = param_names\n        self.ranges = {n: list(r) for n, r in zip(param_names, initial_ranges)}\n        self.threshold = performance_threshold\n        self.expand_rate = expand_rate\n        self.history = []\n    \n    def sample(self):\n        '''从当前范围采样参数'''\n        params = {}\n        for name, (lo, hi) in self.ranges.items():\n            params[name] = np.random.uniform(lo, hi)\n        return params\n    \n    def update(self, boundary_performance):\n        '''根据边界性能更新范围\n        \n        boundary_performance: {param_name: (lo_perf, hi_perf)}\n        '''\n        for name in self.names:\n            lo_perf, hi_perf = boundary_performance.get(name, (1.0, 1.0))\n            lo, hi = self.ranges[name]\n            mid = (lo + hi) / 2\n            span = hi - lo\n            \n            # 下界: 性能好则扩大(更低), 差则缩小\n            if lo_perf > self.threshold:\n                self.ranges[name][0] = lo - span * self.expand_rate\n            else:\n                self.ranges[name][0] = lo + span * self.expand_rate\n            \n            # 上界: 同理\n            if hi_perf > self.threshold:\n                self.ranges[name][1] = hi + span * self.expand_rate\n            else:\n                self.ranges[name][1] = hi - span * self.expand_rate\n        \n        self.history.append({n: tuple(r) for n, r in self.ranges.items()})\n    \n    def summary(self):\n        print('ADR 当前随机化范围:')\n        for name, (lo, hi) in self.ranges.items():\n            span = hi - lo\n            print(f'  {name:20s}: [{lo:.3f}, {hi:.3f}] (span={span:.3f})')\n\n# 示例\nadr = AdaptiveDR(\n    param_names=['friction', 'mass_scale', 'action_delay_s', 'camera_noise'],\n    initial_ranges=[(0.4, 0.6), (0.9, 1.1), (0.0, 0.01), (0.0, 0.01)],\n)\n\nprint('初始范围:')\nadr.summary()\n\n# 模拟5轮ADR更新\nfor round_i in range(5):\n    # 在边界参数值处评估策略性能 (模拟)\n    perf = {}\n    for name in adr.names:\n        lo_p = max(0.5, 1.0 - round_i * 0.05 + np.random.randn()*0.05)\n        hi_p = max(0.5, 1.0 - round_i * 0.05 + np.random.randn()*0.05)\n        perf[name] = (lo_p, hi_p)\n    \n    adr.update(perf)\n\nprint(f'\\n5轮ADR后:')\nadr.summary()\nprint(f'\\n范围自动扩大 → 策略鲁棒性提升 → Sim2Real Gap缩小')\n```\n\n### 练习\n1. 实现ADR, 在IsaacLab中自动调整DR范围训练四足行走策略\n2. 实现BayesSim: 用真实轨迹贝叶斯推断仿真参数后验分布\n3. 对比固定DR和自适应DR在Sim2Real迁移中的成功率差异"
    }
]

for update in UPDATES:
    filepath = BASE + update["file"]
    with open(filepath, "r", encoding="utf-8") as f:
        data = json.load(f)
    existing_titles = [s.get("title","") for s in data.get("sections", [])]
    if update["title"] in existing_titles:
        print(f"SKIP {update['file']}: already exists")
        continue
    new_section = {"title": update["title"], "lastUpdated": TIMESTAMP, "content": update["content"]}
    data["sections"].append(new_section)
    data["lastUpdated"] = TIMESTAMP
    with open(filepath, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
    print(f"OK {update['file']}: +'{update['title']}' ({len(data['sections'])}sec)")

lp_path = "data/learning-path.json"
with open(lp_path, "r", encoding="utf-8") as f:
    lp = json.load(f)
lp["lastUpdated"] = TIMESTAMP
with open(lp_path, "w", encoding="utf-8") as f:
    json.dump(lp, f, ensure_ascii=False, indent=2)
print(f"\nOK learning-path.json -> {TIMESTAMP}")
print("Round 5: 5/10 done")
