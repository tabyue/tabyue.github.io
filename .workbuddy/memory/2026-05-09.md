# 2026-05-09 工作日志

## 每日英文 v2 升级（10:24）
- 用户反馈：v1（3 条 paper-only）英文风格偏紧，建议加权威资讯类英文
- 升级：3 条 → 6 条混合素材，每条新增 `level`（CEFR A2-C1）+ `register`（文体）字段
- 文体类型扩展：paper / news / blog / tip 四种（v1 只有 paper / news / tip 三种）
- 真实素材（v2 首批 6 条）：
  - [B2/Industry News] MIT Tech Review hidden human labor 调查
  - [B2/Tech Blog] NVIDIA generalist-specialist 论
  - [C1/IEEE Spectrum] 2025-26 humanoid stack 战略分析
  - [C1/arXiv Abstract] OA-WAM
  - [C1/arXiv Abstract] Latent Bridge
  - [B1/Writing Tip] Hedging Words 写作小贴士
- 前端：
  - 新增 .de-tag.blog（橙色）.de-tag.tip（蓝色）+ .de-level / .de-meta-top 类
  - 标签彩色区分：PAPER 紫 / NEWS 绿 / BLOG 橙 / TIP 蓝
  - 每张卡片头部展示 [类型 + CEFR 等级 + register 来源]，用户能立刻判断是否符合自己水平
  - 显示数量从 3 → 6（slice(0,6)）
- JSON 新增 `sources` 数组列出 10 大权威源池（automation-5 维护规则参考）
- MEMORY.md 数据规范段落更新 daily-english.json 字段定义（v2 schema）
- 待 commit/push

## automation-5 凌晨轮（05:50）
- 投稿箱：无开放 Issue
- 新闻 +3 (n244-n246)：BAAI RoboOS-NeXT 跨本体多机协作框架 + 蚂蚁灵波 LingBot-World 全量开源 + 北京科博会 2026 开幕（具身天工 3.0 + 6m 招财 + 海信牵头人机交互国标）
- 论文 +2 (p131-p132)：VILAS（<$15K 低成本 VLA 平台 + 折纸软夹爪）+ RoboOS-NeXT（STEM 三层共享记忆 + Brain-Cerebellum）；含 keyInsights+impact+detail
- 开源 +1 (os091)：robbyant/lingbot-world（Apache-2.0，分钟级长时 + 16 FPS 实时世界模型）
- 招聘 +1 (j114)：蚂蚁灵波 26 社招 + 校招（多模态生成 / 实时推理 / 机器人仿真三方向）
- 学习 +1 sec：control-theory +sec-15「多机器人协作控制：RoboOS-NeXT 范式与 STEM 共享记忆」（联动 p132 论文，模块 81K → 90K chars，脱离 5/3 最旧状态）
- 全部 JSON 校验通过 0 dups（news 246 / papers 120 / os 84 / jobs 114）
- sync_learn_split + build_sitemap（424 chapters）已跑

## 每日英文 v3：双数组归档结构（10:38）
- 用户问"是持续积累的还是覆盖的？"——v2 半积累半覆盖（保留 4-5 条 + 新增 1-2 条，被挤出的丢失）
- 升级：双数组结构 `{items, archive}`
  - `items`：当前展示 6 条上限
  - `archive`：所有曾经出现过的条目，**永不删除**（上限 200 条）
  - 每条新增 `id`（md5 hash 前缀 `de-`）+ `firstShown`
- 前端新增"📚 往期回顾"折叠区（默认收起，点击展开 archive 中已被挤出 items 的旧条目）
  - de-archive-toggle 按钮 + de-arch-arrow ▸/▾ + de-arch-count 计数 chip
  - de-archive-grid 重用 .de-card 样式，opacity:.92 视觉略弱化区分
- automation-5 维护流程更新（写入 MEMORY.md）：
  - 新条目计算 id 先查 archive 避免回归重复
  - 被挤出 items 的旧条目保持在 archive 不动
  - archive >200 条时按 firstShown 删最早
- v3 首批 archive=6（与当前 items 相同），随每次自动化运行不断积累

## 每日英文 v3.1：音标查询（10:48）
- 用户问"能否补音标"——直接 JSON 写死有问题（缩写无标准 IPA / 多义词上下文不同 / 维护成本高）
- 改用**点击实时查询**方案：
  - vocab 词点击 → fetch dictionaryapi.dev 拿 IPA + 真人发音 mp3（免费 API，CORS 允许）
  - 弹出 ipa-pop popover：单词 + IPA + 🔊 播放按钮 + 词义 + 中文释义 + 跳转剑桥/有道/韦氏链接
  - mark.de-vocab（英文段落里的高亮词）+ vocab-list 中的 b 元素都支持点击
  - 查询结果缓存 _ipaCache，同词不重复请求
  - API 找不到（缩写/词组）时 fallback 到外链（剑桥/有道/Wiktionary）
  - 点击 popover 外面或 ✕ 按钮关闭
- CSS：.ipa-pop 浮层 + .ipa-phon（IPA 字体 'Doulos SIL' / 'Charis SIL' fallback Georgia）+ .ipa-play 圆形按钮 + .ipa-links 外链 chips
- 标题副标题更新："点击生词查音标 🔊" 提示用户新交互

## 每日英文 v3.2：全文任意单词可查（10:55）
- 用户反馈：希望非高亮的普通词也能查音标
- 实现：英文段落 token 化
  - Step 1: 先用占位符替换 vocab 高亮（避免 wrap 时切错 mark）
  - Step 2: 把剩余英文单词（≥3 字母）包成 `<span class="de-w" onclick="showIpaPopover(...)">word</span>`
  - Step 3: 占位符还原为 mark
- 视觉：.de-w hover 时显示 dotted 下划线 + 文字变蓝（提示可点），不影响阅读流
- 共用 showIpaPopover：mark / b / de-w 三种元素都触发同一个 popover
- 外部点击关闭逻辑更新：判断 .de-vocab / .de-w / vocab-list b 都不触发关闭
- 标题副标题改为"点击任意单词查音标 🔊"

## 全面去重审计（11:03）
- 用户截图发现「开源生态 → 开发框架」FluxVLA Engine 出现两次（os046 / os056，逐际动力 LimX 同项目，4 天前后被分别收录）
- 顺手对 4 大数据数组做严格查重审计（URL + github 仓库路径 + 标题前缀），共发现 46 条重复：
  - **news -28**：URL 完全相同 17 组 / 标题前缀 11 组（如 4 篇报道第三届中国具身智能大会、3 篇报道苏州大会、3 篇报道它石智航融资）→ 保留最新最完整的版本
  - **papers -6**：同一篇 arXiv 论文先后两次入库（VILAS / WholeBodyVLA / ABot-M0 / UnifoLM-VLA / Mechanistic Co-Training / Gemini-ER），同时删除对应 papers/pXXX.json 详情文件
  - **opensource -4**：FluxVLA / HY-Embodied-0.5 / AGIBOT WORLD / Embodied-R1 各重复一次
  - **jobs -8**：URL 同的同岗位先后录入 / 标题完全一样的同岗
- 最终条目数：news 218 / papers 114 / opensource 80 / jobs 106
- MEMORY.md 升级**严格查重四要素**规则：① URL 严格匹配 ② github owner/repo 匹配 ③ 标题前缀 12 字符 ④ arXiv id 匹配。任何一级命中即取消新增/合并到已有。这条规则**强制**要求 automation-5 下次跑前先做四级查重，避免再次累积重复。
- sitemap 重建（424 chapters）+ 全部 JSON 校验通过

## 每日英文 v3.3：标题也支持点击查音标（11:15）
- 用户反馈：标题（h3）里的英文单词没法点
- 修复：抽出 `wrapWords()` + `renderTitle()` 函数，标题用 `renderEn(title, vocab)` 同样处理（vocab 标题里命中也高亮）
- CSS：扩展 `.de-card h3 .de-w` + `.de-card h3 .de-vocab` 选择器与 .de-en 一致，hover 蓝色 + dotted 下划线提示

## 每日英文 v3.4：标题去掉 vocab 蓝底高亮（11:28）
- 用户反馈：标题上叠 vocab 蓝底高亮看上去怪
- 改用 `wrapWords()`（仅单词可点）替代 `renderEn()`（带 vocab 高亮）：标题保留点击查音标能力，但**不**叠加蓝底 mark 高亮
- 删除 `.de-card h3 .de-vocab` CSS 规则
- 视觉更干净：标题加粗大字 + 默认色不变，hover 时 dotted 下划线提示可点

## automation-5 中午轮（13:30）
- 投稿箱：无开放 Issue（gh issue list 返回 []）
- 新闻 +3 (n248-n250)：Figure 03 BotQ 时产 1 台（24x 提速）+ S0 视觉融合楼梯零样本 / 优必选 Thinker-WM 登顶 Libero 长程 / 蓝点触控年销 10 万套关节扭矩 + 6 维力 80% 国内市占率
- 论文 +1 (p133)：IVLR (Tsinghua/BIT/Xiaomi)「文图交错推理轨迹」LIBERO-Long 92.4% SOTA；含 keyInsights + impact + 完整 detail（methodology / experiments / reproduction / mathDetails）
- 招聘 +1 (j115)：蓝点触控 — 算法 / 嵌入式 / 工艺工程 / 客户接入 / 航天民用化五大方向
- 学习 +1 sec：robot-kinematics +sec-1「全身运动学：从机械臂到 30+ DOF 人形机器人」（联动 Figure S0 / Optimus V3 量产 / KAI 115 DOF），模块从 81K → 89K chars，**脱离最薄区**
- 跨实例自检：news 222 / papers 115 / os 81 / jobs 107 全部 0 dups
- sync_learn_split + build_sitemap (10 板块 + 31 模块 + 425 chapters) 已跑
- commit 7175c11 已 push origin/main

## 投稿处理（11:43）
- 用户直接贴链接（GitHub Issues 投稿箱无新条目）：
  1. https://www.longgang.ai/posts/cmnzenwx900045w97p49jdbx4 — 微软 Agent Framework 1.0 发布资讯
  2. https://github.com/microsoft/agent-framework — 同项目 GitHub 仓库
- 处理：同一项目的「资讯 + 开源」两面，分别入两个板块互相引用
  - 新闻 +1（n247）：微软 Agent Framework 1.0 = AutoGen + Semantic Kernel 合并，10.3K Star，与具身智能 Embodied-Agent 模块联动
  - 开源 +1（os092）：framework 分类，10.3K Star / MIT / Python+C#+TS，graph workflow 5 种编排模式 + MCP 集成
- inbox.json processedItems +2 记录
- 校验：JSON OK，无 id 重复（news 219 / os 81）
- sitemap 重建 424 chapters

## daily-english popover +中文释义（14:08）
- 用户截图反馈：popover 现在只有英文 def，希望加中文翻译
- 实现：新增 `_zhCache` + `fetchZhTranslation(word)` 调用 https://api.mymemory.translated.net 免费翻译 API（无 key、CORS 允许）
- showIpaPopover 逻辑：vocab 自带 cn 时优先显示原 cn；否则异步去 MyMemory 拿中文，与英文 def 并列展示
- 错误兜底：API 返回 PLEASE SELECT / invalid / 原文 时不渲染中文
- commit ca7bd18 已 push

## daily-english popover 中文释义修复（14:28）
- 用户截图：单一 MyMemory API 仍只显示英文（API 不稳定 / CORS / 国内可能被限流）
- 修复：fetchZhTranslation 改成多源串行 fallback：
  ① Google Translate gtx 公开端点（client=gtx，CORS 允许，无 key，国内可达，最稳）
  ② MyMemory
  ③ Lingva 公共镜像 lingva.ml / translate.plausibility.cloud / lingva.lunar.icu
- 任何一个返回非空且非错误字串就用，全部失败才置空 _zhCache 走原文
- 同时把英文 def 也翻译成中文：新增 .ipa-def-zh（🇨🇳 + dashed 浅边框分隔），与英文 def 并列展示
- isInvalid 过滤增强：please select / invalid / ^error / 404 not / mymemory warning + 译文与原词全等的情况
- commit e70d4a8 已 push origin/main

## learn 模块「推荐资源」复活（15:12）
- 用户问：以前学习中心有推荐学习资源，现在没了？
- 排查：learning-path.json 每个 module.resources 字段还在（31 模块 31 个全有），但 5/6 升级 B 方案到「单章节翻页模式」时，_renderCurrentSection() 没读这个字段
- 修复：每章正文末尾、底部翻页栏前加 <details class='mod-resources'> 折叠区
  - 第 1 章默认展开 open，其他章节默认折叠避免重复看
  - 兼容 string / {name,url,description} 两种 schema
  - URL 自动 anchor 化（regex 替换 https?:// 链接）
  - 标题显示条数：「📖 本模块推荐学习资源（N 项）」
- CSS：.mod-resources 卡片化 + ▸/▾ 箭头 + dotted 下划线链接
- commit efbf0cf 已 push origin/main

## learn 推荐资源 modId 匹配修复（15:16）
- 用户反馈：上一版还是看不到
- 排查：modId 实际有 3 套形态：① 中文（侧边栏转义）② 英文（learnFileMap.value）③ 学习文件里的 d.id
- 上一版只对比了一种，多数模块匹配不上 → 静默不渲染
- 修复：tryMids = Set([modId, d.id, d.name 转义]) + 反查 learnFileMap 把英文 → 中文 key
- 还加了 m.name === d.name 直接比对兜底
- commit 40dbb58 已 push

## learn 推荐资源 v3 + 首尾跳转（15:25）
- 用户截图反馈：上一版 modId 多源 fallback 还是不显示
- **真正的根因**：以为 resources 在 learning-path.json，**其实学习模块自身的 d.resources 字段更全更结构化**（31 模块全有 {name, url, type, description} 对象）
- 重写：优先读 `d.resources`（来自 learn-split/<mod>/_index.json），找不到才回落到 learning-path.json
- 视觉升级：type 标签 chip（课程 / 视频 / 书 等彩色） + 链接加粗 + desc 浅色副文本
- 用户同时要求：每章加首尾跳转
- 实现：底部翻页栏下方加一行 `.sec-jump`：⏮ 跳到开头 / 第 N / M 章 / 跳到结尾 ⏭
- 仅当模块 ≥3 章时出现；首章隐藏「跳到开头」，末章隐藏「跳到结尾」
- commit e86e7cf 已 push

## learn 翻页栏 5 段合并（15:37）
- 用户截图：上一版有两条独立翻页栏（sec-nav + sec-jump），结尾侧两个按钮（下一章+跳到结尾），开头反而只有一个 → 视觉错乱
- 重构：删除 .sec-jump 独立栏，把首尾跳转作为 ⏮/⏭ 圆形小按钮（.sec-edge-btn）集成进 .sec-nav 主栏
- 布局：[⏮ 圆按钮] [← 上一章 标题] [N/M 进度] [下一章 → / 末章封面] [⏭ 圆按钮]
- 第一章 ⏮ disabled / 最后一章 ⏭ disabled，灰显但占位保持对称
- .sec-nav-btn 加 max-width:42% + ellipsis 防长标题挤出布局
- flex-wrap:wrap 让窄屏自动换行
- commit 89f411c 已 push

## learn 章节内跳转重做（15:44）
- 用户截图标红范围：章节标题下方区域，说"开头位置能看到跳到结尾，结尾位置能看到回到开头"
- 之前理解成章节间（⏮⏭ 跳第一章 / 最后章），完全是误解
- 撤销 sec-edge-btn / sec-nav 5 段式 改回原版三段
- 重做章节内跳转：
  - sec-inpage-jump top：在章节标题 + 更新提示之后、正文之前，右对齐「跳到本章结尾 ⏬」
  - sec-inpage-jump bottom：在正文末尾，左对齐「⏫ 回到本章开头」
  - 隐形锚点 #secAnchorTop / #secAnchorBottom + _jumpInSec(ev, anchorId) 平滑 scrollTo（不动 URL hash）
  - 视觉：dashed 圆角 chip，hover 变实线主题色，低调不抢正文
- commit b1a24c8 已 push

## automation-5 5/9 晚间轮（20:10）
- 投稿箱：无开放 Issue
- 新闻 +3：Figure Helix-02 双机铺床「单一神经网络多人形协作」/ 腾讯云 OpenClaw 落地家庭具身（乐享 Zeroth M1 首款接入）/ 浙江人形 + 杰克 2000 台服装订单
- 论文 +2：p134 From Pixels to Tokens (RUC 4 种潜动作监督统一对比，开源) + p135 STRONG-VLA (28 类扰动 benchmark + 解耦微调，OpenVLA-OFT +14.5%)
- 开源 +1 (os093)：RUCKBReasoning/From_Pixels_to_Tokens
- 招聘 +1 (j116)：浙江人形 2000 台订单核心团队 5 大方向
- 学习 +1 sec：ros2 模块 +sec「多机协作 ROS2 范式」联动 Helix-02，99K → 109K chars
- 0 dups (news 225 / papers 117 / os 82 / jobs 108)
- commit 0d15f3f pushed
