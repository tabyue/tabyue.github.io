# 2026-05-08 工作日志

## automation-5 早间轮（08:30）
- 投稿箱：无开放 Issue
- 新闻 +4 / 论文 +2 / 开源 +2 / 招聘 +2
- 学习中心：mechanical-design 模块 +sec-17（80K → 87K chars，脱离最薄区）
  - 主题：全身触觉皮肤工程，联动 n234 KAI 发布会 18000 触点前沿
  - 涵盖五大触觉传感原理（电阻 / 电容 / 压电 / 光学 GelSight / 磁式 ReSkin）+ 工程难题（信号通路 / 抗串扰 / 柔性走线 / 标定漂移）+ VLA 融合 + 16×16 实战搭建
- commit 5c35c3d 已推送

## automation-5 下午轮（17:20）
- 投稿箱：无开放 Issue（gh issue list 返回 []）
- 新闻 +3 (n238-n240)：智元 G2 龙旗工厂 99.5% 产线落地 + 蚂蚁完成大晓机器人战投工商落地 + Realbotix 北美 19 台 Q2 交付
- 论文 +2 (p127-p128)：OA-WAM（Object-Addressable WAM，slot intervention 0.87）+ AnchorVLA（Anchored Diffusion 移动操作 35ms 闭环）；都含 keyInsights+impact+detail
- 开源 +1 (os089)：UQ-CVMR/AnchorVLA
- 招聘 +1 (j112)：大晓机器人 ACE / 开悟世界模型 / 超级大脑 A1 算法岗
- 学习 +1 sec：vla-models 模块新增 sec-12「Object-Addressable WAM 与锚定扩散：VLA 鲁棒性 + 推理效率前沿（2026-05 最新突破）」（11 → 12 secs，118K → 121K chars）
  - 三篇 5 月新论文（OA-WAM / Latent Bridge / AnchorVLA）合讲，对应 VLA 三大瓶颈：鲁棒性 / 推理延迟 / 多模态保留 + 闭环
  - 含完整代码骨架（slot attention reset / DeltaPredictor 桥接 / Anchored Diffusion + Residual head）+ 实战路径（入门 1-2 天 / 进阶 1-2 周 / 研究级 2-3 个月）
- 全部 JSON 校验通过，无 id 重复（news 240 / papers 116 / os 82 / jobs 112）
- sync_learn_split（2 files updated）+ build_sitemap（407 chapters）已跑

## UI 修复（20:01）
- 学习中心侧边栏：阶段五/六/七点击展开后被 renderLearn 强制打回收起
- 根因：`renderLearn` 内 `si>3?'collapsed':''` 硬编码每次重渲染都重置，没记录用户展开状态。任何后续动作（打开模块 / 切章节 / 进度刷新）触发的 renderLearn 都把它收回
- 修复：引入 `_stageCollapsed` 字典 + `toggleStage(si,el)` 方法记录状态；`openLearnModule` 中"自动展开父阶段"也同步写入字典，保持单一真相源
- commit e4d2767 已 push

## UI 修复 follow-up（20:08）
- 切章节锚定问题：`switchToSection` 只 `body.scrollTop=0`（容器内），但 window 没动 → 视觉上原地不动。改为切完后 `window.scrollTo` 到 learnDetail 容器 top - 56px（粘性导航条 48 + 8 padding），smooth 行为
- 左侧目录默认两级展开：`_isStageCollapsed` 默认值从 `si>3` 改为 `false`，所有 7 个阶段默认全展开；用户主动收起的状态仍然保留在 `_stageCollapsed` 字典里
- commit 7371e2b 已 push
